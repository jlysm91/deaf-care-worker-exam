# WORKLOG

## 95. 2026-06-19 실전 모의시험 오류 문항 전원 정답 처리와 재시험 정리 기준 확인

- 사용자 제보에 따라 실전 모의시험 기준 오류 확인 문항 `11`, `37`, `56`번을 전원 정답 처리하도록 반영했다.
  - 대상 로컬/GitHub 파일:
    - `mock-exam/desktop-trial.html`
    - `mock-exam/mobile-trial.html`
    - `online-class/trial-exam/desktop-trial.html`
    - `online-class/trial-exam/mobile-trial.html`
  - 대상 OneDrive 최종 관리 파일:
    - `03. 모의시험\2-1. ★★★ 실전 모의시험 코드 - 데스크톱용.html`
    - `03. 모의시험\2-2. ★★★ 실전 모의시험 코드 - 모바일용.html`
- 변경 내용:
  - `FORCE_CORRECT_TRIAL_NUMBERS=[11,37,56]`를 추가하고, 제출 채점과 결과 분석 양쪽에서 해당 문항은 선택 여부나 선택 답안과 관계없이 정답으로 계산하게 했다.
  - 11번은 필기, 37번과 56번은 실기 점수에 반영된다.
  - 해당 문항이 미응답이어도 결과 분석의 미응답 목록에 넣지 않아, 오류 문항 때문에 완료 판정이 막히지 않도록 했다.
  - Firebase `result` 저장값에 `answeredCount`, `submittedAt`, `forceCorrectTrialNumbers`를 추가해 이후 재시험/중복 제출 정리 기준을 남기도록 했다.
- 기존 제출 데이터 확인:
  - Firebase `exams/examTrial/users`를 읽기 전용으로 확인했다.
  - 현재 기준 사용자 노드 17개, `result` 7건, 완료 결과 3건, 미완료 결과 4건, `progress`만 있는 기록 10건, 80문항 진행 기록만 있는 사례 1건, 70~79문항 진행 기록만 있는 사례 1건, 재시험으로 보이는 중복 아이디 묶음 2건을 확인했다.
  - 기존 `result`에는 제출 시각이 없으므로 과거 기록끼리는 정확한 최신 시각 비교가 불가능하다. 같은 아이디로 다시 제출한 경우에는 Firebase 구조상 최신 제출이 기존 `result`를 덮어쓴다.
  - Google Form에는 새 기록을 전송하거나 기존 기록을 수정하지 않았다. 중복/재시험 정리는 별도 미리보기 파일을 만든 뒤 승인 후 반영해야 한다.
- 검증:
  - `git diff --check` 통과.
  - 실전 모의시험 로컬/온라인 학습반용 데스크톱·모바일 HTML inline script 문법 파싱 통과.
  - 실전 모의시험 로컬/온라인 학습반용 데스크톱·모바일 HTML `quizRawData` 80문항 확인.
  - 4개 파일 모두 `11`, `37`, `56` 강제 정답 처리, `answeredCount`, `submittedAt` 저장 로직 존재 확인.
  - `node tools/result-page-check.mjs` 통과: 전체 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합.
  - 로컬 `mock-exam`, 온라인 학습반 `online-class/trial-exam`, OneDrive 최종 데스크톱·모바일 파일 SHA-256 일치 확인.
  - SHA-256:
    - 데스크톱 로컬/온라인 학습반/OneDrive: `FD488FA70A99F3E4844C6962C2526F287641E4EF1FD875C204F82D7B8E971E78`
    - 모바일 로컬/온라인 학습반/OneDrive: `33FEF1605CFA24210898D847E6F1C8E8BD6A0E78A3AA981E2E2958674D3EDF15`

## 94. 2026-06-19 실전 모의시험 제출 결과 Google Form 기록 연결

- 사용자 요청에 따라 실전 모의시험 `제출하기` 버튼을 누르면 Google Form 결과지에 이름, 총점, 합격 여부가 기록되도록 연결했다.
  - 결과지 공개 응답 URL: `https://docs.google.com/forms/d/e/1FAIpQLSdmoDKrE48-xFYOhVyrpmAx9DvfML3qMmwlcbefwWL7XhpD_w/viewform`
  - 대상 로컬/GitHub 파일:
    - `mock-exam/desktop-trial.html`
    - `mock-exam/mobile-trial.html`
    - `online-class/trial-exam/desktop-trial.html`
    - `online-class/trial-exam/mobile-trial.html`
  - 대상 OneDrive 최종 관리 파일:
    - `03. 모의시험\2-1. ★★★ 실전 모의시험 코드 - 데스크톱용.html`
    - `03. 모의시험\2-2. ★★★ 실전 모의시험 코드 - 모바일용.html`
- 변경 내용:
  - 기존 실전 모의시험 코드에 남아 있던 이전 Google Form URL을 새 결과지 `formResponse` URL로 교체했다.
  - Google Form 필드 ID를 공개 응답 HTML에서 확인해 반영했다.
    - 이름: `entry.295783163`
    - 총점: `entry.516479247`
    - 합격 여부: `entry.516661777`
  - 총점 기록 형식을 `총점점 (필기 n점 / 실기 n점)`으로 정리했고, 이름은 `getCurrentUserName()` 기준으로 기록하게 했다.
  - `제출 완료` 알림보다 Google Form 전송을 먼저 시작하도록 순서를 조정했다.
- 검증:
  - 새 Google Form `viewform` 및 `formResponse` URL HTTP 200 확인.
  - 공개 Form HTML에서 세 `entry.*` ID 존재 확인 및 코드 반영값 일치 확인.
  - 실전 모의시험 로컬/온라인 학습반용 데스크톱·모바일 HTML inline script 문법 파싱 통과.
  - 실전 모의시험 로컬/온라인 학습반용 데스크톱·모바일 HTML `quizRawData` 80문항 확인.
  - 로컬 `mock-exam`, 온라인 학습반 `online-class/trial-exam`, OneDrive 최종 데스크톱·모바일 파일 SHA-256 일치 확인.
  - 테스트용 가짜 응답 제출은 결과지 오염을 막기 위해 실행하지 않았다.
  - SHA-256:
    - 데스크톱 로컬/온라인 학습반/OneDrive: `6F4BC5021412BAC5B28415F173A85A6FCD9E31D60BA6E00D7C944BED4B61C42C`
    - 모바일 로컬/온라인 학습반/OneDrive: `EDE38E232E110C990257213BD46676D8A11060E23285ECA65F9AD3B2CB73C1F3`

## 93. 2026-06-19 올리사이트 본창 실전 모의시험 완료 표시 단순화

- 사용자 요청에 따라 실전 모의시험 학습 결과는 GitHub Pages 문제풀이 화면에서만 보이게 하고, 올리사이트 메인페이지(로그인 창)는 완료 문구만 표시하도록 OneDrive 로그인 코드를 수정했다.
  - 대상 OneDrive 최종 관리 파일:
    - `03. 모의시험\1. ★★★ 실전 모의시험 로그인 코드.html`
- 변경 내용:
  - 완료 감지 시 `olShowResult()` 점수표를 띄우지 않고 `ol_done_notice`만 표시하도록 변경했다.
  - 완료 문구를 `학습을 완료하였습니다.` / `고생 많으셨습니다.`로 고정했다.
  - GitHub Pages용 실전 모의시험 결과 화면은 그대로 유지했다.
- 검증:
  - OneDrive 로그인 코드 inline script 문법 파싱 통과.
  - Playwright + 로컬 Chrome으로 `mockExamComplete` 메시지를 재현해 올리사이트 본창에서 `ol_done_notice` 표시, `ol-result` 숨김, 필기/실기 결과 문구 미노출을 확인했다.
  - OneDrive 로그인 코드 SHA-256: `2898C312B00B72C2EBED135CE49398CF7EE1A61C6C47500D38B0557FE4F52E33`

## 92. 2026-06-19 실전 모의시험 완료 안내 기준을 80문항 제출로 변경

- 사용자 요청에 따라 실전 모의시험은 합격/불합격 여부보다 80문항을 끝까지 풀고 제출했다는 사실을 학습 완료 안내 기준으로 삼도록 OneDrive 로그인 코드를 수정했다.
  - 대상 OneDrive 최종 관리 파일:
    - `03. 모의시험\1. ★★★ 실전 모의시험 로그인 코드.html`
- 변경 내용:
  - 올리사이트 메인페이지(로그인 창)의 완료 결과 카드 주 문구를 `학습 완료 버튼을 눌러주세요.` / `고생 많으셨습니다.`로 변경했다.
  - 불합격 완료 데이터도 `학습 완료` 버튼이 표시되고 활성화되도록 완료 기준을 `completed && 미응답 0건`으로 바꿨다.
  - 결과 카드에는 합격/불합격 여부와 필기/실기 점수 요약은 계속 표시한다.
  - 진행 중/미완료 안내의 `불합격이면 복습 후 다시 풀어 주세요.` 문구를 `80문항을 모두 풀고 제출하면 학습 완료 버튼을 누를 수 있습니다.`로 정리했다.
  - GitHub Pages용 실전 모의시험 데스크톱/모바일 파일은 이미 제출 완료 시 부모창에 완료 결과를 전송하고 있으므로 이번 항목에서는 수정하지 않았다.
- 검증:
  - OneDrive 로그인 코드 inline script 문법 파싱 통과.
  - Playwright + 로컬 Chrome으로 OneDrive 로그인 코드에 `mockExamComplete` 메시지를 직접 재현했다.
    - 불합격 완료 예시: `학습 완료 버튼을 눌러주세요.`, `고생 많으셨습니다.` 표시 및 `학습 완료` 버튼 활성화 확인.
    - 합격 완료 예시: 동일 안내 문구 표시 및 `학습 완료` 버튼 활성화 확인.
  - OneDrive 로그인 코드 SHA-256: `701615694838DEB310BB485FE9B81CAA9A34BAB61EF5C08BFA3B9AAF43EC17D8`

## 91. 2026-06-19 실전 모의시험 결과 초기화 버튼 제거

- 사용자 요청에 따라 실전 모의시험 결과 유지를 위해 결과 화면의 `다시 풀기` 버튼과 결과 초기화 경로를 제거했다.
  - 대상 로컬/GitHub 파일:
    - `mock-exam/desktop-trial.html`
    - `mock-exam/mobile-trial.html`
    - `online-class/trial-exam/desktop-trial.html`
    - `online-class/trial-exam/mobile-trial.html`
  - 대상 OneDrive 최종 관리 파일:
    - `03. 모의시험\1. ★★★ 실전 모의시험 로그인 코드.html`
    - `03. 모의시험\2-1. ★★★ 실전 모의시험 코드 - 데스크톱용.html`
    - `03. 모의시험\2-2. ★★★ 실전 모의시험 코드 - 모바일용.html`
- 변경 내용:
  - 실전 모의시험 결과 페이지에서 `result-return-btn` 버튼과 전용 CSS를 제거했다.
  - `resetQuiz()` 함수와 `result-return-btn` 클릭 이벤트 연결을 제거해 `progress`/`result`를 삭제하고 문제풀이로 되돌아가는 경로를 없앴다.
  - OneDrive 로그인 코드의 `ol-retry-btn` 버튼과 `olRetry()` 함수를 제거해 `/result.json`, `/progress.json` DELETE 요청 경로를 없앴다.
  - 입장 거부/입력 오류 상태의 `다시 입력하기` 버튼은 결과 초기화 기능이 아니므로 유지했다.
- 검증:
  - 실전 모의시험 로컬/온라인 학습반용 데스크톱·모바일 HTML에서 `result-return-btn`, `resetQuiz`, `초기화되었습니다`, `child("result").remove` 잔여 문자열 0건 확인.
  - OneDrive 로그인 코드에서 `ol-retry-btn`, `olRetry`, `/result.json`, `/progress.json`, `다시 풀기` 잔여 문자열 0건 확인.
  - 실전 모의시험 로컬/온라인 학습반용 데스크톱·모바일 HTML과 OneDrive 로그인 코드 inline script 문법 파싱 통과.
  - 로컬 `mock-exam`, 온라인 학습반 `online-class/trial-exam`, OneDrive 최종 데스크톱·모바일 파일 SHA-256 일치 확인.
  - SHA-256:
    - 데스크톱 로컬/온라인 학습반/OneDrive: `FCA244C2AF506EE8E57383F13044D2D58E48A895B0D20718ABE9DCED20AD7E49`
    - 모바일 로컬/온라인 학습반/OneDrive: `1BDCDF6BB2E4E3A61709DB98635A1FAA448953A4FE94C929D5187529821E5FBF`
    - OneDrive 로그인: `1F15D047AA247E0060D20E7003D86EF768B9933A09D8AFE32CFD25A3916B3633`

## 90. 2026-06-19 실전 모의시험 제출 결과를 올리사이트 본창에 표시

- 사용자 요청에 따라 실전 모의시험 1~80번 제출 직후 올리사이트 메인 로그인 창에서도 합격/불합격 여부와 필기/실기 점수를 요약 표시하도록 수정했다.
  - 대상 로컬/GitHub 파일:
    - `mock-exam/desktop-trial.html`
    - `mock-exam/mobile-trial.html`
    - `online-class/trial-exam/desktop-trial.html`
    - `online-class/trial-exam/mobile-trial.html`
  - 대상 OneDrive 최종 관리 파일:
    - `03. 모의시험\1. ★★★ 실전 모의시험 로그인 코드.html`
    - `03. 모의시험\2-1. ★★★ 실전 모의시험 코드 - 데스크톱용.html`
    - `03. 모의시험\2-2. ★★★ 실전 모의시험 코드 - 모바일용.html`
- 변경 내용:
  - 실전 모의시험 제출 결과 저장값에 `written`, `practical`, `completed`, `passed`, `eligible`, `correctAnswers`를 명시적으로 포함했다.
  - 부모창 `postMessage`는 합격자뿐 아니라 80문항을 모두 푼 불합격자도 전송하도록 `eligible` 기준에서 `completed` 기준으로 완화했다.
  - OneDrive 로그인 코드는 제출 완료 결과를 감지하면 `아직 학습 완료 전입니다`가 아니라 `olShowResult()`를 호출해 합격/불합격, 총점, 필기/실기 점수, 합격 기준을 표시한다.
  - 미응답이 남은 제출 데이터만 기존 `아직 학습 완료 전입니다` 안내를 유지한다.
- 검증:
  - 실전 모의시험 로컬/온라인 학습반용 데스크톱·모바일 HTML과 OneDrive 로그인 코드 inline script 문법 파싱 통과.
  - 실전 모의시험 로컬/온라인 학습반용 데스크톱·모바일 HTML `quizRawData` 80문항 파싱 통과.
  - 정적 검증으로 4개 실전 모의시험 HTML 모두 불합격 완료 결과 전송 조건과 결과 저장 플래그 포함 확인.
  - Playwright + 로컬 Chrome으로 OneDrive 로그인 코드에 `mockExamComplete` 메시지를 직접 재현했다.
    - 불합격 예시: `불합격`, `필기 20 / 35`, `실기 27 / 45` 표시 및 완료 버튼 숨김 확인.
    - 합격 예시: `합격`, `필기 25 / 35`, `실기 40 / 45` 표시 및 완료 버튼 표시 확인.
  - SHA-256:
    - 데스크톱 로컬/온라인 학습반/OneDrive: `4786412B45FAFB405E38B46BA4386D467D56118875BBC92057E68E3FD94828FA`
    - 모바일 로컬/온라인 학습반/OneDrive: `B46F19085C14334C3DED6471720AFDFBA4E52428818D2C2D5AB8549A1AC336B9`
    - OneDrive 로그인: `C49F1AED68EAD19614B4E39994AA1124596B61574C2E8ACA0549C1740639E677`

## 89. 2026-06-19 실전 모의시험 관리자 등록 없이 즉시 입장 허용

- 사용자 요청에 따라 실전 모의시험은 관리자용 학습자 등록(`authorizedUsers`)에 포함하지 않아도 되도록 입장 흐름을 수정했다.
  - 대상 로컬/GitHub 파일:
    - `mock-exam/desktop-trial.html`
    - `mock-exam/mobile-trial.html`
    - `online-class/trial-exam/desktop-trial.html`
    - `online-class/trial-exam/mobile-trial.html`
  - 대상 OneDrive 최종 관리 파일:
    - `03. 모의시험\1. ★★★ 실전 모의시험 로그인 코드.html`
    - `03. 모의시험\2-1. ★★★ 실전 모의시험 코드 - 데스크톱용.html`
    - `03. 모의시험\2-2. ★★★ 실전 모의시험 코드 - 모바일용.html`
- 변경 내용:
  - 실전 모의시험 데스크톱/모바일 페이지의 `verifyAuthorizedUser()`에서 `authorizedUsers/{uid}` 조회와 `allowedScopes.mockExam` 검사를 제거했다.
  - 아이디에 Firebase key 금지 문자 `. # $ [ ] /`가 있는 경우만 막고, 그 외에는 입력한 아이디와 이름으로 바로 `examTrial` 사용자 데이터를 생성/갱신하게 했다.
  - OneDrive 로그인 코드는 `online-class/trial-exam` GitHub Pages 경로를 열도록 변경했다.
  - 로그인 버튼 진행 문구를 `등록 정보 확인 중...`에서 `입장 준비 중...`으로 변경했다.
- 검증:
  - 실전 모의시험 로컬/온라인 학습반용 데스크톱·모바일 HTML과 OneDrive 로그인 코드 inline script 문법 파싱 통과.
  - 실전 모의시험 로컬/온라인 학습반용 데스크톱·모바일 HTML `quizRawData` 80문항 파싱 통과.
  - 실전 모의시험 로컬/온라인 학습반용 데스크톱·모바일 HTML 및 OneDrive 로그인 코드에서 `authorizedUsers`, `allowedScopes`, `등록 정보 확인 중` 잔여 문자열 0건 확인.
  - Playwright + 로컬 Chrome으로 OneDrive 로그인 코드에 테스트 아이디/이름을 입력했을 때 `online-class/trial-exam/...` 새 창 URL이 열리고, `authorizedUsers` 네트워크 요청 0건임을 확인했다.
  - SHA-256:
    - 데스크톱 로컬/온라인 학습반/OneDrive: `77398C602D3020467A74C972BD5E68A5DE2146B190EF34921D7D590EBB8C204A`
    - 모바일 로컬/온라인 학습반/OneDrive: `9F69073F6DF9CF30CEB65352AE105971A95790E357521981C6F34D04AE5D6877`
    - OneDrive 로그인: `F8927246FCD50A845025015B7405D34ECC49077CBA943544BB2AFC4C83B21C6E`

## 88. 2026-06-19 온라인 학습반용 실전 모의시험 GitHub 폴더 추가

- 사용자 요청에 따라 온라인 학습반에서 직접 사용할 수 있도록 실전 모의시험 데스크톱/모바일 파일을 별도 폴더에 배치했다.
  - 추가 폴더: `online-class/trial-exam`.
  - 추가 파일:
    - `online-class/trial-exam/desktop-trial.html`
    - `online-class/trial-exam/mobile-trial.html`
  - 원본은 기존 GitHub Pages용 `mock-exam/desktop-trial.html`, `mock-exam/mobile-trial.html`이며 내용 수정 없이 그대로 복사했다.
- 검증:
  - 원본과 온라인 학습반용 복사본의 SHA-256 해시가 각각 일치함을 확인했다.
  - 데스크톱 해시: `C4F47C183E48EE2AD7DDD685F58E82782FD4CA7BD46764B73C3FAFA91C503D4A`
  - 모바일 해시: `05B1391D5513F5E1C0BF253A4B36E65B126D65FA7FD02BE303561D3F502F7A00`

## 87. 2026-06-19 실전 모의시험 80문항 생성

- 사용자 요청에 따라 실제 시험 전 연습용 `실전 모의시험`을 새로 만들었다.
  - 운영 예정 시간 맥락: 2026-06-19 금요일, 2026-06-22 월요일 19:00~21:00.
  - GitHub Pages용 파일: `mock-exam/desktop-trial.html`, `mock-exam/mobile-trial.html`.
  - Firebase 저장/권한 scope 충돌을 피하기 위해 새 `EXAM_ID`를 `examTrial`로 분리했다.
  - 로그인 UI는 현재 1회 모의고사 로그인 코드의 디자인/결과 화면 구조를 복제하고, 표시명/URL/scope만 `실전 모의시험` 기준으로 변경했다.
- 데이터 구성:
  - 1~10회 모의고사의 기존 데스크톱/모바일 `quizRawData`를 추출해 데스크톱/모바일 원본 데이터가 같은지 먼저 확인했다.
  - 각 회차에서 정확히 8문항씩, 총 80문항을 고정 시드로 섞어 구성했다.
  - 필기 35문항, 실기 45문항 구조를 유지했고 기존 합격 기준 `필기 21문항 이상`, `실기 27문항 이상` 로직을 그대로 사용한다.
  - 각 문항에는 검증용 `sourceExam`, `sourceNumber`, `sourcePosition`, `trialNumber` 메타데이터를 함께 남겼다.
- OneDrive 대응:
  - 처음 생성 위치였던 `01. 합격!! 모의고사\실전 모의시험` 폴더를 사용자 재요청에 따라 `03. 모의시험\실전 모의시험`으로 이동했다.
  - 이후 사용자가 `03. 모의시험` 최상위의 기존 복사본 파일들을 삭제하고, 완성본 4개를 `03. 모의시험` 최상위에 두었음을 확인했다.
  - 최종 위치: `03. 모의시험`.
  - 로그인 코드, 데스크톱용 코드, 모바일용 코드, 퀴즈 문제 데이터 코드 파일 4개가 최종 위치에 있다.
  - `03. 모의시험\실전 모의시험` 중첩 폴더는 남아 있지 않다.
- 검증:
  - 신규 로컬/OneDrive 데스크톱·모바일 코드 및 로그인 코드 inline script 문법 파싱 통과.
  - 신규 로컬 데스크톱/모바일 파일에서 잔여 `1회 모의고사`, `exam1`, `desktop-01.html`, `mobile-01.html`, `해당 모의고사` 문자열 0건 확인.
  - Playwright + 로컬 Chrome으로 데스크톱/모바일 신규 파일 렌더링 시 `실전 모의시험`, `examTrial`, `quizRawData 80문항` 표시와 페이지 오류 0건 확인.
  - Playwright + 로컬 Chrome으로 로그인 코드가 `mock-exam/desktop-trial.html`, `mock-exam/mobile-trial.html`, `examTrial` DB 경로를 생성함을 확인했다.
  - 로컬과 OneDrive의 데스크톱/모바일 파일 SHA-256 일치 확인.
  - SHA-256:
    - 로컬/OneDrive 데스크톱: `C4F47C183E48EE2AD7DDD685F58E82782FD4CA7BD46764B73C3FAFA91C503D4A`
    - 로컬/OneDrive 모바일: `05B1391D5513F5E1C0BF253A4B36E65B126D65FA7FD02BE303561D3F502F7A00`
    - OneDrive 로그인: `E5AAFC438B9C36F333A14758CBBFCF752F1BDDE429F2370D4EEB3CF7459D0C13`

## 86. 2026-06-19 1400제 2~4부 토론방 동적 축소 감지 동일 적용

- 사용자 확인에 따라 1부에서 확정된 토론방 패널 동적 축소 감지 보정을 1400제 2, 3, 4부 최종 로그인 코드에도 동일하게 적용했다.
  - 대상: OneDrive 1400제 `2부`, `3부`, `4부`의 `1. ★★★ n부 로그인 코드.html`.
  - 각 파일의 초기 `queueLoginLayoutFit();` 바로 다음에 `installLoginLayoutObserver();` 호출을 추가했다.
  - 글자 크기 공식, 카드 디자인, 문구, 권한 확인, URL 분기, Firebase 로직은 변경하지 않았다.
- 검증:
  - 1400제 2~4부 삽입용 로그인 코드 3개 inline script 문법 파싱 통과.
  - 2~4부 모두 `installLoginLayoutObserver()` 정의와 초기화 호출이 존재함을 확인했다.
  - Playwright + 로컬 Chrome으로 터치 태블릿 가로 조건에서 부모 영역만 `750x500 -> 377x390`으로 줄이는 동적 재현을 각 부별로 실행했다.
  - 2~4부 모두 명시적 `window.resize` 없이 `mainTitleFont 38px -> 30px`, `fits true`로 재계산됨을 확인했다.
  - 수정된 OneDrive 1400제 삽입용 로그인 코드 SHA-256:
    - 2부: `E1EA85B7F12817479392F3BDBC872D40D4F78B0E1FF3918347819E9ADE321AFB`
    - 3부: `11CA965073C3909B0AC4822A45552AC736B57622BCD4019484D6B6C374B1D289`
    - 4부: `8C74AF99E4BCFAF2EB939CE4B14D5FC0AD17B290CAE62DB63BAE75056CCA68C6`

## 85. 2026-06-19 1400제 1부 토론방 동적 축소 감지 누락 원인 수정

- 사용자 추가 제보 화면 기준으로 1회 모의고사 로그인 코드와 1400제 1부 로그인 코드를 동적 패널 축소 조건에서 다시 비교했다.
  - 정적 최종 크기 비교에서는 양쪽 스타일이 같았지만, 토론방 패널이 열린 뒤 부모 영역만 줄어드는 경우가 별도 문제였다.
  - 1회 모의고사는 초기화 시 `olInstallLoginLayoutObserver()`를 호출해 부모 크기 변화를 감시하지만, 1400제 1부는 `installLoginLayoutObserver()` 함수가 정의만 되어 있고 호출되지 않았다.
  - 그 결과 1400제 1부는 부모가 `750x500`에서 `377x390`으로 줄어도 `window.resize`가 따로 발생하지 않으면 기존 `38px` 제목/`0.98` transform이 유지되어 카드가 잘렸다.
- OneDrive 1400제 1부 최종 로그인 코드에만 `installLoginLayoutObserver();` 초기화 호출을 추가했다.
  - 글자 크기 공식, 카드 디자인, 문구, 권한 확인, URL 분기, Firebase 로직은 변경하지 않았다.
- 검증:
  - 1400제 1부 삽입용 로그인 코드 inline script 문법 파싱 통과.
  - Playwright + 로컬 Chrome으로 터치 태블릿 가로 조건에서 부모 영역만 `750x500 -> 377x390`으로 줄이는 동적 재현을 실행했다.
  - 수정 후 1400제 1부는 명시적 `window.resize` 없이도 `mainTitleFont 38px -> 30px`, `transform 0.98 -> 0.829`, `fits true`로 재계산됨을 확인했다.
  - 수정된 OneDrive 1400제 1부 삽입용 로그인 코드 SHA-256: `78A6B1270FD664F8E7BB4D75084167F95B0D340A77273CB7EA96D57D472DF5FC`.

## 84. 2026-06-19 1400제 1부 거부 카드 모의고사 1회 레이아웃 재일치

- 사용자 추가 사진 1~4번을 기준으로 1회 모의고사 로그인 코드와 1400제 1부 삽입용 로그인 코드를 다시 엄격 비교했다.
  - 직전 1400제 1부 전용 `shrinkDeniedNoticeToFit()` 보강이 모의고사와 다른 원인이었다.
  - 모의고사는 거부 상태에서 별도 텍스트 강제 축소가 아니라 기존 `fitLoginLayout()`/`scaleLoginToVisibleHeight()` 흐름으로 카드 전체 레이아웃을 맞춘다.
  - 1400제 1부에서 해당 커스텀 축소 함수와 호출을 제거해 1회 모의고사 방식으로 복귀했다.
- 검증:
  - 1400제 1부 삽입용 로그인 코드 inline script 문법 파싱 통과.
  - 1회 모의고사 대비 1400제 1부 공통 로그인 요소 15개 inline style 차이 0건 확인.
  - Playwright + Chrome으로 `750x500`, `568x390`, `377x526`, `318x280` 부모 크기에서 1회 모의고사와 1400제 1부의 카드 크기, 거부 박스 크기, 배지/제목/본문 폰트 크기, transform 값을 비교했다.
  - `750x500`, `568x390`, `377x526`에서는 양쪽 모두 fit 통과, `318x280` 극단 조건에서는 양쪽이 같은 transform/치수로 동작함을 확인했다.
  - 수정된 OneDrive 1400제 1부 삽입용 로그인 코드 SHA-256: `D84BF698F0707D1918A6688210E38806B65E803C0558163AB3F069585C4B845A`.

## 83. 2026-06-19 1400제 1부 태블릿 토론방 거부 카드 텍스트 강제 축소

- 사용자 추가 제보에 따라 범위를 1400제 1부 삽입용 로그인 코드로만 제한해 재검토했다.
  - 1회 모의고사 로그인 코드와 비교한 결과, 기본 visible-height 카드 맞춤은 들어가 있었지만 태블릿 토론방처럼 실제 하단이 가려지는 조건에서는 1400제 1부의 긴 텍스트가 체감상 그대로 커 보일 수 있었다.
  - OneDrive 1400제 1부 `1. ★★★ 1부 로그인 코드.html`의 거부 화면 compact 조건을 `panelWidth < 700 || card.clientWidth < 620` 기준으로 직접 적용했다.
  - compact 거부 화면에서는 배지 `16px`, 큰 제목 `28px`, 거부 제목 약 `18px`, 본문 `11px`까지 단계적으로 줄이고, 부제목이 남아도 강제로 숨기도록 했다.
- 검증:
  - 1400제 1부 삽입용 로그인 코드 inline script 문법 파싱 통과.
  - Playwright + Chrome으로 1회 모의고사와 1400제 1부를 `568x705`, `468x640`, `420x560` 부모 크기에서 비교했다.
  - 1400제 1부 일반 거부 상태와 부제목 강제 재표시 상태 모두 `sub=none`, 카드 fit 통과, compact 글자 크기 적용을 확인했다.
  - 수정된 OneDrive 1400제 1부 삽입용 로그인 코드 SHA-256: `7940FF513DFCB60699F57837153B81359C226C03BEDAD20B2BC43AFF1055CC6E`.

## 82. 2026-06-19 1400제 실제 로그인 페이지 잔여 부제목 및 거부 카드 축소 보정

- 사용자 추가 제보 화면을 1회 모의고사 로그인 코드와 다시 비교했다.
  - OneDrive 삽입용 1400제 1~4부 로그인 코드에는 이미 `1부. 요양보호와 인권` 부제목이 제거되어 있었지만, repo의 `quiz-1400/login.html`에는 선택된 부 입력 화면에서 해당 부제목이 남아 있었다.
  - `quiz-1400/login.html?part=1` 기준 선택된 부 화면은 `[시작하기 전에 아이디와 이름을 입력해 주세요.]`만 보이도록 수정했다.
  - `scope-denied` 문구는 repo 로그인/학습 파일과 OneDrive 대응 학습 파일에서 `해당 1400제에 등록되어 있지 않습니다.`로 통일했다.
- OneDrive 1400제 1~4부 삽입용 로그인 코드에는 1회 모의고사의 visible-height 기반 카드 맞춤 흐름 위에 거부 카드 전용 `shrinkDeniedNoticeToFit()` 보강을 추가했다.
  - 부제목이 잘못 남거나 부모 영역이 더 타이트한 경우에도 `denied_title`, `denied_msg`, `retry` 버튼 크기를 단계적으로 낮춰 카드가 부모 영역 안에 들어가도록 했다.
- 검증:
  - OneDrive 1400제 1~4부 삽입용 로그인 코드 inline script 문법 파싱 통과.
  - 로컬 `quiz-1400/login.html`, `desktop-01~04.html`, `mobile-01~04.html` inline script 문법 파싱 통과.
  - Playwright + Chrome으로 `quiz-1400/login.html?part=1`에서 입력 안내 문구 표시와 부제목 미표시를 확인했다.
  - Playwright + Chrome으로 1회 모의고사와 1400제 1부 삽입용 로그인 코드를 `568x705`, `468x640`, `420x560` 부모 크기에서 비교해 거부 상태 카드 fit 통과를 확인했다.
  - 1400제 1부 삽입용 로그인 코드에서 부제목을 강제로 다시 표시한 재현 조건에서도 카드 fit 통과를 확인했다.
  - 로컬 1400제 데스크톱/모바일 8개 파일과 OneDrive 대응 파일 8개의 줄바꿈 정규화 SHA-256 일치를 확인했다.

## 81. 2026-06-19 1400제 로그인 상태별 안내 문구 및 토론방 패널 표시 재보정

- 사용자 재확인에 따라 OneDrive 1400제 1~4부 최종 로그인 코드에서 입력 화면과 상태 안내 화면을 다시 분리했다.
  - 일반 입력 화면에는 모의고사와 동일하게 `[시작하기 전에 아이디와 이름을 입력해 주세요.]` 안내 문구를 복구했다.
  - 학습 중, 입장 거부, 학습 완료 안내 상태에서는 해당 안내 문구와 아이디/이름 입력 폼을 숨기도록 보정했다.
  - 입장 거부 메시지는 `n부. ...` 주제명 없이 `입력하신 정보는 해당 1400제에 등록되어 있지 않습니다.` 기준으로 통일했다.
- 1400제 1~4부의 거부/학습 중 카드 표시 흐름을 모의고사 로그인 코드의 `olShowDeniedStyleNotice` 방식에 맞춰 정리했다.
  - 토론방 패널처럼 좁은 부모 영역에서 거부 카드가 모의고사와 같은 크기와 배치로 표시되도록 inline style과 layout 함수 동작을 재검증했다.
  - 조기 학습 완료 버튼 차단 안내도 별도 inline 문구 대신 공통 학습 미완료 안내를 호출하도록 1~4부 모두 통일했다.
- 검증:
  - 1400제 1~4부 로그인 코드 4개 inline script 문법 파싱 통과.
  - 모의고사 2회 로그인 코드 대비 1400제 1~4부 공통 로그인 요소 15개 inline style 차이 0건 확인.
  - 1400제 1~4부 상태 전환 UI 함수 블록 SHA-256 동일 확인.
  - Unicode escape 기준으로 1400제 1~4부 소스 전체의 `n부. ...` 주제 줄 0건 확인.
  - Playwright + Chrome으로 panel/mobile/tablet 크기에서 입력 화면, scope-denied 입장 거부 화면, 학습 중 화면을 렌더링해 입력 화면 안내 문구 표시, 거부/학습 중 입력 영역 숨김, 카드 영역 fit 통과를 확인했다.

## 80. 2026-06-19 1400제 로그인 주제 줄 제거 및 토론방 패널 재검증

- 사용자 요청에 따라 OneDrive 1400제 1~4부 최종 로그인 코드에서 가운데 주제 문구 `n부. ...` 줄만 제거했다.
  - `ol1400_n_subtitle` 요소를 제거하고, 해당 요소를 다시 표시하거나 스타일링하던 잔여 참조도 제거했다.
  - 배지 `1400題 - n부`, 큰 제목, 아이디/이름 입력, 시작 버튼, 권한 확인, 학습 완료 조건, desktop/mobile URL 분기, Firebase 로직은 변경하지 않았다.
- 사용자가 첨부한 토론방 패널 화면 기준으로 모의고사 1회 로그인 코드와 1400제 1~4부 로그인 코드를 다시 비교했다.
  - 주제 줄 제거 전에는 1400제 scope-denied 화면에 주제 문구가 남아 있어 모의고사와 표시 구조가 달랐다.
  - 제거 후 1400제 1~4부 모두 토론방 패널 가정 크기에서 `입장할 수 없습니다` 카드가 부모 영역 안에 들어오는 것을 확인했다.
- 검증
  - 1400제 1~4부 로그인 코드 4개 인라인 스크립트 문법 파싱 통과.
  - 1400제 1~4부에서 `ol1400_n_subtitle`, `n부. ...` 주제 문구, `el('subtitle')` 잔여 참조 0건 확인.
  - 모의고사 1회 로그인 코드와 1400제 1~4부 로그인 코드의 공통 로그인 요소 14개 인라인 스타일이 모두 일치함을 확인.
  - 1400제 1~4부의 UI 함수 블록 해시가 모두 동일함을 확인.
  - 로컬 Chrome + Playwright로 1400제 1~4부 일반 렌더링을 데스크톱, 모바일, 태블릿 가로 화면에서 확인했고 오류 0건.
  - 로컬 Chrome + Playwright로 토론방 패널 가정 `568x790` 크기에서 1400제 1~4부 scope-denied 화면이 잘림 없이 표시됨을 확인.

## 79. 2026-06-19 1400제 로그인 UI 모의고사 기준 재동기화

- 사용자 요청에 따라 OneDrive 1400제 1~4부 최종 로그인 코드의 UI 디자인을 현재 모의고사 1~10회 로그인 코드 기준으로 다시 맞췄다.
- 1400제 1~4부 `1. ★★★ n부 로그인 코드.html` 4개에 모의고사 로그인 코드의 공통 인라인 스타일 15개, 제목 모바일 줄바꿈 마커, `denied_title` 구조, 스크롤 잠금/크기 보정/ResizeObserver 기반 로그인 레이아웃 함수를 반영했다.
- 1400제 부별 배지/주제명, `PART`, `PART_NO`, `REQUIRED_EXAM_IDS`, desktop/mobile URL 분기, Firebase 인증, 학습 완료 조건, 완료 버튼 노출 제어 로직은 유지했다.
- 로컬 `mock-exam`/`quiz-1400` 문제풀이 HTML, 문제 데이터, 정답, Firebase 저장 로직, GitHub Pages 결과 UI는 변경하지 않았다.
- 검증
  - 1400제 1~4부 로그인 코드 4개 인라인 스크립트 문법 파싱 통과.
  - 1400제 1~4부 로그인 코드의 새 UI 함수 블록 해시가 모두 동일함을 확인.
  - 모의고사 1회 로그인 코드와 1400제 1~4부 로그인 코드의 공통 로그인 요소 15개 인라인 스타일이 모두 일치함을 확인.
  - 1400제 1~4부의 `PART`, `PART_NO`, `REQUIRED_EXAM_IDS`, desktop/mobile URL 분기, 권한 scope 확인 값이 유지됨을 확인.
  - 로컬 Chrome + Playwright로 1400제 1~4부 로그인 HTML을 데스크톱, 모바일, 태블릿 가로 화면에서 렌더링해 카드/폼/버튼 표시와 콘솔 페이지 오류 0건을 확인.

## 78. 2026-06-18 1회 모의고사 안내 카드 축소 조건 보정

- 사용자 추가 설명에 따라 OneDrive 1회 모의고사 로그인 코드에서 기본 안내 화면은 기존 큰 카드 형태로 유지하고, 토론방 패널이 열려 콘텐츠 폭이 좁아진 경우에만 안내 카드를 축소하도록 다시 보정했다.
- `olNoticeLayoutWidth()`를 추가해 현재 wrapper/부모 컨테이너의 실제 표시 폭을 계산하고, `900px` 미만일 때만 compact 안내 카드(`720px` 폭)를 적용하도록 했다.
- `900px` 이상 기본 화면에서는 카드 `width:100%`, 기존 큰 글자/배지/안내 박스 크기와 16:9 중앙 정렬을 유지한다.
- 내부 스크롤 부모의 `overflow:hidden`, `scrollbar-width:none` 처리는 안내만 보이는 데스크톱 상태에서 유지해 회색 화살표/스크롤 표시가 보이지 않도록 했다.
- 로컬 `mock-exam/desktop-01.html`, `mock-exam/mobile-01.html` 문제 파일은 수정하지 않았다.
- 검증:
  - OneDrive 1회 모의고사 로그인 코드 HTML inline script 문법 검사 통과.
  - headless Edge 기본 폭 가정 `1150x780` 부모에서는 `compact:false`, 카드 `1150x647`, 제목 `62px`, 내부 스크롤 숨김, 카드가 부모 안에 들어감을 확인했다.
  - headless Edge 토론방 패널 가정 `780x450` 부모에서는 `compact:true`, 카드 `720x405`, 내부 스크롤 숨김, 반복 `resize` 후 높이 변화 0px을 확인했다.
  - 수정된 OneDrive 로그인 코드 SHA-256: `02F16034291D8B7FC1189F24F74B28A53EBE9DE540A484C31636E168D11D66F5`.

## 77. 2026-06-18 1회 모의고사 토론방 패널 대응 안내 카드 축소

- 사용자 제보에 따라 OneDrive 1회 모의고사 로그인 코드에서 토론방 패널이 열렸을 때 안내 카드가 크게 유지되어 위아래가 어색하게 잘리는 문제를 보정했다.
- 안내만 보이는 데스크톱 상태의 고정 카드 폭을 `min(100%,720px)`로 줄이고, 제목/배지/안내 박스 크기도 함께 낮춰 토론방 패널이 열린 좁은 영역에 들어가도록 했다.
- 같은 상태에서 올리사이트 내부 스크롤 부모를 `overflow:hidden`, `scrollbar-width:none`으로 잠가 토론방 아래에 보이던 회색 화살표/스크롤 표시가 나오지 않도록 했다.
- 모바일·터치·짧은 화면에서는 기존 자동 맞춤 흐름을 유지했다.
- 로컬 `mock-exam/desktop-01.html`, `mock-exam/mobile-01.html` 문제 파일은 수정하지 않았다.
- 검증:
  - OneDrive 1회 모의고사 로그인 코드 HTML inline script 문법 검사 통과.
  - headless Edge에서 토론방 패널 상황을 가정한 `780x450` 스크롤 부모를 만들고 `olShowProgressNotice()` 및 반복 `olResize()`/`resize` 이벤트를 실행했다.
  - 검증 결과 카드가 `720x405`, 부모 `overflow:hidden`, `scrollbar-width:none`, 높이 변화 0px, 카드 상하가 부모 영역 안에 들어감을 확인했다.
  - 수정된 OneDrive 로그인 코드 SHA-256: `0EBB71BDBF817D4394DBCB7E770B24B2ED309F8C0F3F2AE60988608F54D60874`.

## 76. 2026-06-18 1회 모의고사 안내 카드 재맞춤 고정 보정

- 사용자 제보에 따라 OneDrive 1회 모의고사 로그인 코드에서 토론방을 열고 닫은 뒤 진행/미완료 안내 카드가 작게 다시 맞춰지는 문제를 보정했다.
- `olIsNoticeOnlyLoginState()`, `olUseStableNoticeLayout()`, `olApplyStableNoticeLayout()`을 추가해 입력 영역이 숨겨지고 안내만 보이는 데스크톱 화면에서는 카드가 원래 큰 16:9 비율과 중앙 정렬을 유지하도록 했다.
- 모바일·터치·짧은 화면에서는 기존 자동 맞춤 흐름을 그대로 사용하도록 조건을 제한했다.
- 로컬 `mock-exam/desktop-01.html`, `mock-exam/mobile-01.html` 문제 파일은 수정하지 않았다.
- 검증:
  - OneDrive 1회 모의고사 로그인 코드 HTML inline script 문법 검사 통과.
  - headless Edge 1120px 폭 미리보기에서 `olShowProgressNotice()` 후 반복 `olResize()`/`resize` 이벤트를 실행해도 카드가 약 1080x608, `aspect-ratio:16/9`, `justify-content:center`, 높이 변화 0px로 유지됨을 확인했다.
  - 수정된 OneDrive 로그인 코드 SHA-256: `F5DDF3A206255E9B38D0B893A09A0A8C07C8E99F772E04B4572CFAC637E303D7`.

## 75. 2026-06-18 1회 모의고사 로그인 입장 후 입력 영역 숨김 테스트

- 사용자 요청에 따라 OneDrive `01. 합격!! 모의고사\01회 모의고사\02. 개선판 (完)\02. (★삭제 주의) 최종 코드\1. ★★★ 1회 모의고사 로그인 코드.html`만 테스트 대상으로 수정했다.
- 아이디/이름 검증 전과 오류/다시 입력 상태는 기존처럼 입력 영역을 유지하고, 사용자가 `모의고사 시작하기`를 눌러 시험 창에 입장한 뒤의 진행/미완료/완료 안내 상태에서는 `olSetEntryFieldsVisible(false)`로 `[시작하기 전에 아이디와 이름을 입력해 주세요.]`, 입력 폼, 시작 버튼을 숨기도록 바꿨다.
- 로컬 `mock-exam/desktop-01.html`, `mock-exam/mobile-01.html` 문제 파일은 수정하지 않았다.
- 검증:
  - OneDrive 1회 모의고사 로그인 코드 HTML inline script 문법 검사 통과.
  - `olShowProgressNotice`, `olShowIncompleteNotice`, `olShowCompletionNotice`, `olFinishLearning` 함수가 입장 후 입력 영역을 숨기도록 정적 확인했다.
  - headless Edge 로컬 미리보기에서 `olShowProgressNotice()` 실행 후 `ol-sub`/`ol-form`은 `display:none`, `ol_done_notice`는 `display:block`이며 카드 높이가 276px로 줄어드는 것을 확인했다.
  - 수정된 OneDrive 로그인 코드 SHA-256: `66E2EC4BB290AFDDFA36E4680BA7641CB0D3F7311549348B7C0D062DE75E0BD8`.

## 74. 2026-06-18 모의고사 결과 페이지 이전/다음 버튼 제거

- 사용자 확인에 따라 GitHub Pages 모의고사 1~10회 학습결과 페이지에서 의도치 않게 표시된 결과 화면 전용 `이전`/`다음` 버튼을 제거했다.
- 로컬 `mock-exam/desktop-01.html`~`desktop-10.html`, `mock-exam/mobile-01.html`~`mobile-10.html`에서 결과 영역의 `result-grid-nav`, `result-prev-btn`, `result-next-btn`, `result-page-info` 관련 HTML/CSS/JS 잔여물을 제거했다.
- OneDrive 모의고사 1~10회 최종 코드 데스크톱/모바일 20개 파일에도 같은 제거를 동기화했다.
- 문제 풀이 중 사용하는 `navigation-buttons`, `prev-btn`, `next-btn`, 빠른 문항 이동, 점수/오답 복습/다시 풀기/학습 완료 로직은 변경하지 않았다.
- 1400제, 문제 데이터, 정답, Firebase 저장 로직은 변경하지 않았다.
- 검증
  - 로컬 저장소와 OneDrive 최종 코드에서 `result-prev-btn`, `result-next-btn`, `result-grid-nav`, `result-page-info` 잔존 0건 확인.
  - 로컬 모의고사 문제 풀이용 `prev-btn`/`next-btn`/`navigation-buttons` 유지 확인.
  - 로컬 모의고사 20개 HTML 인라인 스크립트 문법 파싱 통과.
  - OneDrive 모의고사 최종 코드 20개 HTML 인라인 스크립트 문법 파싱 통과.
  - `git diff --check` 통과.

## 73. 2026-06-18 모의고사 1~10회 로그인 UI 1400제 기준 동기화

- 사용자 요청에 따라 OneDrive 모의고사 1~10회 최종 로그인 코드의 로그인 UI를 1400제 1~4부 기준과 동일하게 맞췄다.
- 1400제에서 확정된 로그인 카드 디자인과 가로 태블릿 스크롤 잠금 구조를 모의고사 10개 로그인 코드에 모두 적용했다.
  - 로그인 HTML 인라인 스타일 15개를 1400제 1부 기준과 동일하게 맞췄다.
  - 기존 모의고사 반응형 로그인 함수 블록을 `olLoginScrollLocks`, `olIsLandscapeTablet()`, `olSetLoginLandscapeScrollLock()`, `olScaleLoginToVisibleHeight()` 포함 구조로 교체했다.
  - 태블릿 가로 모드에서만 wrapper와 OlliSite 스크롤 부모를 잠그고, 세로 모드나 일반 화면에서는 잠근 스타일을 복구하도록 했다.
- 각 회차의 텍스트, `desktop-01.html`~`desktop-10.html`, `mobile-01.html`~`mobile-10.html`, `exam1`~`exam10` 저장 키, 결과 화면 로직은 유지했다.
- 1400제 로그인 코드, 로컬 문제 페이지, 문제 데이터, 정답, Firebase 저장, GitHub Pages 결과 UI는 변경하지 않았다.
- 검증
  - 모의고사 1~10회 로그인 영역 스타일 15개가 1400제 1부와 모두 동일함을 확인.
  - 모의고사 1~10회 로그인 레이아웃/스크롤 잠금 함수 블록이 모두 동일함을 확인.
  - 모의고사 1~10회 인라인 스크립트 문법 파싱 통과.
  - 각 회차의 `exam1`~`exam10`, `desktop-XX.html`, `mobile-XX.html` 값 유지 확인.

## 72. 2026-06-18 1400제 2~4부 가로 태블릿 스크롤 잠금 확대 적용

- 사용자 확인 결과 1부 시험 적용이 정상 동작해, 같은 보정을 OneDrive 1400제 2, 3, 4부 최종 로그인 코드에도 적용했다.
- 1부에서 검증된 `isLandscapeTablet()`, `loginScrollLocks`, `setLoginLandscapeScrollLock()`, 가로 태블릿용 `transform: scale(...)` 보정 블록을 2, 3, 4부에 동일하게 복제했다.
- 태블릿 가로 모드에서만 wrapper와 OlliSite 스크롤 부모를 잠그고, 세로 모드나 일반 화면에서는 잠근 스타일을 복구하도록 했다.
- 부별 제목, 주제명, 저장 키, 문제 목록 등 각 부별 콘텐츠와 동작 값은 유지했다.
- 모의고사 로그인 코드, 로컬 문제 페이지, 문제 데이터, 정답, Firebase 저장, GitHub Pages 결과 UI는 변경하지 않았다.
- 검증
  - 1400제 1~4부 모두 새 스크롤 잠금 마커 10건씩 반영 확인.
  - 1~4부의 로그인 레이아웃/스크롤 잠금 함수 블록이 모두 동일함을 확인.
  - 1~4부 로그인 코드 인라인 스크립트 문법 파싱 통과.

## 71. 2026-06-18 1400제 1부 가로 태블릿 스크롤 잠금 시험 적용

- 사용자 요청에 따라 전체 반영 전 시험 대상으로 OneDrive 1400제 1부 최종 로그인 코드만 수정했다.
- 태블릿 가로 모드에서 상하 스크롤이 생기지 않도록 1부 로그인 코드에 제한적으로 보정을 추가했다.
  - `isLandscapeTablet()`으로 `pointer: coarse`, `orientation: landscape`, `min-width: 700px`, `max-height: 900px` 조건을 분리했다.
  - 가로 태블릿일 때만 카드 전체를 실제 보이는 높이의 88% 기준으로 `transform: scale(...)` 처리한다.
  - 같은 조건에서 login wrapper는 `overflow:hidden`으로 바꾸고, OlliSite의 스크롤 부모는 `overflow-y:hidden`과 `overscroll-behavior:none`으로 잠근다.
  - 세로 모드나 일반 화면으로 돌아오면 잠근 부모 스타일을 원래 값으로 복구한다.
- 1400제 2, 3, 4부 로그인 코드, 모의고사 로그인 코드, 로컬 문제 페이지, 문제 데이터, 정답, Firebase 저장, GitHub Pages 결과 UI는 변경하지 않았다.
- 검증
  - 새 스크롤 잠금 마커가 1부에만 있고 2, 3, 4부에는 없는 것을 확인했다.
  - 1부 로그인 코드 인라인 스크립트 문법 파싱 통과.

## 70. 2026-06-18 1400제 3부 로그인 UI 1부 기준 재동기화

- 사용자 확인에 따라 1400제 1부와 3부 로그인 코드를 다시 비교했다.
- OneDrive 1400제 3부 최종 로그인 코드에만 적용했다.
  - 3부에 남아 있던 가로 태블릿용 `available*0.90` 축소 보정 차이를 제거했다.
  - `visibleLoginHeight()`부터 `fitLoginLayout()`까지의 UI 레이아웃 계산 블록을 1부와 완전히 동일하게 맞췄다.
  - 상단 HTML 인라인 `style` 속성 15개가 1부와 3부에서 모두 동일한 것을 확인했다.
- 3부/주제명/저장 키/문제 목록처럼 부별 콘텐츠와 동작에 필요한 값은 유지했다.
- 1400제 1, 2, 4부 로그인 코드, 모의고사 로그인 코드, 로컬 문제 페이지, 문제 데이터, 정답, Firebase 저장, GitHub Pages 결과 UI는 변경하지 않았다.
- 검증
  - 1부와 3부의 로그인 UI 레이아웃 함수 블록 동일성 확인.
  - 1부와 3부의 상단 HTML 스타일 속성 동일성 확인.
  - 3부 로그인 코드 인라인 스크립트 문법 파싱 통과.

## 69. 2026-06-18 1400제 3부 로그인 UI 되돌림 및 가로 태블릿 높이 맞춤

- 사용자 확인 화면 기준으로 1400제 3부 로그인 코드의 별도 기기별 UI 실험을 되돌렸다.
- OneDrive 1400제 3부 최종 로그인 코드에만 적용했다.
  - `getLoginLayoutMode`, `getLoginPreset`, `cardWidth`, `formWidth`, `data-ol-layout` 기반 실험 블록을 제거했다.
  - 1부 로그인 코드의 UI 계산 블록과 동일한 구조로 복원했다.
  - 가로 태블릿에서는 UI 값을 새로 만들지 않고 마지막 단계의 `transform: scale(...)`만 실제 보이는 높이의 90% 기준으로 적용해 세로 스크롤이 생기지 않도록 여유를 둔다.
- 1400제 1, 2, 4부 로그인 코드, 모의고사 로그인 코드, 로컬 문제 페이지, 문제 데이터, 정답, Firebase 저장, GitHub Pages 결과 UI는 변경하지 않았다.
- 검증
  - 3부 로그인 코드에서 별도 실험 마커 제거 확인.
  - 3부 로그인 코드 레이아웃 블록이 예상한 가로 태블릿 축소 조건 외에는 1부와 동일함을 비교 확인.
  - 3부 로그인 코드 인라인 스크립트 문법 파싱 통과.
## 68. 2026-06-18 1400제 3부 로그인 카드 폭 균형 보정

- 사용자 확인 화면 기준으로 1400제 3부 로그인 카드가 한눈에는 들어오지만 가로로 과도하게 퍼져 보기 좋지 않은 상태를 보정했다.
- OneDrive 1400제 3부 최종 로그인 코드에만 적용했다.
  - 모드별 preset에 `cardWidth`와 `formWidth`를 추가했다.
  - `default`, `tablet-portrait`, `tablet-landscape`, `embedded-tight`에서 파란 카드가 화면 폭 전체로 늘어나지 않도록 최대폭을 제한했다.
  - 입력창/버튼 영역도 카드 폭에 맞춰 별도 폭으로 중앙 정렬되도록 했다.
- 1400제 1, 2, 4부, 모의고사 로그인 코드, 로컬 문제 페이지, 문제 데이터, 정답, Firebase 저장, GitHub Pages 결과 UI는 변경하지 않았다.
- 검증:
  - 카드/폼 폭 조정 마커가 1400제 3부 로그인 코드에만 들어간 것을 확인했다.
  - 1400제 3부 로그인 코드 인라인 스크립트 문법 파싱 통과.

## 67. 2026-06-18 1400제 3부 기기별 로그인 레이아웃 분리

- 사용자 요청에 따라 파일을 나누지 않고 OneDrive 1400제 3부 최종 로그인 코드 안에서 기기별 레이아웃 모드를 분리했다.
- 3부 로그인 코드에만 `getLoginLayoutMode()`와 `getLoginPreset()`을 추가했다.
  - `phone`, `tablet-portrait`, `tablet-landscape`, `embedded-tight`, `default` 모드로 구분한다.
  - 각 모드별로 카드 패딩, 제목/부제 크기, 입력창 높이, 버튼 높이, 안내 박스 크기와 여백을 별도 preset으로 적용한다.
  - 마지막 단계에서 모드별 target 비율에 맞춰 카드 전체를 추가 축소한다.
- 1400제 1, 2, 4부, 모의고사 로그인 코드, 로컬 문제 페이지, 문제 데이터, 정답, Firebase 저장, GitHub Pages 결과 UI는 변경하지 않았다.
- 검증:
  - 기기별 레이아웃 분리 마커가 1400제 3부 로그인 코드에만 들어간 것을 확인했다.
  - 1400제 3부 로그인 코드 인라인 스크립트 문법 파싱 통과.

## 66. 2026-06-18 1400제 3부 태블릿 추가 압축 보정

- 사용자 재확인 화면 기준으로 1400제 3부 태블릿 로그인 화면이 여전히 한눈에 보기 어렵다고 판단해 3부 로그인 코드만 추가 보정했다.
- OneDrive 1400제 3부 최종 로그인 코드에만 적용했다.
  - 태블릿/터치 환경에서는 `tightHeight`를 더 적극적으로 적용하도록 조건을 강화했다.
  - 카드 전체 축소 기준을 실제 보이는 높이의 82%로 낮춰 여유 공간을 더 확보하도록 했다.
  - 미완료 안내 문구 크기를 CSS 변수(`--ol-notice-main-size`, `--ol-notice-sub-size`)로 제어해 태블릿 압축 모드에서 더 작게 보이도록 했다.
  - 안내 문구 줄간격과 두 번째 줄 위쪽 여백도 줄였다.
- 1400제 1, 2, 4부, 모의고사 로그인 코드, 로컬 문제 페이지, 문제 데이터, 정답, Firebase 저장, GitHub Pages 결과 UI는 변경하지 않았다.
- 검증:
  - 추가 압축 마커가 1400제 3부 로그인 코드에만 들어간 것을 확인했다.
  - 1400제 3부 로그인 코드 인라인 스크립트 문법 파싱 통과.

## 65. 2026-06-18 1400제 태블릿 높이 맞춤 자동 축소

- 사용자 확인 화면 기준으로 태블릿 OlliSite 화면에서 1400제 로그인 카드가 세로 높이에 맞지 않아 버튼 하단이 잘리는 문제를 보정했다.
- OneDrive 1400제 1~4부 최종 로그인 코드의 `fitLoginLayout()`을 높이 기반으로 강화했다.
  - `visibleLoginHeight()`로 현재 viewport와 overflow 부모 컨테이너의 실제 보이는 높이를 계산하도록 했다.
  - 높이가 부족하면 배지, 제목, 부제, 입력창, 버튼, 안내 박스의 여백과 크기를 더 압축하도록 했다.
  - 그래도 부족하면 `scaleLoginToVisibleHeight()`로 카드 전체를 보이는 높이에 맞춰 축소하고 wrapper 높이를 함께 조정하도록 했다.
  - 초기 렌더링 뒤 OlliSite 레이아웃이 늦게 잡히는 경우를 위해 300ms/900ms 지연 재계산과 `visualViewport.resize` 재계산을 추가했다.
- 모의고사 로그인 코드, 로컬 `mock-exam`/`quiz-1400` 문제 페이지, 문제 데이터, 정답, Firebase 저장, GitHub Pages 결과 UI는 변경하지 않았다.
- 검증:
  - OneDrive 1400제 최종 로그인 코드 4개에 `visibleLoginHeight`, `scaleLoginToVisibleHeight`, `visualViewport.resize`, 지연 재계산 마커 반영 확인.
  - OneDrive 1400제 로그인 코드 4개 인라인 스크립트 문법 파싱 통과.
  - 로컬 Node 환경에 Playwright 패키지가 없어 실제 브라우저 렌더링 검증은 실행하지 못했다.

## 64. 2026-06-18 미완료 안내 문구 오해 소지 보정

- 사용자 판단에 따라 미완료 안내의 두 번째 줄을 모의고사와 1400제 모두 동일하게 보정했다.
  - 기존: `모든 문제를 풀고 기준을 충족해 주세요.`
  - 변경: `불합격이면 복습 후 다시 풀어 주세요.`
- `아직 학습 완료 버튼을 누를 수 없습니다.` 문구, `학습 완료` 주황색 강조, 큰 글자 크기, 안내 박스 간격은 그대로 유지했다.
- OneDrive 모의고사 1~10회 최종 로그인 코드와 1400제 1~4부 최종 로그인 코드에만 반영했다.
- 로컬 `mock-exam`/`quiz-1400` 문제 페이지, 문제 데이터, 정답, Firebase 저장, GitHub Pages 결과 UI는 변경하지 않았다.
- 검증:
  - OneDrive 로그인 코드 14개에서 새 문구 28곳 반영 확인.
  - 기존 `모든 문제를 풀고 기준을 충족해 주세요.` 잔존 0건 확인.
  - OneDrive 로그인 코드 14개 인라인 스크립트 문법 파싱 통과.

## 63. 2026-06-18 1400제 시작 버튼과 안내 박스 간격 조정

- 사용자 요청에 따라 1400제 로그인 화면도 확인했다.
- OneDrive 1400제 1~4부 최종 로그인 코드의 `done_notice`에도 위쪽 여백이 없어서 모의고사와 동일하게 `margin-top:28px`을 추가했다.
- 모의고사 로그인 코드, 로컬 `mock-exam`/`quiz-1400` 문제 페이지, 문제 데이터, 정답, Firebase 저장, GitHub Pages 결과 UI는 변경하지 않았다.
- 검증:
  - OneDrive 1400제 최종 로그인 코드 4개에서 `done_notice` 위쪽 여백 반영 확인.
  - 기존 여백 없는 `done_notice` 스타일 제거 확인.
  - OneDrive 1400제 로그인 코드 4개 인라인 스크립트 문법 파싱 통과.

## 62. 2026-06-18 모의고사 시작 버튼과 안내 박스 간격 조정

- 사용자 확인 화면 기준으로 모의고사 로그인 화면에서 `모의고사 시작하기` 버튼과 미완료 안내 박스가 너무 붙어 보이는 문제를 보정했다.
- OneDrive 모의고사 1~10회 최종 로그인 코드의 `ol_done_notice`에 위쪽 여백을 추가했다.
  - 기본 스타일: `margin-top:28px`
  - 모바일/작은 화면 보정 스타일: `clamp(20px,5vw,28px)` ~ `clamp(28px,4vw,40px)`
- 1400제 로그인 코드, 로컬 `mock-exam`/`quiz-1400` 문제 페이지, 문제 데이터, 정답, Firebase 저장, GitHub Pages 결과 UI는 변경하지 않았다.
- 검증:
  - OneDrive 모의고사 최종 로그인 코드 10개에서 기본/반응형 위쪽 여백 반영 확인.
  - 기존 여백 없는 `ol_done_notice` 스타일 제거 확인.
  - OneDrive 모의고사 로그인 코드 10개 인라인 스크립트 문법 파싱 통과.

## 61. 2026-06-18 학습 완료 버튼 안내 강조 표시

- 사용자 요청에 따라 OneDrive 최종 로그인 코드의 미완료 안내 문구를 모의고사와 1400제에서 동일하게 맞췄다.
  - 공통 문구: `아직 학습 완료 버튼을 누를 수 없습니다. / 모든 문제를 풀고 기준을 충족해 주세요.`
  - `학습 완료` 부분은 굵은 주황색(`#F97316`)으로 강조했다.
  - 안내 문구 자체에 큰 글자 크기(`clamp(22px,2.8vw,34px)`)를 적용해 기존 카드 안에서 더 시원하게 보이도록 했다.
- OneDrive 모의고사 1~10회 로그인 코드와 1400제 1~4부 로그인 코드에만 반영했다.
- 로컬 `mock-exam`/`quiz-1400` 문제 페이지, 문제 데이터, 정답, Firebase 저장, GitHub Pages 결과 UI는 변경하지 않았다.
- 검증:
  - OneDrive 로그인 코드 14개에서 공통 문구, 주황색 강조, 큰 글자 스타일 반영 확인.
  - 기존 `아직 완료할 수 없습니다`, `불합격이면 오답 복습`, `60% 이상 맞혀 주세요` 미완료 안내 문구 제거 확인.
  - OneDrive 로그인 코드 14개 인라인 스크립트 문법 파싱 통과.

## 60. 2026-06-18 안내 문구 축약 및 1400제 로그인 코드 노출 방지

- 농 학습자가 직접 읽고 풀어야 하는 화면임을 고려해 OneDrive 최종 로그인 코드의 안내 문구를 짧게 줄였다.
  - 모의고사 1~10회 로그인 코드의 진행 중 안내를 `풀이 중입니다. / 모든 문항을 풀고 합격하면 완료됩니다.`로 축약했다.
  - 모의고사 1~10회 로그인 코드의 미완료/불합격 안내를 `아직 완료할 수 없습니다. / 불합격이면 오답 복습 후 다시 풀어 주세요.`로 축약했다.
  - 1400제 1~4부 로그인 코드의 진행/조건 안내를 `풀이 중입니다. / 모든 문항을 풀고 60% 이상 맞혀 주세요.`와 `아직 완료할 수 없습니다. / 모든 문항을 풀고 60% 이상 맞혀 주세요.`로 정리했다.
- OneDrive 1400제 1~4부 최종 로그인 코드에 `hideLeakedWrapperCode()`를 추가했다.
  - OlliSite가 wrapper 시작 태그 일부를 텍스트로 노출하는 경우 `div id="ol1400_N_wrap"` 텍스트 노드를 즉시 제거하도록 했다.
  - 초기 실행, 0ms 지연 실행, 300ms 지연 실행을 함께 넣어 OlliSite 렌더링 타이밍 차이에 대응했다.
- 로컬 `mock-exam`/`quiz-1400` 문제 페이지, 문제 데이터, 정답, Firebase 저장, GitHub Pages 결과 UI는 변경하지 않았다.
- 검증:
  - OneDrive 모의고사 최종 로그인 코드 10개에서 새 짧은 문구 반영 및 기존 긴 문구 제거를 확인했다.
  - OneDrive 1400제 최종 로그인 코드 4개에서 `hideLeakedWrapperCode()`와 새 짧은 문구 반영을 확인했다.
  - OneDrive 로그인 코드 14개 인라인 스크립트 문법 파싱 통과.
## 59. 2026-06-18 1400제 로그인 코드 차시목록 숨김 추가

- 사용자 요청에 따라 OneDrive 1400제 1~4부 로그인 코드에서 올리사이트 상단 `차시목록` 버튼을 항상 숨기도록 `isHostAlwaysHiddenControlText()`와 host guard 분기를 추가했다.
- 기존 `학습을 완료합니다.` 및 `다음 차시` 표시/차단 로직은 유지했고, `토론방` 버튼은 숨김 대상에 포함하지 않았다.
- 검증:
  - 1400제 1~4부 로그인 코드 4개 HTML inline script 문법 검사 통과.
  - 4개 파일 모두 `차시목록` 숨김 마커와 host guard 함수 포함 확인.
  - 4개 파일 모두 `토론방` 문자열 미포함 확인.

## 58. 2026-06-18 로그인 코드 입장 거부 안내 문구 보정

- 사용자 제보에 따라 OneDrive 모의고사 1~10회 로그인 코드의 입장 거부 카드에서 `입장할 수 없습니다` 아래 안내 설명이 항상 보이도록 수정했다.
  - `ol_denied_msg` 기본 문구를 `입력하신 정보는 해당 모의고사에 등록되어 있지 않습니다. 관리자에게 문의해 주세요.`로 채웠다.
  - `olShowScopeDeniedNotice()`에도 같은 fallback 문구를 넣어 메시지가 비어 들어와도 안내가 표시되도록 했다.
- OneDrive 1400제 1~4부 로그인 코드의 scope denied 문구를 `해당 1400제 부`에서 `해당 1400제 n부`로 바꿨다.
- 검증:
  - 모의고사 1~10회 로그인 코드 10개와 1400제 1~4부 로그인 코드 4개 inline script 문법 검사 통과.
  - 모의고사 10개 파일에 입장 거부 안내 기본 문구와 JS fallback 문구가 모두 들어간 것을 확인했다.
  - 1400제 4개 파일에 `해당 1400제 부` 잔존 문구가 없고 `해당 1400제 '+PART+'부` 동적 문구가 들어간 것을 확인했다.

## 57. 2026-06-18 모의고사 1~10회 로그인 코드 1400제 기준 통일

- 사용자 요청에 따라 OneDrive `01. 합격!! 모의고사` 1~10회 `1. ★★★ n회 모의고사 로그인 코드.html`을 1400제 로그인 코드 흐름과 같은 기준으로 정리했다.
- 기존 모의고사 로그인 코드 안의 결과 표시 UI, 정답 배열, 회차별 Firebase 경로, 회차별 GitHub Pages URL, 기존 결과 복습 코드는 삭제하지 않고 그대로 유지했다.
- 로그인 카드 구조를 1400제와 같은 큰 배지/제목/폼/완료 안내/입장 거부 카드 구조로 맞췄고, 모바일·태블릿 자동 맞춤용 `olFitLoginLayout()` / `olQueueLoginLayoutFit()` 흐름을 1~10회 전체에 적용했다.
- 진행 중이거나 미완료/불합격 상태에서는 올리사이트 하단 `학습을 완료합니다.` 및 `다음 차시` 버튼이 숨겨지고, 모든 문항을 풀고 합격한 경우에만 완료 안내와 함께 하단 완료 버튼이 표시되도록 유지했다.
- 입장 거부 시에는 1400제처럼 별도 `입장할 수 없습니다` 카드와 `다시 입력하기` 버튼을 표시하도록 맞췄다.
- 검증:
  - OneDrive 모의고사 1~10회 로그인 코드 10개 HTML inline script 문법 검사 통과.
  - 10개 파일 모두 `ol-wrap`, `ol-form`, `ol_denied`, `olFitLoginLayout`, `olQueueLoginLayoutFit`, `olShowLoginForm`, `mockExamComplete`, `olInstallHostCompletionGuard` 마커 포함 확인.
  - `차시목록` 숨김 및 `토론방` 미간섭 흐름은 기존 요청 기준으로 유지했다.

## 56. 2026-06-18 1400제 올리사이트 완료 버튼 노출 조건 보정

- 사용자 요청에 따라 1400제 1~4부 올리사이트 메인/로그인 코드에서 하단 `학습을 완료합니다.` 및 `다음 차시` 버튼이 처음부터 보이지 않도록 모의고사 1회 흐름과 동일하게 맞췄다.
- 1400제 1~4부 GitHub Pages 결과 화면에서 학습 완료 버튼을 누르면 `quiz1400Complete` 메시지를 opener로 보내고, 올리사이트 화면이 다시 포커스를 받은 뒤 완료 안내와 하단 완료 버튼이 나타나도록 했다.
- 완료 조건은 단순 결과 존재 여부가 아니라 해당 부의 필수 결과 기록이 모두 존재하고, 모든 문항이 응답되어 있으며, 정답 수가 전체 문항의 60% 이상인 경우로 제한했다.
- OneDrive 1400제 1~4부 로그인 코드에는 하단 완료/다음차시 버튼 숨김, 조기 클릭 차단, 완료 안내 표시, Firebase 결과 재확인 로직을 반영했다.
- OneDrive 1400제 1~4부 데스크톱/모바일 최종 코드 8개 파일을 로컬 `quiz-1400`과 다시 동기화했고 SHA-256 해시 일치를 확인했다.
- 검증:
  - 로컬 `quiz-1400` 1~4부 데스크톱/모바일 8개 HTML inline script 문법 검사 통과.
  - OneDrive 1400제 1~4부 로그인 코드 4개 HTML inline script 문법 검사 통과.
  - OneDrive 1400제 1~4부 데스크톱/모바일 최종 코드 8개 HTML inline script 문법 검사 통과.
  - `node tools/result-page-check.mjs` 통과: 28개 파일, 196개 결과 화면 viewport 케이스.
  - `git diff --check` 통과.

## 55. 2026-06-18 1400제 로그인 코드 모바일·태블릿 자동 맞춤 보정

- 사용자 제보에 따라 1400제 3부 올리사이트 복사용 로그인 코드가 스마트폰 화면에서 파란 로그인 카드 안의 배지/버튼이 위아래로 잘려 보이는 문제를 점검했다.
- 원인:
  - 로그인 카드가 `aspect-ratio:16/9`와 `justify-content:center` 기반이라 모바일·태블릿에서 카드 내부 콘텐츠 높이가 카드 높이보다 커지면 위아래로 넘칠 수 있었다.
- 수정:
  - OneDrive `02. 합격!! 1400題` 1~4부 `1. ★★★ n부 로그인 코드.html`에 동일한 `fitLoginLayout()` / `queueLoginLayoutFit()` 자동 맞춤 함수를 추가했다.
  - 모바일·태블릿, 좁은 화면, 낮은 화면, 실제 내부 overflow 상황에서는 16:9 고정비를 풀고 카드 높이를 내용 기준으로 자동 조정하도록 했다.
  - 배지, 제목, 부제목, 입력창, 시작 버튼, 입장 거부 카드의 크기·여백을 viewport에 맞춰 `clamp()` 값으로 재계산하도록 했다.
  - `resize`와 `orientationchange` 이벤트에서 다시 맞춤이 실행되도록 했다.
  - 이름 정규화의 공백 처리 정규식도 `/\s+/g`로 보정했다.
- 검증:
  - 1400제 1~4부 로그인 코드 4개 inline script 문법 검사 통과.
  - 자동 맞춤 함수 블록이 1~4부에서 SHA-256 해시 기준으로 완전히 동일함을 확인했다.
  - `git diff --check` 통과.
  - 브라우저 실측 검증은 독립 Playwright 실행에서 `playwright-core` 누락으로 실패했고, Codex in-app browser는 `file://` 미리보기 접근이 보안 정책으로 차단되어 수행하지 못했다.

## 54. 2026-06-18 1400제 1·2·4부 공통 로직 3부 기준 재확인

- 사용자 요청에 따라 1400제 3부에서 정상 확인된 전체 코드 구조를 기준으로 1부, 2부, 4부 데스크톱/모바일 전체 코드를 다시 비교했다.
- 확인:
  - 부별 문제 데이터, 장 목록, 3부 10장 세부절 선택 화면은 부별 구성 차이이므로 그대로 보존했다.
  - 데스크톱 1·2·4부와 모바일 2·4부의 `const NAV_PAGE_SIZE` 이후 공통 로직은 3부와 해시 기준으로 이미 일치했다.
  - 모바일 1부는 동작 차이는 없었지만, 공통 함수 구간의 빈 줄 수가 3부 기준과 1줄 달라 공통 로직 해시가 달랐다.
- 수정:
  - 로컬 `quiz-1400/mobile-01.html`의 공통 함수 구간 공백을 3부 모바일 기준과 동일하게 맞췄다.
  - 문제 데이터, 장 목록, 결과 UI, 권한 확인 로직, 저장 로직은 삭제하거나 재구성하지 않았다.
- OneDrive 동기화:
  - `02. 합격!! 1400題\1부\2. (★삭제 주의) 최종 코드\02. 개선판\3. ★★★ 1부 코드 - 모바일용.html`에 동일 변경을 반영했다.
  - 로컬 파일과 OneDrive 저장 파일의 SHA-256 해시 일치를 확인했다.
- 검증:
  - 로컬 1400제 1~4부 데스크톱/모바일 8개 파일 inline script 문법 검사를 통과했다.
  - OneDrive 1400제 1~4부 데스크톱/모바일 최종 코드 8개와 로그인 코드 4개 inline script 문법 검사를 통과했다.
  - 3부 기준 공통 로직 해시 비교에서 데스크톱 1·2·4부와 모바일 1·2·4부가 모두 일치했다.
  - `node tools/result-page-check.mjs` 통과: 28개 파일, 196개 결과 화면 viewport 케이스.
  - `git diff --check` 통과.

## 53. 2026-06-18 1400제 3부 권한 키 상수 누락 보정

- 사용자 요청에 따라 1400제 1부 전체 코드와 2~4부 코드를 비교해 누락된 부분을 점검했다.
- 원인:
  - 1부, 2부, 4부에는 `const QUIZ_1400_PART = n;` 선언이 있었지만, 3부 데스크톱/모바일에는 `const QUIZ_1400_PART = 3;` 선언이 누락되어 있었다.
  - 이 때문에 3부에서 권한 키 `part3`를 만들 때 `QUIZ_1400_PART` 참조 오류가 발생하고, 결과적으로 `등록 정보를 확인하지 못했습니다. 잠시 후 다시 시도해 주세요.` fallback 안내가 표시될 수 있었다.
- 수정:
  - 로컬 `quiz-1400/desktop-03.html`, `quiz-1400/mobile-03.html`에 `const QUIZ_1400_PART = 3;`을 추가했다.
  - 1400제 1~4부 데스크톱/모바일 8개 파일 전체에 부별 상수, REST 권한 조회, 권한 안내 문구, SDK 기반 `authorizedUsers` 조회 제거 여부를 전수 확인했다.
- OneDrive 동기화:
  - `02. 합격!! 1400題` 1~4부 `02. 개선판` 데스크톱/모바일 최종 코드 8개를 로컬과 동기화했다.
  - 8개 OneDrive 대응 파일과 로컬 파일의 SHA-256 해시 일치를 확인했다.
- 검증:
  - 로컬 1400제 1~4부 데스크톱/모바일 8개 inline script 문법 검사 통과.
  - OneDrive 1400제 1~4부 데스크톱/모바일 8개 inline script 문법 검사 통과.
  - `node tools/result-page-check.mjs` 통과: 28개 파일, 196개 결과 화면 viewport 케이스.
  - `git diff --check` 통과.

## 52. 2026-06-17 1400제 GitHub 페이지 권한 확인 방식 통일

- 사용자 확인 화면에서 GitHub 1400제 페이지가 `등록 정보를 확인하지 못했습니다. 잠시 후 다시 시도해 주세요.` 알림을 띄우는 문제를 점검했다.
  - Firebase REST로 `authorizedUsers/jlysm91` 조회는 정상이라 등록 데이터 문제보다는 GitHub 학습 페이지 내부 권한 조회 방식 문제로 판단했다.
  - 기존 데스크톱 1400제는 Firebase SDK `database.ref('authorizedUsers/...')`로 등록 명단을 다시 읽어 SDK 조회 실패 시 fallback 문구가 나올 수 있었다.
  - 모바일 1400제에는 권한 확인 함수 정의가 누락된 상태라 데스크톱/모바일 간 동작 차이 위험이 있었다.
- 로컬 `quiz-1400` 1~4부 데스크톱/모바일 8개 파일의 등록 명단 확인을 올리사이트 로그인 코드와 같은 REST `fetch()` 방식으로 통일했다.
  - `authorizedUsers/{uid}.json`을 직접 읽고, 등록 없음/사용 중지/이름 불일치/부별 권한 없음은 기존 안내 문구로 분기한다.
  - 진행 저장과 결과 저장에 사용하는 Firebase Database SDK 흐름은 변경하지 않았다.
- OneDrive 동기화:
  - `02. 합격!! 1400題` 1~4부 `02. 개선판` 데스크톱/모바일 최종 코드 8개를 로컬과 동기화했다.
  - 8개 OneDrive 대응 파일과 로컬 파일의 SHA-256 해시 일치를 확인했다.
- 검증:
  - 로컬 1400제 1~4부 데스크톱/모바일 8개 inline script 문법 검사 통과.
  - OneDrive 1400제 1~4부 데스크톱/모바일 8개 inline script 문법 검사 통과.
  - 정적 검사로 8개 파일 모두 `verifyAuthorizedUser`, `getAuthorizedUserDbUrl`, `showAuthorizationDenied`가 1회씩 존재하고 SDK 기반 `authorizedUsers` 조회가 제거된 것을 확인했다.
  - `node tools/result-page-check.mjs` 통과: 28개 파일, 196개 결과 화면 viewport 케이스.
  - `git diff --check` 통과.

## 51. 2026-06-17 1400제 로그인 거부 화면 부제목 유지

- 사용자 요청에 따라 OneDrive `02. 합격!! 1400題` 1~4부 올리사이트 복사용 로그인 코드의 입장 거부 화면에서도 부제목을 유지하도록 보정했다.
  - `1부. 요양보호와 인권`, `2부. 노화와 건강증진`, `3부. 요양보호와 생활지원`, `4부. 상황별 요양보호 기술` 문구가 거부 안내 카드 위에 계속 표시된다.
  - 거부 화면 전환 시 `subtitle`을 숨기던 처리를 제거했다.
- 수정 대상:
  - OneDrive `02. 합격!! 1400題` 1~4부 `2. (★삭제 주의) 최종 코드\02. 개선판\1. ★★★ n부 로그인 코드.html`
- 검증:
  - 1400제 로그인 코드 4개 inline script 문법 검사 통과.
  - 정적 검사로 4개 파일 모두 부제목 문구 유지, `1400題 - n부` 배지 유지, 거부 화면에서 부제목 숨김 처리 제거를 확인했다.

## 50. 2026-06-17 1400제 로그인 코드 배지 문구와 크기 보정

- 사용자 요청에 따라 OneDrive `02. 합격!! 1400題` 1~4부 올리사이트 복사용 로그인 코드의 표시를 보정했다.
  - 배지 문구를 `n부 1400제`에서 `1400題 - n부` 형식으로 변경했다.
  - 카드 최소 높이, 내부 여백, 배지, 제목, 부제목, 입력창, 시작 버튼, 입장 거부 안내 카드 크기를 조금 키웠다.
- 수정 대상:
  - OneDrive `02. 합격!! 1400題` 1~4부 `2. (★삭제 주의) 최종 코드\02. 개선판\1. ★★★ n부 로그인 코드.html`
- 검증:
  - 1400제 로그인 코드 4개 inline script 문법 검사 통과.
  - 정적 검사로 4개 파일 모두 `1400題 - n부` 배지와 확대된 주요 크기 값이 들어간 것을 확인했다.

## 49. 2026-06-17 1400제 1~4부 올리사이트 로그인 코드 재생성

- 사용자 확인에 따라 OneDrive `02. 합격!! 1400題` 1~4부 올리사이트 복사용 로그인 코드를 별도로 재생성했다.
  - 각 부의 `1. ★★★ n부 로그인 코드.html`을 같은 구조로 맞췄다.
  - 아이디/이름 입력 후 `authorizedUsers/{uid}`를 `fetch()`로 확인한다.
  - `allowedScopes.quiz1400.all` 또는 `allowedScopes.quiz1400.partN` 권한이 있어야 해당 부 데스크톱/모바일 GitHub Pages 학습 창을 연다.
  - 권한이 없으면 GitHub 페이지를 열지 않고, 안내창 후 올리사이트 코드 영역에 `입장할 수 없습니다` 안내를 표시한다.
  - 기기 유형에 따라 `desktop-0N.html` 또는 `mobile-0N.html`로 이동한다.
- 수정 대상:
  - OneDrive `02. 합격!! 1400題` 1~4부 `2. (★삭제 주의) 최종 코드\02. 개선판\1. ★★★ n부 로그인 코드.html`
- 검증:
  - 1400제 로그인 코드 4개 inline script 문법 검사 통과.
  - 정적 검사로 4개 파일 모두 부별 배지, `PART_NO`, GitHub Pages URL 생성, `quiz1400.partN` 권한 검사, 권한 거부 안내, 새 창 열기 흐름 포함 확인.

## 48. 2026-06-17 1400제 결과 UI 모의고사 기준 보강

- 사용자 요청에 따라 로컬 `quiz-1400` 1~4부 데스크톱/모바일 결과 UI를 모의고사 결과 UI 기준으로 보강했다.
  - 오답 문항 카드에 hover/focus 상세 팝업을 추가해 문제 내용, 문제 코드, 정답, 선택 답, 보기별 정답/선택 표시가 나오도록 했다.
  - 결과 하단 버튼 순서를 모의고사 흐름처럼 `다시 풀기` 다음 `장 선택하기`로 정렬하고 버튼 스타일을 결과 UI 기준으로 맞췄다.
  - 1400제의 부별 문제 데이터, 정답, Firebase 저장/권한 검증, 진행 흐름은 변경하지 않았다.
- OneDrive 동기화:
  - `02. 합격!! 1400題` 1~4부 `02. 개선판` 데스크톱/모바일 최종 코드 8개를 로컬 `quiz-1400`과 동기화했다.
  - 8개 OneDrive 대응 파일과 로컬 파일의 SHA-256 해시 일치를 확인했다.
- 검증:
  - `git diff --check` 통과.
  - 로컬 1400제 1~4부 데스크톱/모바일 8개 inline script 문법 검사 통과.
  - OneDrive 1400제 1~4부 데스크톱/모바일 8개 inline script 문법 검사 통과.
  - `node tools/result-page-check.mjs` 통과: 28개 파일, 196개 결과 화면 viewport 케이스.
  - 정적 검사로 8개 파일 모두 오답 팝업 CSS/함수, 오답 카드 `data-review-*` 속성, 렌더 후 tooltip binding, 결과 버튼 순서가 들어간 것을 확인했다.
- 참고:
  - 로컬 브라우저 확인은 외부 Firebase 스크립트 로드 제한으로 핵심 시험 스크립트가 실행되지 않아 실제 hover 재현까지는 하지 못했고, 기존 결과 화면 자동 검사와 정적 검증으로 확인했다.

## 47. 2026-06-17 모의고사 권한 거부 화면 올리사이트 내부 표시로 재조정

- 사용자 재요청에 따라 OneDrive 모의고사 1~10회 로그인 코드의 `scope-denied` 흐름을 다시 조정했다.
  - 권한 범위 미등록 시 GitHub 모의고사 페이지를 열지 않는다.
  - `입력하신 정보는 해당 모의고사에 등록되어 있지 않습니다. 관리자에게 문의해 주세요.` 안내창을 먼저 표시한다.
  - 안내창을 닫으면 올리사이트 메인페이지 코드 영역 안에 `입장할 수 없습니다` 카드와 안내 문구가 표시된다.
  - 이전에 추가했던 GitHub 입장 불가 페이지 열기 및 올리사이트 닫기 시도 함수는 제거했다.
- 수정 대상:
  - OneDrive `01. 합격!! 모의고사` 1~10회 `1. ★★★ n회 모의고사 로그인 코드.html`
- 검증:
  - OneDrive 모의고사 로그인 코드 10개 inline script 문법 검사 통과.
  - 10개 파일 모두 `alert(message)` 후 `olShowScopeDeniedNotice(message)` 흐름 포함 확인.
  - 10개 파일 모두 `olOpenDeniedExamPageAndCloseHost`, `olTryCloseHostPage`, `olIsHostCloseControlText` 제거 확인.

## 46. 2026-06-17 모의고사 권한 거부 시 GitHub 입장 불가 화면 유지

- 사용자 요청에 따라 OneDrive 모의고사 1~10회 로그인 코드의 `scope-denied` 처리 흐름을 변경했다.
  - 올리사이트 로그인 코드가 권한 범위 미등록을 사전 안내로 끝내지 않고, 해당 GitHub 모의고사 페이지를 열도록 했다.
  - GitHub 페이지는 기존 권한 검사에 따라 `입장할 수 없습니다` 화면과 등록되지 않았다는 안내를 표시한다.
  - GitHub 입장 불가 화면을 연 뒤 올리사이트 메인페이지는 닫기 버튼 클릭을 우선 시도하고, 이어 `window.close()` / `window.top.close()`를 시도하도록 했다.
  - 포커스 복귀 감시에서도 `scope-denied`가 일반 미완료 안내로 바뀌지 않도록 닫기 재시도만 수행하게 했다.
- 수정 대상:
  - OneDrive `01. 합격!! 모의고사` 1~10회 `1. ★★★ n회 모의고사 로그인 코드.html`
- 검증:
  - OneDrive 모의고사 로그인 코드 10개 inline script 문법 검사 통과.
  - 10개 파일 모두 `olOpenDeniedExamPageAndCloseHost`, `olTryCloseHostPage`, `scope-denied` 전용 열기/닫기 흐름 포함 확인.

## 45. 2026-06-15 모의고사 입장 거부 안내 유지 보정

- 사용자 요청에 따라 권한 범위 미등록 안내 문구를 `이 학습자는...`에서 `입력하신 정보는 해당 모의고사에 등록되어 있지 않습니다. 관리자에게 문의해 주세요.`로 변경했다.
- 로컬 `mock-exam` 1~10회 데스크톱/모바일 결과 페이지의 `scope-denied` 문구를 동일하게 갱신했다.
- 로컬 `quiz-1400` 로그인 및 1~4부 데스크톱 결과 페이지의 권한 범위 미등록 문구도 `입력하신 정보는...` 형식으로 맞췄다.
- OneDrive 모의고사 1~10회 로그인 코드에 `olScopeDeniedMessage` 상태와 `olShowScopeDeniedNotice()`를 추가했다.
  - `scope-denied` 입장 거부가 발생한 뒤 올리사이트 하단 학습 완료 버튼을 눌러도 일반 미완료 안내가 덮어쓰지 않는다.
  - 해당 경우에는 입력한 정보가 해당 모의고사에 등록되어 있지 않다는 안내만 표시된다.
- OneDrive 동기화:
  - 모의고사 1~10회 데스크톱/모바일 최종 코드 파일을 로컬 `mock-exam`과 동기화했다.
  - 1400제 1~4부 데스크톱/모바일 최종 코드 파일을 로컬 `quiz-1400`과 해시 기준으로 확인했다.
- 검증:
  - `git diff --check` 통과.
  - `node tools/result-page-check.mjs` 통과: 28개 파일, 196개 결과 화면 viewport 케이스.
  - 로컬 모의고사/1400제 및 OneDrive 모의고사 로그인 코드 inline script 문법 검사 통과: 35개 파일, 59개 script.
  - OneDrive 최종 코드 해시 확인 통과: 모의고사 20개, 1400제 8개 파일 일치.

## 44. 2026-06-15 관리자 등록 페이지 목록 및 비밀번호 prompt 개선

- OneDrive 로컬 전용 `00. 관리자용 학습자 등록.html`을 수정했다.
  - 페이지 내부의 관리자 비밀번호 입력칸을 제거했다.
  - 페이지를 열면 `prompt`로 관리자 비밀번호를 입력받도록 변경했다.
  - 관리자 비밀번호를 `150601`로 변경했다.
  - 아이디/이름 입력칸의 예시 placeholder를 제거해 빈 칸으로 보이게 했다.
  - 오른쪽 영역에 `등록 목록`을 추가해 Firebase `authorizedUsers`에 저장된 아이디와 이름을 바로 확인할 수 있게 했다.
  - 등록 목록 항목을 클릭하면 해당 학습자의 등록 정보를 입력 폼으로 불러와 수정할 수 있게 했다.
- 검증:
  - 관리자 HTML inline script 문법 검사 통과.
  - 관리자 HTML에 prompt, `150601`, 등록 목록 영역이 포함되고 `admin-password` 입력칸 및 placeholder가 제거됐음을 확인했다.
  - 브라우저 도구의 `file://` 접근은 보안 정책상 차단된 상태라 우회하지 않았다.

## 43. 2026-06-15 관리자 등록 페이지 및 authorizedUsers 입장 검증

- 로컬 전용 관리자 HTML을 OneDrive 관리 폴더에 추가했다.
  - `00. 관리자용 학습자 등록.html`
  - 관리자 비밀번호, 아이디, 이름, 사용 상태, 모의고사 1~10회, 1400제 1~4부 적용 범위를 입력해 `authorizedUsers/{uid}`에 저장한다.
  - 기본값은 전체 모의고사 및 전체 1400제 허용이며, 필요 시 회차/부별 체크박스로 제한할 수 있다.
- Firebase 기준 명단 구조를 `authorizedUsers/{uid}`로 정했다.
  - `name`, `enabled`, `allowedScopes.mockExam`, `allowedScopes.quiz1400`를 저장한다.
  - 진행/결과 데이터는 기존 `exams/{examId}/users/{uid}` 구조를 유지한다.
- 로컬 `mock-exam/desktop-01.html`~`desktop-10.html`, `mock-exam/mobile-01.html`~`mobile-10.html`에 등록 명단 기반 입장 검증을 적용했다.
  - 등록되지 않은 아이디, 이름 불일치, 사용 중지, 해당 회차 미허용 상태를 차단한다.
  - 통과한 경우 기존 진행 저장 및 학습 결과를 그대로 불러온다.
- 로컬 `quiz-1400/login.html`, `quiz-1400/desktop-01.html`~`desktop-04.html`, `quiz-1400/mobile-01.html`~`mobile-04.html`에 1400제 부별 권한 검증을 적용했다.
  - `quiz1400.all` 또는 `quiz1400.partN` 권한이 있어야 해당 부에 입장할 수 있다.
  - 1400제 결과 UI 디자인은 이번 작업에서 수정하지 않았다.
- OneDrive 동기화:
  - 모의고사 1~10회 데스크톱/모바일 최종 코드 파일을 로컬과 동기화했다.
  - 1400제 1~4부 데스크톱/모바일 최종 코드 파일을 로컬과 동기화했다.
  - 모의고사 1~10회 OneDrive 로그인 코드도 `authorizedUsers` 검증 기준으로 갱신했다.
- 검증:
  - `git diff --check` 통과.
  - `node tools/result-page-check.mjs` 통과: 28개 파일, 196개 결과 화면 viewport 케이스 확인.
  - 로컬 모의고사 20개, 로컬 1400제 9개, OneDrive 모의고사 로그인 10개, 관리자 HTML 1개 inline script 문법 검사 통과.
  - OneDrive 모의고사 1~10회 및 1400제 1~4부 데스크톱/모바일 최종 코드가 로컬 파일과 SHA-256 해시 일치함을 확인했다.
  - 브라우저 도구의 `file://` 접근은 보안 정책상 차단되어 우회하지 않았다.

## 42. 2026-06-15 모의고사 아이디/이름 일치 검증 추가

- 사용자 요청에 따라 1~10회 모의고사에서 아이디만 맞고 이름이 틀려도 기존 진행/결과가 열리는 문제를 보완했다.
- 로컬 `mock-exam/desktop-01.html`~`desktop-10.html`, `mock-exam/mobile-01.html`~`mobile-10.html`에 Firebase 사용자 식별 검증을 추가했다.
  - 최초 진입 시 `exams/{examN}/users/{uid}` 아래 `name`과 `profile/name`을 저장한다.
  - 저장된 이름이 있는 아이디로 다시 진입할 때 입력 이름이 다르면 진행 중 저장/학습 결과를 열지 않고 차단 안내를 표시한다.
  - 진행 저장값과 결과 저장값에도 `uid`, `name`을 함께 저장하도록 했다.
- OneDrive `01. 합격!! 모의고사`의 1~10회 로그인 코드 최종 파일에도 동일한 아이디/이름 검증을 반영했다.
  - 올리사이트 로그인 코드에서 GitHub Pages를 열기 전에 Firebase 사용자 이름을 확인한다.
  - 이름 불일치 또는 완료 메시지 이름 불일치 시 학습 완료/결과 확인 흐름을 막는다.
  - `차시목록` 숨김 처리와 `토론방` 유지 처리는 그대로 보존했다.
- OneDrive 1~10회 데스크톱/모바일 최종 코드 파일도 로컬 `mock-exam` 파일과 SHA-256 해시 일치까지 확인했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - `node tools/result-page-check.mjs` 통과: 28개 파일, 196개 결과 화면 viewport 케이스 확인.
  - 로컬 20개 HTML과 OneDrive 로그인 코드 10개 inline script 구조/문법 검사 통과.
  - 로컬 HTTP 서버에서 대표 1회 데스크톱/모바일 페이지 로드 및 브라우저 콘솔 오류 없음 확인.
- 남은 주의사항:
  - 기존 Firebase 사용자 기록에 이름이 비어 있던 경우에는 업데이트 후 첫 정상 진입 이름으로 프로필이 채워진다.
  - 사전 등록된 명단만 허용하는 방식이 필요하면 별도 roster/auth 노드를 추가해야 한다.

## 41. 2026-06-15 모의고사 로그인 코드 차시목록 상시 숨김 처리

- 사용자 요청에 따라 1~10회 모의고사 OneDrive 로그인 코드에서 올리사이트 상단 `차시목록` 버튼을 항상 숨기고 클릭도 차단하도록 보정했다.
- 수정 대상:
  - OneDrive `01. 합격!! 모의고사` 아래 1~10회 `1. ★★★ n회 모의고사 로그인 코드.html`
- 변경 내용:
  - `차시목록`은 합격/완료 여부와 관계없이 `display:none`, `disabled`, `aria-hidden=true`로 처리한다.
  - 이벤트 캡처 차단에도 `차시목록`을 포함해 렌더링이 늦게 다시 나타나는 경우에도 클릭 이동을 막는다.
  - `토론방` 버튼은 숨김/차단 대상에 포함하지 않았다.
- 검증:
  - 1~10회 로그인 코드 10개 inline script 문법 검사 통과.
  - 10개 파일 모두 `차시목록` 상시 숨김 헬퍼와 클릭 차단 로직 포함 확인.
  - `토론방`이 상시 숨김 대상에 포함되지 않았음을 확인.
  - OneDrive 로그인 코드 최종 파일 수정 시간이 `2026-06-15 오전 5:42:03`으로 갱신됨을 확인.

## 40. 2026-06-14 2~10회 모의고사 결과 UI 및 로그인 코드 1회 기준 일괄 적용

- 사용자 요청에 따라 1회 모의고사에 먼저 적용했던 학습 결과 UI와 동작을 2~10회 모의고사 전체에 동일하게 반영했다.
- 로컬 `mock-exam/desktop-02.html`~`desktop-10.html`, `mock-exam/mobile-02.html`~`mobile-10.html`을 수정했다.
  - 각 회차의 `quizRawData`, 정답, `EXAM_ID`, 회차명은 해당 회차 값으로 유지했다.
  - 결과 화면 UI, `학습 완료` 버튼, 합격/미응답 완료 조건, 오답 문항 hover/focus 상세 팝업, 1400제 기준 모바일 결과 UI를 1회 기준으로 맞췄다.
- OneDrive 대응 파일 동기화:
  - 2~10회 `2-1. ★★★ n회 모의고사 코드 - 데스크톱용.html`과 `2-2. ★★★ n회 모의고사 코드 - 모바일용.html`에 로컬 변경을 반영했다.
  - 2~10회 `1. ★★★ n회 모의고사 로그인 코드.html`을 별도 로그인 코드로 갱신했다.
  - 로그인 코드의 `examN`, 데스크톱/모바일 GitHub Pages URL, `olCorrectAnswers`가 각 회차 로컬 문제 데이터와 일치함을 확인했다.
  - `-DESKTOP-N9UCJ8E` 충돌/임시 파일은 수정하지 않았다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - `node tools/result-page-check.mjs` 통과: 28개 파일, 196개 결과 화면 viewport 케이스 확인.
  - 2~10회 로컬 데스크톱/모바일 18개 HTML과 OneDrive 로그인 코드 9개 inline script 문법 검사 통과.
  - 2~10회 로컬 데스크톱/모바일 정답 80개 및 OneDrive 로그인 코드 `olCorrectAnswers` 80개 일치 확인.
  - OneDrive 2~10회 데스크톱/모바일 최종 코드 파일이 로컬 파일과 SHA-256 해시 일치함을 확인했다.
  - 로컬 HTTP 미리보기에서 2회 데스크톱/모바일 대표 페이지를 열어 회차 표기, 결과 페이지 요소, 학습 완료 버튼, 모바일 빠른 문항 이동 영역과 콘솔 오류 없음 확인.

## 39. 2026-06-14 1회 모의고사 오답 문항 hover 상세 팝업 추가

- 사용자 요청에 따라 1회 모의고사 결과 화면의 `틀린 문항` 카드에 hover/focus 상세 팝업을 추가했다.
- `mock-exam/desktop-01.html`과 `mock-exam/mobile-01.html`을 수정했다.
  - 각 오답 카드에 마우스를 올리거나 키보드 포커스가 들어가면 고정 위치 팝업이 표시된다.
  - 팝업에는 문항 번호, 문제 코드, 문제 내용, 정답 번호, 선택 번호, 전체 보기 목록을 표시한다.
  - 보기 목록에서 정답은 초록 계열, 선택 답안은 붉은 계열로 구분한다.
  - 스크롤 박스 안에서 잘리지 않도록 팝업은 `body`에 고정 위치로 생성한다.
  - 마우스가 다른 곳으로 이동하거나 스크롤/리사이즈되면 팝업이 사라진다.
- OneDrive 대응 파일 동기화:
  - `2-1. ★★★ 1회 모의고사 코드 - 데스크톱용.html`
  - `2-2. ★★★ 1회 모의고사 코드 - 모바일용.html`
  - 로컬 파일과 OneDrive 대응 파일의 SHA-256 해시 일치를 확인했다.
- 검증:
  - 1회 데스크톱/모바일 inline script 문법 검사 통과.
  - `git diff --check` 통과.
  - `node tools/result-page-check.mjs` 통과: 28개 파일, 196개 결과 화면 viewport 케이스 확인.
  - 로컬 HTTP 미리보기에서 저장된 1회 결과를 열어 오답 카드에 `data-review-no`, hover/focus 핸들러가 렌더링됨을 확인했다.
- 남은 주의사항:
  - 자동화 브라우저의 포인터 이벤트 검증은 도구 제약으로 실제 hover 시각 상태까지는 직접 확인하지 못했고, DOM 렌더링과 문법/회귀 검증으로 확인했다.

## 38. 2026-06-14 1회 모의고사 다음 차시 편법 이동 차단

- 사용자 확인에 따라 학습을 완료하지 않은 상태에서 올리사이트의 `다음 차시` 버튼을 눌러 다음 회차로 넘어가는 문제를 보완했다.
- OneDrive 1회 모의고사 로그인 코드 최종 파일만 수정했다.
  - 기존 `학습을 완료합니다` 버튼 제어 대상에 `다음 차시`와 `다음차시` 버튼 텍스트를 추가했다.
  - 미응답 또는 불합격 상태에서는 접근 가능한 올리사이트 문서 안의 `학습을 완료합니다`와 `다음 차시` 버튼을 숨기고 비활성화한다.
  - 버튼이 동적으로 다시 렌더링되거나 숨김이 늦게 적용되는 경우를 대비해 `pointerdown`, `mousedown`, `touchstart`, `click` 캡처 단계에서 이동을 차단한다.
  - 차단 시 기존 미완료 안내를 다시 표시하도록 했다.
- 검증:
  - 1회 로그인 코드 inline script 문법 검사 통과.
  - `다음 차시`/`다음차시` 탐지 로직과 캡처 이벤트 등록 위치 확인.
  - OneDrive 로그인 코드 최종 파일 수정 시간이 `2026-06-14 오후 4:27:54`로 갱신됨을 확인.
- 남은 주의사항:
  - 이 차단도 올리사이트 버튼이 같은 DOM 또는 접근 가능한 상위 문서에 있을 때 적용된다. 별도 보안 컨텍스트면 브라우저 정책상 직접 차단이 제한될 수 있다.

## 37. 2026-06-14 1회 모의고사 학습 완료 조건 제한

- 사용자 요청에 따라 1회 모의고사에서 학습 완료 안내와 버튼이 편법적으로 노출되지 않도록 조건을 강화했다.
- `mock-exam/desktop-01.html`과 `mock-exam/mobile-01.html`에 완료 판정 공통 로직을 추가했다.
  - 80문항을 모두 응답하고, 필기 21문항 이상 및 실기 27문항 이상으로 합격한 경우에만 결과 화면의 `학습 완료` 버튼을 표시한다.
  - 미응답 또는 불합격 상태에서는 `학습 완료` 버튼을 숨기고, 완료 신호(`mockExamComplete`)도 전송하지 않는다.
  - 완료 신호에는 실제 답안과 정답 배열을 포함해 로그인 코드가 다시 판정할 수 있도록 했다.
- OneDrive 1회 모의고사 로그인 코드 최종 파일도 수정했다.
  - 모의고사 시작 후 아이디/이름 입력창과 시작 버튼을 숨기고 진행 안내만 표시한다.
  - Firebase 결과가 전 문항 응답 및 합격 조건을 만족할 때만 입력값을 유지한 채 입력창을 다시 표시하고 완료 안내를 띄운다.
  - 미응답 또는 불합격 결과가 저장되어도 완료 안내와 `학습을 완료합니다` 안내가 나오지 않도록 했다.
  - 같은 DOM에 접근 가능한 경우 올리사이트의 `학습을 완료합니다` 버튼도 조건 충족 전에는 숨기고, 조건 충족 후 다시 보이도록 했다.
- OneDrive 대응 파일 동기화:
  - `2-1. ★★★ 1회 모의고사 코드 - 데스크톱용.html`
  - `2-2. ★★★ 1회 모의고사 코드 - 모바일용.html`
  - 로컬 파일과 OneDrive 대응 파일의 SHA-256 해시 일치를 확인했다.
- 검증:
  - `git diff --check` 통과.
  - 번들 Node로 `node --check tools/result-page-check.mjs` 통과.
  - `node tools/result-page-check.mjs` 통과: 28개 파일, 196개 결과 화면 viewport 케이스 확인.
  - 1회 데스크톱/모바일 코드와 1회 로그인 코드의 inline script 문법 검사 통과.
  - 완료 조건 함수 추출 검증 통과: 합격은 `true`, 불합격과 미응답은 `false`.
  - 브라우저 플러그인은 보안 정책상 `file://` 직접 접근이 차단되어, 로컬 HTTP 서버로 페이지 로드 확인 후 서버를 종료했다.
- 남은 주의사항:
  - 올리사이트 하단 버튼은 같은 DOM 또는 접근 가능한 상위 문서에 있을 때만 숨김/복구가 가능하다. 별도 보안 컨텍스트에 있으면 브라우저 정책상 직접 제어가 제한될 수 있다.

## 36. 2026-06-14 1회 모의고사 학습 완료 안내 문구와 강조 색상 보정

- 사용자 요청에 따라 1회 모의고사 학습 완료 안내 문구를 더 짧고 직접적인 표현으로 조정했다.
- `mock-exam/desktop-01.html`과 `mock-exam/mobile-01.html`의 `학습 완료` 버튼 fallback 안내 문구를 다음처럼 분기했다.
  - 데스크톱: `화면 아래의 '학습을 완료합니다.' 버튼을 누르세요. ↘`
  - 모바일: `화면 아래의 '학습을 완료합니다.' 버튼을 누르세요.` 다음 줄에 `↓`
- OneDrive 1회 모의고사 로그인 코드 최종 파일의 완료 안내 카드도 동일 문구로 수정했다.
  - 데스크톱은 문장 끝에 `↘`가 붙도록 조정했다.
  - 모바일은 다음 줄에 `↓`가 표시되도록 조정했다.
  - `'학습을 완료합니다.'` 문구에는 주황색 `#F29A27` 강조를 적용했다.
- OneDrive 대응 파일 동기화:
  - `2-1. ★★★ 1회 모의고사 코드 - 데스크톱용.html`
  - `2-2. ★★★ 1회 모의고사 코드 - 모바일용.html`
  - 로컬 파일과 OneDrive 대응 파일의 SHA-256 해시 일치를 확인했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - 1회 데스크톱/모바일 코드와 1회 로그인 코드 인라인 스크립트 문법 검사 통과.
  - `node tools/result-page-check.mjs` 통과: 28개 파일, 196개 결과 화면 viewport 케이스 확인.
  - Chrome/Playwright에서 로그인 코드 완료 안내가 데스크톱/모바일 각각 요청 문구로 표시되고, 강조 문구 색상이 `rgb(242, 154, 39)`로 적용됨을 확인했다.
  - Chrome/Playwright에서 결과 화면 `학습 완료` 버튼 fallback 안내 문구가 데스크톱/모바일 각각 요청 문구로 표시됨을 확인했다.
- 남은 주의사항:
  - 브라우저 기본 `alert()` fallback 문구에는 색상을 입힐 수 없으므로, 주황색 강조는 실제 올리사이트 안내 카드 DOM에 적용했다.
  - 이번 작업은 1회 모의고사 데스크톱/모바일 코드와 1회 로그인 코드 안내 문구만 수정했다.

## 35. 2026-06-14 1회 모의고사 결과 화면 학습 완료 버튼 및 올리사이트 완료 안내 보정

- 사용자 요청에 따라 1회 모의고사 결과 화면에서 `회 선택하기` 버튼을 제거하고, `다시 풀기` 아래에 `학습 완료` 버튼을 추가했다.
- `mock-exam/desktop-01.html`과 `mock-exam/mobile-01.html` 모두 `학습 완료` 버튼 클릭 시 올리사이트 본 창을 focus한 뒤 GitHub Pages 문제풀이/결과 창을 닫도록 했다.
- 창이 닫히지 않는 환경에서는 안내 문구를 표시하도록 했다.
  - 데스크톱: `↘`와 함께 오른쪽 아래의 `학습을 완료합니다` 버튼 안내
  - 모바일: `↓`와 함께 화면 아래의 `학습을 완료합니다` 버튼 안내
- OneDrive 1회 모의고사 로그인 코드 최종 파일의 완료 안내도 데스크톱/모바일에 맞춰 `↘`/`↓` 안내가 나오도록 수정했다.
- OneDrive 대응 파일 동기화:
  - `2-1. ★★★ 1회 모의고사 코드 - 데스크톱용.html`
  - `2-2. ★★★ 1회 모의고사 코드 - 모바일용.html`
  - 로컬 파일과 OneDrive 대응 파일의 SHA-256 해시 일치를 확인했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - 1회 데스크톱/모바일 코드와 1회 로그인 코드 인라인 스크립트 문법 검사 통과.
  - `node tools/result-page-check.mjs` 통과: 28개 파일, 196개 결과 화면 viewport 케이스 확인.
  - Chrome/Playwright에서 1회 데스크톱/모바일 결과 화면의 버튼 순서가 `다시 풀기`, `학습 완료`로 표시되고, `회 선택하기` 버튼이 사라졌음을 확인했다.
  - Chrome/Playwright에서 1회 로그인 코드 완료 안내가 데스크톱은 `↘`, 모바일은 `↓` 문구로 표시됨을 확인했다.
- 남은 주의사항:
  - 이번 작업은 사용자 요청대로 1회 모의고사 데스크톱/모바일 코드와 1회 로그인 코드만 수정했고, 2~10회는 수정하지 않았다.

## 34. 2026-06-14 1회 모의고사 로그인 코드 학습 결과 UI를 1400제 모바일 결과 UI 기준으로 보정

- 사용자 추가 설명에 따라 대상 범위를 1회 모의고사 `로그인 코드` 안의 `학습 결과 UI 디자인`으로 재확정했다.
- 기준 파일을 다시 확인해 `1400題 4부 로그인 코드 - 모바일용.html`의 `학습 결과`, `오답 중심 복습`, `틀린 문항` 스크롤 목록 구조를 기준으로 삼았다.
- OneDrive 1회 모의고사 로그인 코드 최종 파일의 결과 화면을 다음 구조로 교체했다.
  - 상단: `1회 모의고사 / 학습 결과 / 응시자`
  - 요약 카드: 합격·불합격 상태, 정답률, 정답, 오답, 미응답, 필기·실기 점수와 합격 기준
  - 복습 카드: `오답 중심 복습` 아래 `틀린 문항` 목록만 내부 세로 스크롤
  - 하단 버튼: `회 선택하기`, `다시 풀기`
- 로그인 화면, 새 창 열기, 완료 안내, Firebase 완료 확인 흐름은 건드리지 않았다.
- 검증:
  - 1회 로그인 코드 인라인 스크립트 문법 검사 통과.
  - Chrome/Playwright 모바일 390x844 렌더링에서 `학습 결과` UI, `오답 중심 복습`, `틀린 문항 2문항`, `회 선택하기`/`다시 풀기` 버튼 표시 확인.
  - 오답 80개 케이스에서 `.result-review-list`만 `overflow-y:auto`로 실제 내부 스크롤되고, 바깥 복습 카드는 스크롤 박스로 변하지 않음을 확인.
- 남은 주의사항:
  - 이번 작업은 사용자 요청대로 1회 모의고사 로그인 코드만 수정했고, 2~10회 모의고사 로그인 코드는 수정하지 않았다.

## 33. 2026-06-14 1회 모의고사 모바일 결과 복습 박스 스크롤 보정

- 사용자 요청에 따라 1회 모의고사 모바일 학습 결과 화면만 대상으로 확인했다.
- `mock-exam/mobile-01.html`의 결과 화면에서 `#quick-nav-container-result`에 남아 있던 `overflow-x:hidden` 때문에 바깥 복습 카드가 세로 스크롤 영역처럼 계산되는 문제를 보정했다.
- 바깥 복습 카드는 1400제 결과 UI처럼 `overflow:visible`로 두고, 실제 상하 스크롤은 내부 `.result-review-list`의 `overflow-y:auto`에만 남도록 한 줄만 수정했다.
- OneDrive 대응 파일 `01회 모의고사/02. 개선판 (完)/02. (★삭제 주의) 최종 코드/2-2. ★★★ 1회 모의고사 코드 - 모바일용.html`에도 동일 변경을 반영했다.
- 로컬 파일과 OneDrive 대응 파일의 SHA-256 해시가 일치함을 확인했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - `node tools/result-page-check.mjs` 통과: 28개 파일, 196개 결과 화면 viewport 케이스 확인.
  - Chrome/Playwright 모바일 390x844 렌더링에서 바깥 복습 카드 `overflow-y: visible`, 내부 틀린 문항 목록 `overflow-y: auto` 확인.
- 남은 주의사항:
  - 이번 작업은 사용자 요청대로 1회 모의고사 모바일 결과 UI만 수정했고, 2~10회 모의고사 및 1400제 파일은 수정하지 않았다.

## 32. 2026-06-14 OneDrive 우선 확인 흐름을 AGENTS 고정 규칙으로 승격

- 사용자가 매번 요청하지 않아도 같은 방식으로 처리되도록 `AGENTS.md`에 고정 작업 규칙을 추가했다.
- `mock-exam` 또는 `quiz-1400` 코드 파일 수정 시 기본 순서를 다음으로 명시했다.
  1. 로컬 파일 수정
  2. 대응 OneDrive 최종 코드 파일 동기화/저장
  3. OneDrive 또는 로컬 미리보기 기준 확인
  4. 검증 실행
  5. `WORKLOG.md` 갱신
  6. Git 커밋
  7. GitHub `origin/main` 푸시
- 이 OneDrive 우선 확인 흐름과 자동 커밋/푸시 규칙은 사용자가 명시적으로 규칙 변경을 요청하지 않는 한 임의로 빼거나, 약화하거나, 삭제하지 않는다고 기록했다.

## 31. 2026-06-14 OneDrive 우선 확인 후 GitHub 확정 흐름

- 앞으로 `mock-exam` 또는 `quiz-1400` 코드 수정 시 기본 확인 순서를 다음처럼 운영한다.
  1. 로컬 저장소 파일을 수정한다.
  2. 대응되는 OneDrive 최종 코드 파일에 먼저 동기화한다.
  3. OneDrive/로컬 미리보기 기준으로 화면과 동작을 먼저 확인한다.
  4. 이상이 없으면 로컬 Git 저장소에 커밋한다.
  5. 마지막에 GitHub `origin/main`으로 푸시한다.
- 이유:
  - 다른 컴퓨터에서 작업할 때는 OneDrive가 실시간 동기화 기준이 되므로, GitHub Pages를 매번 열어 확인하는 것보다 빠르고 캐시 문제도 적다.
  - GitHub Pages 확인은 배포 후 최종 확인용으로만 쓰는 편이 토큰과 시간을 줄일 수 있다.
  - OneDrive 폴더 자체는 현재 `.git` 저장소가 아니므로 "OneDrive 커밋"이 아니라 "OneDrive 최종 코드 파일 동기화/저장"으로 표현한다.
- 단, GitHub는 최종 이력 관리와 배포 기준이므로 생략하지 않는다.
  - 작업 완료 전에는 OneDrive 동기화 확인, 검증 실행, `WORKLOG.md` 갱신, Git 커밋, GitHub 푸시까지 마무리한다.

## 30. 2026-06-12 모의고사 모바일 결과 UI 재정렬 및 완료 안내 보완

- 사용자 확인 화면에서 1400제 모바일 결과 UI와 모의고사 모바일 결과 UI가 다르게 보이는 문제를 다시 확인했다.
  - 모의고사 모바일 1~10회 결과 화면의 이전 오버라이드 일부가 남아 있어 1400제와 간격, 버튼 구조, 결과 카드 배치가 완전히 같지 않았다.
  - `mock-exam/mobile-01.html`~`mock-exam/mobile-10.html` 결과 UI 보정 블록을 1400제 모바일 결과 UI 기준으로 다시 맞췄다.
  - 결과 화면 하단에 1400제의 `장 선택하기` 버튼과 같은 위치/구조로 `회 선택하기` 버튼을 추가했다.
  - `회 선택하기`는 문제풀이 창에서 자동으로 올리사이트 본 창을 focus/close 시도하고, 닫히지 않으면 본 창에서 회차를 선택하라는 안내를 표시한다.
- 1회 모의고사 완료 안내가 실제 환경에서 뜨지 않는 문제를 보완했다.
  - OneDrive 1회 로그인 코드의 새 창 열기 순서를 `window.open()` 우선, 실패 시 `window.top.open()` fallback으로 조정했다.
  - `postMessage`가 환경에 따라 opener로 돌아오지 않을 수 있어, 1회 로그인 코드에 Firebase 결과 존재 여부만 확인하는 보조 완료 확인을 추가했다.
  - 본 창은 결과 페이지로 전환하지 않고, 결과 저장이 확인되면 "문제풀이가 완료되었습니다. 올리사이트의 학습 완료하기 버튼을 눌러주세요." 안내만 표시한다.
  - 학습 완료하기 자동 클릭은 적용하지 않았다.
- OneDrive 원본 관리 폴더 동기화:
  - 모의고사 1~10회 모바일 최종 코드 10개를 로컬 `mock-exam/mobile-01.html`~`mobile-10.html`과 동기화했고 SHA-256 일치를 확인했다.
  - 1회 모의고사 로그인 코드는 OneDrive 최종 코드 파일에 직접 반영했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - `node tools/result-page-check.mjs` 통과: 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합.
  - 모의고사 모바일 1~10회 HTML 인라인 스크립트 문법 검사 통과.
  - 1회 로그인 코드 인라인 스크립트 문법 검사 통과.
  - 로컬 모바일 미리보기에서 결과 화면이 `회 선택하기`/`다시 풀기` 2개 버튼, `오답 중심 복습`, 1400제형 결과 카드 폭으로 표시됨을 확인했다.
  - 로컬 완료 안내 보조 확인 미리보기에서 Firebase 결과 존재 응답 시 완료 안내가 `display: block`으로 표시됨을 확인했다.

## 1. 현재 프로젝트 목표

- 청각장애인 요양보호사 자격시험 대비 모의고사 웹 파일을 관리한다.
- A컴퓨터와 B컴퓨터에서 같은 GitHub 저장소를 기준으로 이어 작업할 수 있게 한다.
- 핵심 작업 대상은 `mock-exam`, `quiz-1400`, `vocabulary` 폴더이며, 문제/정답 데이터는 원본 PDF, 정답지, 검수 엑셀 기준으로만 수정한다.

## 2. 지금까지 완료한 작업

- `mock-exam` 1~10회 데스크톱용/모바일용 파일을 Git 저장소에 정리했다.
- 1회 모의고사에서 만든 모바일/태블릿 UI 개선 사항을 2~10회 모의고사에도 동일하게 반영했다.
- 스마트폰에서 내용이 넘칠 때만 문항 영역이 세로 스크롤되도록 조정했다.
- 태블릿 세로 화면에서는 남는 공간이 과도하지 않도록 문제 영역 크기를 자동 조절하도록 수정했다.
- 결과 페이지의 틀린 문항, 미응답 문항, 정답 문항 칩 목록을 화면 크기에 맞게 반응형으로 정리했다.
- 통합 엑셀과 PDF/정답지 기준으로 1~10회 총 800문항의 정답번호를 재검토했다.
- 엑셀 기준으로 발견된 정답번호 불일치 43건을 데스크톱용과 모바일용 모두 수정했다.
- 명확한 문구 오타 일부를 원문 확인 후 수정했다.
  - 예: `허루` -> `하루`
  - 예: `뇌졸중으로 뇌가 손상` -> `뇌졸중으로 소뇌가 손상`
  - 예: `영양주머니를 영양액을` -> `영양주머니에 영양액을`
  - 예: 3회 23번 중복 보기 수정
- `quiz-1400/login.html`을 추가했고, `quiz-1400/mobile-01.html`의 모바일 스크롤 관련 수정도 반영했다.
- `AGENTS.md`에 사용자가 "작업 종료 정리해 주세요"라고 요청할 때 수행할 작업 종료 규칙을 추가했다.
- 최상위 폴더를 번호 형식으로 정리했다.
  - `01. 청각장애인 요양보호사`
  - `02. 작업 목록`
- 실제 작업 대상 폴더 `mock-exam`, `quiz-1400`, `vocabulary`는 삭제하지 않고 `02. 작업 목록` 아래로 이동했다.
- 사용자가 `03. 기존 프로젝트 파일`은 불필요하다고 판단해 삭제했다.
- 앞으로 `02. 작업 목록/mock-exam` 또는 `02. 작업 목록/quiz-1400` 코드 파일을 수정할 때는 OneDrive의 원본 관리 폴더에도 동일 변경을 반영하기로 했다.
  - 기준 경로: `C:\Users\COM-JHYUTG\OneDrive\바탕 화면\01. 개인용\01. 자기계발\01. 코딩\00. 업무 관련 코딩\1. 청각장애인 요양보호사 양성`
  - 모의고사: `01. 합격!! 모의고사`
  - 1400제: `02. 합격!! 1400題`
- OneDrive의 `01회 모의고사/02. 개선판 (完)/02. (★삭제 주의) 최종 코드` 폴더를 검토했다. 파일은 삭제하지 않고, 유지/삭제 후보만 분류했다.
- OneDrive의 `01회 모의고사/02. 개선판 (完)/02. (★삭제 주의) 최종 코드` 폴더를 정리했다. 로컬 최신 데스크톱/모바일 파일을 OneDrive 주력 파일에 반영한 뒤, 삭제 후보 파일을 제거해 3개 파일만 남겼다.
- 사용자의 요청으로 대비용 파일 3개를 복구했다. 삭제 당시 원본은 휴지통/주변 폴더에서 찾지 못해, 로컬 최신 `desktop-01.html` 기준으로 `3. ★★★ 1회 모의고사 퀴즈 문제 데이터 코드.html`, `4. ★★ 1회 모의고사 임시용 코드.html`, `5. ★ 1회 모의고사 검토용 코드.html`을 다시 생성했다.
- `02. 작업 목록/(관련 자료)/모의고사_1-10회_문항_정답_정리_PDF대조완료.xlsx` 기준으로 `02. 작업 목록/mock-exam` 1~10회 데스크톱/모바일 문제 데이터를 재검증했다.
- 검증 결과 20개 HTML 파일 모두 각 80문항이며, 데스크톱/모바일 문제 데이터는 서로 일치했고, 엑셀 기준 정답번호 불일치는 0건이었다.
- 엑셀의 문제/보기 텍스트와 HTML 텍스트는 문항 설명 포함 여부, 마침표/띄어쓰기, 이미지 보기 표기 등에서 차이가 있었다. 다만 엑셀 원본에도 문맥상 명백히 의심되는 보기 텍스트가 있어 HTML 문제 데이터를 엑셀 텍스트로 일괄 덮어쓰지는 않았다.

## 3. 수정한 파일과 수정 이유

| 파일/폴더 | 수정 이유 |
| --- | --- |
| `mock-exam/desktop-01.html` ~ `mock-exam/desktop-10.html` | 1~10회 데스크톱용 모의고사 UI, 결과 페이지, 문제/정답 데이터 보정 |
| `mock-exam/mobile-01.html` ~ `mock-exam/mobile-10.html` | 1~10회 모바일용 모의고사 UI, 스마트폰/태블릿 반응형 처리, 문제/정답 데이터 보정 |
| `quiz-1400/login.html` | 올리사이트/퀴즈용 로그인 페이지 추가 |
| `quiz-1400/mobile-01.html` | 모바일 화면에서 불필요한 세로 스크롤이 남지 않도록 조정 |
| `AGENTS.md` | 작업 종료 정리 요청 시 Git 상태 확인, 변경 요약, `WORKLOG.md` 갱신, 검증 기록, 남은 작업 정리, 명시적 허용 시 커밋/푸시 규칙 추가 |
| `WORKLOG.md` | A/B 컴퓨터에서 이어 작업하기 위한 핵심 작업 요약 기록 |
| `01. 청각장애인 요양보호사/` | 청각장애인 요양보호사 관련 자료를 별도로 관리하기 위한 빈 분류 폴더 |
| `02. 작업 목록/` | 실제 작업 대상인 `mock-exam`, `quiz-1400`, `vocabulary`를 관리하기 위한 분류 폴더 |
| `AGENTS.md` | 앞으로 `mock-exam`, `quiz-1400` 코드 수정 시 OneDrive 대응 파일에도 동일 변경을 반영하도록 작업 규칙 추가 |

## 4. 실행한 테스트 또는 확인한 내용

- Git 상태 확인:
  - 브랜치: `main`
  - 원격 저장소: `origin` -> `https://github.com/jlysm91/deaf-care-worker-exam.git`
  - `HEAD`와 `origin/main` 동기화 여부 확인
- `mock-exam` 전체 데이터 검증:
  - 데스크톱 10개 파일, 모바일 10개 파일 모두 각 80문항인지 확인
  - 데스크톱/모바일의 문항, 보기, 정답, 영상 ID 데이터가 서로 일치하는지 확인
  - 통합 엑셀 기준 800문항 정답번호 불일치가 0건인지 확인
- `git diff --check`로 공백 오류 및 기본 diff 오류 여부를 확인했다.
- 사용자가 모바일/태블릿 UI 적용 상태를 실제 화면에서 확인했다.
- 마지막으로 반영된 주요 커밋:
  - `4e82806 Add quiz login page and mobile scroll fix`
  - `aa038d1 Correct integrated mock exam answers`
  - `5291a36 Correct mock exam PDF content`
  - `c169bc5 Apply first mock exam UI to rounds 2-10`
- 폴더 구조 정리 확인:
  - `01. 청각장애인 요양보호사`, `02. 작업 목록` 확인
  - `02. 작업 목록` 아래에 `mock-exam`, `quiz-1400`, `vocabulary` 보관 확인
  - OneDrive 기준 경로와 `01. 합격!! 모의고사`, `02. 합격!! 1400題` 폴더 존재 확인
  - `git status -sb`로 폴더 이동으로 인한 경로 변경 상태 확인
- OneDrive 1회 모의고사 최종 코드 폴더 검토:
  - 총 29개 파일 확인
  - `.bak-*` 백업 파일 16개, `-DESKTOP-N9UCJ8E` 충돌 파일 5개 확인
  - `2-1. ★★★ 1회 모의고사 코드 - 데스크톱용(2차).html`, `2-2. ★★★ 1회 모의고사 코드 - 모바일용.html`은 80문항이나 로컬 기준 파일보다 1회 문구 보정이 덜 반영됨
  - `3. ★★★ 1회 모의고사 퀴즈 문제 데이터 코드.html`, `4. ★★ 1회 모의고사 임시용 코드.html`, `5. ★ 1회 모의고사 검토용 코드.html`은 `모0120` 문항 ID가 빠지고 과거 오타가 남아 있어 실사용 파일로 부적절함
- OneDrive 1회 모의고사 최종 코드 폴더 정리 확인:
  - 정리 실행 시점에 표시된 삭제 후보 10개 파일 삭제
  - 남은 파일은 로그인 코드, 데스크톱용 최종 코드, 모바일용 최종 코드 3개
  - OneDrive 데스크톱/모바일 최종 코드 파일이 로컬 `02. 작업 목록/mock-exam/desktop-01.html`, `mobile-01.html`과 SHA-256 해시 기준으로 동일함을 확인
  - 데스크톱/모바일 최종 코드 모두 `모0101`~`모0180` 80개 문항 ID 확인
  - 과거 오타 패턴 `요양보서비스`, `허루`, `심한 화상이었면`, `30분 정도로`, `다리를 통로`, `완전히 관심`, `굽은 자세` 등이 남아 있지 않음을 확인
- OneDrive 1회 모의고사 대비용 파일 복구 확인:
  - `3. ★★★ 1회 모의고사 퀴즈 문제 데이터 코드.html`은 로컬 최신 `desktop-01.html`의 `quizRawData` 블록 기준으로 재생성
  - `4. ★★ 1회 모의고사 임시용 코드.html`, `5. ★ 1회 모의고사 검토용 코드.html`은 로컬 최신 `desktop-01.html`과 SHA-256 해시 기준 동일
  - 복구한 3개 파일 모두 `모0101`~`모0180` 80개 문항 ID 확인
  - 복구한 3개 파일 모두 과거 오타 패턴이 남아 있지 않음을 확인
- `mock-exam` 1~10회 엑셀 대조 검증:
  - 기준 엑셀 `전체문항` 시트에서 800문항을 읽어 대조
  - `02. 작업 목록/mock-exam/desktop-01.html` ~ `desktop-10.html`, `mobile-01.html` ~ `mobile-10.html` 모두 `quizRawData` 80문항 확인
  - 데스크톱/모바일 같은 회차의 `number`, `question`, `options`, `correctAnswer`, `videoId` 일치 확인
  - 엑셀 기준 `correctAnswer` 불일치 0건 확인
  - 엑셀 텍스트와 HTML 텍스트의 차이는 발견됐지만, 엑셀 자체에 의심 행이 있어 코드에 일괄 반영하지 않음
- 사용자가 추가 제공한 PDF 10개를 이미지 렌더링 후 Windows OCR과 페이지 이미지 육안 확인으로 재검수했다.
  - PDF는 텍스트 추출이 되지 않는 스캔형 파일이라 `pypdfium2` 렌더링 이미지와 Windows OCR 결과를 함께 사용했다.
  - 엑셀 기준 정답번호는 맞았지만, PDF 원문 기준 문제/보기 문구가 실제로 다른 항목이 있어 `mock-exam` 1, 2, 3, 4, 5, 8회 데스크톱/모바일 파일을 수정했다.
  - 주요 수정: 2회 5번 보기 ④/⑤, 2회 50번 문항, 2회 57번 보기 배열, 2회 75번 보기 배열, 3회 18번 정답 보기, 4회 41번 정답 보기, 8회 54번 보기 배열 등.
  - `02. 작업 목록/mock-exam`의 수정 회차 12개 HTML은 모두 80문항이며, 데스크톱/모바일 같은 회차 데이터가 일치함을 확인했다.
  - OneDrive `01. 합격!! 모의고사`의 대응 주력 최종 코드 파일(`2-1` 데스크톱용, `2-2` 모바일용) 12개를 로컬 최신 파일과 동일하게 맞췄고 SHA-256 해시 일치를 확인했다.
  - OneDrive 대응 데이터 전용 최종 파일(`3. ★★★ ... 퀴즈 문제 데이터 코드.html`) 6개는 로컬 최신 `desktop-XX.html`의 `quizRawData` 블록 기준으로 재생성했고, 80문항 데이터가 로컬과 일치함을 확인했다.
  - OCR 임시 산출물 `.tmp_pdf_ocr`, `.tmp_pdf_pages`는 검증 후 삭제했다.
  - `git diff --check`는 통과했으며, Git의 CRLF 변환 경고만 표시됐다.

## 5. 아직 남은 작업

- 새 오타 검수표나 원본 PDF 기준 추가 수정 요청이 있으면 문제/보기/정답만 좁게 수정한다.
- 실제 GitHub Pages 또는 올리사이트 반영 화면에서 캐시가 남아 있을 수 있으므로 필요 시 새로고침 후 확인한다.
- `quiz-1400`의 나머지 회차나 로그인 흐름을 추가로 손볼 경우, `mock-exam`과 섞지 말고 별도 범위로 작업한다.
- 새 컴퓨터에서 이어 작업할 때는 먼저 원격 최신 상태를 가져와 로컬 상태를 맞춘다.
- 현재 `01. 청각장애인 요양보호사`는 빈 폴더라 Git에는 표시되지 않을 수 있다. 필요하면 실제 목록 파일을 추가한다.
- `mock-exam`, `quiz-1400` 수정 시 OneDrive 쪽 대응 파일 구조가 로컬과 다르므로 최종 코드 파일을 먼저 확인한다. 중복 백업 파일이나 `-DESKTOP-N9UCJ8E` 같은 충돌 파일은 사용자가 요청하지 않는 한 수정하지 않는다.
- OneDrive 1회 모의고사 최종 코드 폴더는 주력 3개 파일과 대비용 3개 파일, 총 6개 파일을 유지한다. 이후 1회 모의고사 수정 시 주력 데스크톱/모바일 파일을 우선 반영하고, 필요 시 대비용 파일도 최신 기준으로 맞춘다.
- 엑셀 텍스트와 HTML 텍스트를 완전히 맞추려면 먼저 엑셀 원본의 의심 행을 원본 PDF/docx로 재확인해야 한다. 예: 3회 10번 보기처럼 문항 주제와 맞지 않는 보기 텍스트가 `정상` 상태로 남아 있는 행이 있다.

## 6. 다음 컴퓨터에서 이어서 작업할 때 주의할 점

- 작업 시작 전 반드시 실행한다.
  - `git status -sb`
  - `git fetch origin main`
  - `git rev-list --left-right --count HEAD...origin/main`
- 로컬이 뒤처져 있으면 먼저 `git pull --ff-only origin main`으로 최신화한다.
- 작업 중 로컬 변경이 있으면 덮어쓰지 말고 먼저 변경 파일을 확인한다.
- 문제/정답 데이터는 임의로 바꾸지 말고 원본 PDF, 정답지, 검수 엑셀 중 확인 가능한 기준이 있을 때만 수정한다.
- `videoId`는 문제 데이터와 연결되어 있으므로 문제/보기/정답 수정 중에도 함부로 삭제하거나 바꾸지 않는다.
- 데스크톱용과 모바일용은 같은 회차의 문제 데이터가 일치해야 한다.
- `02. 작업 목록/mock-exam` 또는 `02. 작업 목록/quiz-1400` 코드 파일을 수정하면 OneDrive 기준 경로의 대응 파일에도 동일 변경을 반영하고, 작업 종료 전 반영 여부를 확인한다.
- GitHub Pages나 외부 사이트는 반영 지연 또는 브라우저 캐시가 있을 수 있다.
- 민감 정보, 로그인 정보, API 키는 이 파일이나 코드에 기록하지 않는다.

## 7. 2026-06-04 작업 종료 요약

- 1~10회 모의고사 코드 작업은 종료 상태다.
- 마지막 반영 커밋은 `4151e87 Correct mock exams from PDF review`이며, `main` 브랜치는 `origin/main`과 동기화되어 있었다.
- PDF 10개를 스캔 이미지 기준으로 재검수했고, 확인된 문제/보기 문구 오류는 `02. 작업 목록/mock-exam`의 데스크톱용/모바일용 파일에 반영했다.
- 수정 대상은 1, 2, 3, 4, 5, 8회 모의고사 코드 파일이며, 같은 회차의 데스크톱용/모바일용 문제 데이터 일치를 확인했다.
- OneDrive `01. 합격!! 모의고사`의 대응 주력 최종 코드 파일과 데이터 전용 최종 파일에도 동일 변경을 반영했다.
- 검증 결과 `mock-exam` 데스크톱용 10개, 모바일용 10개 파일은 모두 각 80문항이고, 데스크톱/모바일 같은 회차 데이터가 일치했다.
- `git diff --check`는 통과했고, OCR 임시 산출물 `.tmp_pdf_ocr`, `.tmp_pdf_pages`는 삭제했다.
- 이번 종료 정리 요청 전 기준 Git 작업 트리는 깨끗했다.
- 이번 종료 정리에서는 코드 파일을 추가 수정하지 않았고, `WORKLOG.md`에 종료 요약만 추가했다.

## 8. 2026-06-04 1400題 1부 다듬기 진행 요약

- GitHub 저장소 구조는 루트의 `mock-exam`, `quiz-1400`, `vocabulary`, `AGENTS.md`, `WORKLOG.md` 형식으로 유지하기로 했다. 로컬 정리용 `01/02` 폴더 구조를 GitHub에 반영하지 않는다.
- 이번 작업 범위는 `quiz-1400/desktop-01.html`, `quiz-1400/mobile-01.html` 2개 파일로 제한했다.
- `quiz-1400/login.html`과 OneDrive의 `1. ★★★ 1부 로그인 코드.html`은 사용자 지시에 따라 수정하지 않았다.
- 1400題 1부 결과 페이지를 모의고사 코드의 결과 UX처럼 `학습 요약`, `틀린 문항`, `미응답 문항`, `정답 문항` 칩 목록 방식으로 바꿨다.
- 모바일 1부는 결과 페이지 전환 후 하단 문제 풀이 버튼이 남지 않도록 `quiz-active` 상태일 때만 퀴즈 화면이 강제 표시되게 보정했다.
- 모바일 1부에 모의고사 모바일 기준의 태블릿 세로 화면 보기 카드 자동 높이/글자 크기 보정 로직을 추가했다.
- 문제 데이터, 정답, `videoId`, Firebase `examId`, `examResult1400` postMessage 제출 흐름은 바꾸지 않았다.
- OneDrive `02. 합격!! 1400題/1부/2. (★삭제 주의) 최종 코드/02. 개선판`의 데스크톱용/모바일용 최종 코드 파일에도 동일 변경을 반영했고 SHA-256 해시 일치를 확인했다.
- 검증:
  - `git diff --check` 통과. Git의 CRLF 변환 경고만 표시됨.
  - `quiz-1400/desktop-01.html`, `quiz-1400/mobile-01.html` 인라인 스크립트 문법 파싱 통과.
  - 1부 장별 데이터 개수 확인: 1장 33문항, 2장 103문항, 3장 29문항, 4장 64문항.
  - 데스크톱/모바일 1부의 장별 문제 데이터 일치 확인.
  - 로컬 브라우저에서 데스크톱 결과 화면, 스마트폰 결과 화면, 스마트폰 문제 화면, 태블릿 세로 문제 화면을 확인했다.
- 초기 1부 다듬기 변경은 사용자 요청에 따라 커밋/푸시까지 완료했다. 이후 사용자가 학습 결과 UI만 두 번째 사진 방식으로 되돌리도록 추가 요청했다.

## 9. 2026-06-04 1400題 1부 결과 UI 추가 복구 요약

- 사용자 요청에 따라 `quiz-1400/desktop-01.html`, `quiz-1400/mobile-01.html`의 학습 결과 UI만 두 번째 사진 기준의 `문항별 결과 (C: 정답 / M: 내 답)` 그리드/페이저 방식으로 되돌렸다.
- `학습 요약`, `틀린 문항`, `미응답 문항`, `정답 문항` 칩 목록 CSS와 렌더링 함수만 제거하고, 80문항 단위 결과 그리드와 이전/다음 페이지 이동을 복구했다.
- 로그인 코드, 제출/장 선택 흐름, 문제 데이터, 정답, `videoId`, Firebase `examId`, `examResult1400` postMessage 흐름, 모바일/태블릿 문제 화면 보정은 바꾸지 않았다.
- OneDrive `02. 합격!! 1400題/1부/2. (★삭제 주의) 최종 코드/02. 개선판`의 데스크톱용/모바일용 최종 코드 파일에도 동일 변경을 반영했고 SHA-256 해시 일치를 확인했다.
- 검증:
  - `git diff --check` 통과.
  - 데스크톱/모바일 인라인 스크립트 문법 파싱 통과.
  - 1부 장별 데이터 개수 확인: 1장 33문항, 2장 103문항, 3장 29문항, 4장 64문항.
  - 데스크톱/모바일 1부의 장별 문제 데이터 개수 일치 확인.
  - 임시 검증용 복사본을 headless Chrome으로 렌더링해 데스크톱 결과 화면은 10열, 모바일 결과 화면은 5열 문항별 그리드로 표시되고 칩 UI가 남아 있지 않음을 확인했다.

## 10. 2026-06-04 1400題 1부 모바일 세로 화면 추가 조정 요약

- 사용자 요청에 따라 `quiz-1400/mobile-01.html`만 수정했다. 로그인 코드, 데스크톱용 코드, 문제 데이터, 정답, `videoId`, Firebase `examId`, 제출/장 선택 흐름은 바꾸지 않았다.
- 스마트폰/태블릿 세로 화면에서 빠른 문항 이동 패널이 과도하게 크게 뜨지 않도록 하단 팝업 폭, 화살표, 버튼 높이, 마지막 페이지 빈 칸 표시를 조정했다.
- 스마트폰 세로 화면의 스크롤 판정을 1회 모의고사 모바일 방식과 맞춰 `#quiz-content.scroll-needed/no-scroll-needed` 기준으로 다시 조정했다.
  - 문제 내용과 5지선다가 한 화면에 맞으면 `#quiz-content.no-scroll-needed`가 붙고 상하 스크롤이 작동하지 않는다.
  - 내용이 넘치면 `#quiz-content.scroll-needed`가 붙어 문제 내용과 5지선다가 함께 상하 스크롤된다.
  - `#options`는 별도 스크롤 컨테이너로 만들지 않고 모의고사처럼 `#quiz-content` 안에서 함께 움직인다.
- 닫힌 상태의 빠른 문항 이동 그리드가 보기 카드 위에 투명하게 남아 터치를 가로막지 않도록 `visibility`와 `pointer-events`를 차단했다.
- OneDrive `02. 합격!! 1400題/1부/2. (★삭제 주의) 최종 코드/02. 개선판/3. ★★★ 1부 코드 - 모바일용.html`에도 동일 변경을 반영했고 SHA-256 해시 일치를 확인했다.
- 검증:
  - `git diff --check` 통과.
  - `quiz-1400/mobile-01.html` 인라인 스크립트 문법 파싱 통과.
  - headless Chrome 390x844 스마트폰 세로 화면에서 1장 1번 기준 `#quiz-content.no-scroll-needed`, `overflowY: hidden`, 문서/보기/문제 영역 스크롤값 모두 0임을 확인했다.
  - headless Chrome 390x640 스마트폰 세로 화면에서 오버플로 발생 시 `#quiz-content.scroll-needed`가 적용되고, 보기 카드 위 터치 드래그로 `#quiz-content.scrollTop`이 증가하며 문서 스크롤은 0임을 확인했다.
  - 닫힌 빠른 문항 이동 그리드는 `visibility: hidden`, `pointer-events: none`이고, 열린 상태에서는 `visible/auto`로 돌아오는 것을 확인했다.
  - 1부 전체 229문항을 390x844와 390x640 스마트폰 세로 화면으로 스캔해 오버플로 여부와 `scroll-needed`, `no-scroll-needed` 적용 여부가 어긋나는 문항이 없음을 확인했다.
  - headless Chrome 390x844 및 768x1024 세로 화면에서 빠른 문항 이동 패널의 2장 1번/103번 표시 높이를 확인했다.

## 11. 2026-06-04 1400題 1부 모바일 빠른 문항 이동 시범 조정 요약

- 사용자 요청에 따라 `quiz-1400/mobile-01.html`만 수정했다. 로그인 코드, 데스크톱용 코드, 문제 데이터, 정답, `videoId`, Firebase `examId`, 제출/장 선택 흐름은 바꾸지 않았다.
- 스마트폰 세로 화면에서 빠른 문항 이동 버튼이 아래쪽에서 잘려 보이던 문제를 보정했다.
  - 버튼 실제 높이가 이전 CSS의 `.nav-btn` 변수에 덮여 계산 높이보다 커지는 부분을 막고, 빠른 문항 이동 전용 `--quick-nav-btn-height` 기준으로 버튼/화살표/그리드 높이를 맞췄다.
  - 패널 내부 그리드는 세로 중앙 정렬되도록 해서 마지막 페이지처럼 문항 수가 적을 때도 아래로 붙지 않게 했다.
- 80번 다음 페이지처럼 마지막 줄 번호가 적은 경우, 남은 번호가 왼쪽부터 시작하지 않고 10열 그리드의 가운데에 오도록 첫 번호의 `grid-column-start`를 계산해 적용했다.
- OneDrive `02. 합격!! 1400題/1부/2. (★삭제 주의) 최종 코드/02. 개선판/3. ★★★ 1부 코드 - 모바일용.html`에도 동일 변경을 반영했고 SHA-256 해시 일치를 확인했다.
- 검증:
  - `git diff --check` 통과.
  - `quiz-1400/mobile-01.html` 인라인 스크립트 문법 파싱 통과.
  - headless Chrome 390x844 스마트폰 세로 화면에서 빠른 문항 이동 1~80번 페이지가 잘리지 않고, 81~103번 페이지는 같은 패널 높이 안에서 세로 중앙에 배치됨을 확인했다.
  - headless Chrome 390x640 스마트폰 세로 화면에서도 1~80번 페이지와 81~103번 페이지가 잘리지 않고, 101~103번이 10열 그리드 가운데에 배치됨을 확인했다.

## 2026-06-05 1400題 1~4부 문제/정답 데이터 대조 및 반영 요약

- 기준 자료: OneDrive `00. 문제 및 정답 내용 정리/1400題_1_2_3_4부_문항정답_PDF원문대조_최종본.xlsx`
- 대상 코드: `quiz-1400/desktop-01.html` ~ `desktop-04.html`, `quiz-1400/mobile-01.html` ~ `mobile-04.html`
- 1~4부 전체 1190문항을 `부-장-절-문항번호` 키 기준으로 코드 데이터와 대조했다.
- 코드의 기존 `correctAnswer`를 정답 기준으로 유지하고, Excel/PDF 정리본에서 확인 가능한 문제/보기 OCR성 오탈자를 수정했다.
- 수정 범위는 문제/보기 텍스트 중심이며 `videoId`, 제출/결과 흐름, Firebase/postMessage 흐름은 변경하지 않았다.
- OneDrive 원본 관리 폴더의 1400題 1~4부 최종 코드 HTML 12개 파일에도 동일한 문제/보기 데이터 변경을 반영했다.
- Excel 정리본의 `문항정리_1_2_3_4부`, `3부_PDF원문보완완료`, `3부_수정내역` 시트도 코드 기준과 맞도록 갱신했다.
- 검증 결과:
  - 원격 GitHub Pages 코드 기준 데스크톱/모바일 데이터 수: 1부 229문항, 2부 211문항, 3부 509문항, 4부 241문항
  - 데스크톱/모바일 동일성: 1~4부 모두 일치
  - Excel 정리본과 GitHub 원격 코드 최종 대조: 1190문항 기준 누락 0건, 추가 0건, 불일치 0건
- GitHub 반영:
  - 최종 원격 커밋: `0e3b5e20764d917a4e78e4743ef9145dbec120b8`
  - 커밋 메시지: `Correct 1400 quiz data text`
  - 작성자/커미터: `Codex <267193182+codex@users.noreply.github.com>`
- 작업 중 임시 GitHub Actions workflow를 사용했으나 최종 원격 트리에는 임시 workflow 파일이 남지 않도록 정리했다.
- 이번 작업 범위 밖의 UI/CSS 변경은 최종 GitHub 반영분에서 제외했다.

## 2026-06-05 1회 모의고사/1400題 1부 모바일 빠른 문항 이동 시범 조정

- 사용자 승인 전 전체 반영을 피하기 위해 `mock-exam/mobile-01.html`, `quiz-1400/mobile-01.html`만 수정했다.
- 1400題 1부 모바일 빠른 문항 이동에서 선택한 답을 문항 번호 옆에 작은 `data-answer` 표시로 붙였다. 3자리 문항에서도 숫자와 선택 답이 겹치지 않도록 번호와 답 표시를 분리했다.
- 1회 모의고사 모바일 빠른 문항 이동은 기존 80문항 전체 표시 구조를 유지하고, 패널 폭·내부 여백·간격·버튼 높이만 소폭 조정했다.
- OneDrive 대응 최종 모바일 파일 2개에도 동일본을 반영하고 SHA-256 해시 일치를 확인했다.
- 검증:
  - `mock-exam/mobile-01.html`, `quiz-1400/mobile-01.html` 인라인 스크립트 문법 파싱 통과.
  - headless Chrome 390x844 모바일 세로 화면에서 1400題 1부 2장 103번 선택 답 `(2)` 표시, 빠른 문항 이동 패널 overflow 없음 확인.
  - headless Chrome 390x844 모바일 세로 화면에서 1회 모의고사 빠른 문항 이동 패널 overflow 없음 확인.
- GitHub 커밋/푸시는 사용자 승인 전 시범 단계라 진행하지 않았다.

## 12. 2026-06-11 결과 페이지 공통 검증 시작 요약

- 모의고사와 1400題의 문제 풀이/점수 계산/저장 흐름은 서로 다르므로, 실제 HTML 코드 공통화 전에 결과 페이지 레이아웃 검증 기반을 먼저 추가했다.
- 새 파일 `tools/result-page-check.mjs`를 추가했다.
  - 별도 npm 패키지 없이 로컬 Chrome/Edge headless 모드를 CDP로 제어한다.
  - `mock-exam` 20개 파일과 `quiz-1400` 8개 파일의 결과 페이지 CSS를 대상으로 검증용 결과 화면을 구성한다.
  - 스마트폰, 짧은 스마트폰, 태블릿 세로, 태블릿 가로, 노트북, 데스크톱 화면 크기에서 가로 넘침, 주요 결과 요소 겹침, 결과 개수 일치를 검사한다.
  - `--target`, `--viewports`, `--screenshots` 옵션을 지원한다.
- 이번 단계에서는 `mock-exam` 또는 `quiz-1400` 실제 HTML 코드 파일을 수정하지 않았다. 따라서 OneDrive 대응 코드 파일 동기화는 필요하지 않았다.
- 검증:
  - 대표 파일 4개(`mock-exam/desktop-01.html`, `mock-exam/mobile-01.html`, `quiz-1400/desktop-01.html`, `quiz-1400/mobile-01.html`)를 2개 화면 크기로 먼저 검사했고 통과했다.
  - `node tools/result-page-check.mjs`로 전체 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합을 검사했고 통과했다.
- 다음 단계에서 실제 공통화 작업을 진행할 경우, 먼저 결과 렌더링 함수의 공통 데이터 형태를 정하고 `mock-exam`과 `quiz-1400`에는 각각 어댑터만 두는 방식으로 좁게 적용한다.

## 13. 2026-06-11 결과 페이지 검증 도구 자동 커밋/푸시 규칙 반영

- 사용자가 이번 결과 페이지 공통 검증 도구 작업은 별도 재요청 없이 커밋/푸시까지 진행하라고 명시했다.
- `AGENTS.md`에 기본 원칙은 명시 요청 후 커밋/푸시하되, 사용자가 특정 작업군에 대해 자동 커밋/푸시를 허용한 경우에는 검증 완료 후 별도 재확인 없이 커밋/푸시한다는 예외 규칙을 추가했다.
- 이번 자동 커밋/푸시 대상은 `tools/result-page-check.mjs`, 관련 `AGENTS.md`/`WORKLOG.md` 갱신으로 제한한다.
- 원격 `origin/main`이 로컬보다 16커밋 앞서 있어 먼저 fast-forward로 최신화했고, `WORKLOG.md` 충돌은 원격 기록과 결과 페이지 검증 기록을 모두 보존하는 방식으로 정리했다.

## 14. 2026-06-11 1400題 1~4부 결과 페이지 한글/오답 중심 구조 반영

- 사용자 요청에 따라 `quiz-1400/desktop-01.html` ~ `desktop-04.html`, `quiz-1400/mobile-01.html` ~ `mobile-04.html`의 학습 결과 페이지를 `C:/M:` 표기 중심의 전체 문항 칸 구조에서 한글 표기와 오답 중심 복습 구조로 바꿨다.
  - 결과 요약은 `정답`, `오답`, `미응답`으로 표시한다.
  - 결과 상세는 `복습이 필요한 문항` 아래 `틀린 문항`, `미응답 문항` 목록으로 분리한다.
  - 각 항목은 `정답 n번`, `내 답 n번` 또는 `내 답 없음`으로 보여 주며, 기존 `C: 정답 / M: 내 답` 표기는 제거했다.
- 모바일용 1400題 1~4부 빠른 문항 이동은 문항 번호만 보이도록 통일했다.
  - 선택 답 `(2)` 같은 별도 텍스트 표시와 `data-answer` CSS를 제거했다.
  - 답변 완료 상태는 기존처럼 버튼 색상으로만 구분한다.
- 문제 데이터, 정답, `videoId`, Firebase 저장/결과 저장, 제출/postMessage 흐름은 변경하지 않았다.
- OneDrive 원본 관리 폴더의 1400題 1~4부 `02. 개선판` 데스크톱/모바일 최종 HTML 8개에도 동일 변경을 반영했고, 로컬 파일과 SHA-256 해시 일치를 확인했다.
- `tools/result-page-check.mjs`는 새 1400題 결과 구조를 검증하도록 갱신했다.
  - 모바일 파일 앞쪽 `document.write('<style>...')` 문자열을 실제 CSS로 잘못 잡지 않도록 가장 큰 실제 `<style>` 블록을 사용하게 고쳤다.
  - 검증 harness의 `body`에 `desktop-page`/`mobile-page` 클래스를 붙여 실제 CSS 적용 상태와 더 가깝게 검사하도록 했다.
  - `position: fixed/sticky` 요소는 의도적 오버레이로 보고 일반 형제 겹침 오류에서 제외했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - 1400題 1~4부 데스크톱/모바일 HTML 8개 인라인 스크립트 문법 파싱 통과.
  - `node tools/result-page-check.mjs`로 전체 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합을 검사했고 통과했다.
- 후속 사용자 요청으로 이 저장소의 기본 종료 규칙이 자동 커밋/푸시로 바뀌었으므로, 이 변경분도 함께 커밋/푸시 대상에 포함한다.

## 15. 2026-06-11 전체 작업 자동 커밋/푸시 기본 규칙 반영

- 사용자가 앞으로는 별도 지시 없이도 작업 완료 후 Codex가 알아서 커밋/푸시해야 한다고 명시했다.
- `AGENTS.md`의 기존 규칙을 특정 작업군 예외 방식에서 저장소 전체 기본 자동 커밋/푸시 방식으로 바꿨다.
  - Codex가 코드, 문서, 도구 파일을 수정한 경우 작업 완료 후 관련 검증, OneDrive 대응 파일 확인, `WORKLOG.md` 갱신, `git status` 확인, 스테이징, 커밋, `origin/main` 푸시를 기본 종료 흐름으로 삼는다.
  - 사용자가 해당 작업에서 `커밋하지 마`, `푸시하지 마`, `검토만 해`처럼 명시적으로 금지한 경우에만 자동 커밋/푸시를 하지 않는다.
  - 검증 실패, 원격 선행 커밋, 병합 충돌, 인증 문제처럼 바로 푸시할 수 없는 상황은 가능한 범위에서 해결을 시도하고, 해결 불가능한 차단 사유만 보고한다.
- 이 규칙 변경에 따라 직전 1400題 1~4부 결과 페이지/모바일 빠른 문항 이동 개선 작업도 이번 커밋/푸시에 포함한다.

## 16. 2026-06-11 1400題 결과 페이지 오답 중심/미응답 접기 재조정

- 사용자 피드백에 따라 직전 결과 페이지의 하단 중복 요약 카드 구조를 줄이고, 1400題 1~4부 데스크톱/모바일 결과 화면을 오답 중심 구조로 다시 조정했다.
  - 하단 `정답/오답/미응답` 요약 카드 3개를 제거했다.
  - `틀린 문항`을 첫 영역으로 크게 보여 주고, `미응답 문항`은 기본 접힘 상태의 `details` 영역으로 숨겼다.
  - 미응답을 펼쳤을 때는 반복적인 `정답/내 답 없음` 카드 대신 문항 번호 칩만 보이게 줄였다.
- 상단 결과 요약과 하단 복습 목록이 서로 다른 숫자를 보이지 않도록 `analyzeResultAnswers()` 분석 함수를 추가하고, `showResult()`와 `renderResultGrid()`가 같은 계산 결과를 쓰도록 바꿨다.
- 모바일 빠른 문항 이동의 번호-only 구조와 선택 답 텍스트 제거는 이전 불편을 해결한 부분이므로 유지했다.
- 문제 데이터, 정답, `videoId`, Firebase 저장/결과 저장, 제출/postMessage 흐름은 변경하지 않았다.
- OneDrive 원본 관리 폴더의 1400題 1~4부 `02. 개선판` 데스크톱/모바일 최종 HTML 8개에도 동일 변경을 반영했고, 로컬 파일과 SHA-256 해시 일치를 확인했다.
- `tools/result-page-check.mjs`도 새 구조에 맞춰 1400題 결과 페이지에서 접힌 미응답 영역과 미응답 번호 칩을 검사하도록 갱신했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - 1400題 1~4부 데스크톱/모바일 HTML 8개 인라인 스크립트 문법 파싱 통과.
  - 대표 스크린샷 확인: `quiz-1400/desktop-01.html` 1366x768, `quiz-1400/mobile-01.html` 390x844에서 오답 중심/미응답 접기 구조가 정상 표시됨.
  - `node tools/result-page-check.mjs`로 전체 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합을 검사했고 통과했다.

## 17. 2026-06-11 결과 페이지 미응답 상세 제거 및 빠른 문항 이동 답 배지 통일

- 사용자 피드백에 따라 1400題 1~4부 데스크톱/모바일 결과 페이지의 하단 `미응답 문항` 상세 영역을 완전히 제거했다.
  - 상단 결과 요약의 `미응답` 통계는 유지했다.
  - 하단 복습 영역은 `틀린 문항`만 보여 주도록 줄였다.
  - 미응답 상세 영역을 제거한 만큼 `틀린 문항` 목록의 세로 표시 높이를 조금 늘렸다.
- 빠른 문항 이동 표기를 모의고사 1~10회와 1400題 1~4부에 다시 통일했다.
  - 선택 답이 있는 문항은 `28 [1]`처럼 보이도록 했다.
  - 실제 구현은 문항 번호와 답을 `nav-number`, `nav-answer` span으로 분리해 100번대 문항에서 텍스트가 잘리지 않도록 했다.
  - 모의고사 모바일은 기존처럼 1~80번이 한 창에 보이는 구조를 유지했다.
  - 1400題 모바일은 100문항 이상에서 폭이 좁아지는 문제를 줄이기 위해 빠른 문항 이동을 40문항 단위로 나누도록 조정했다.
- 문제 데이터, 정답, `videoId`, Firebase 저장/결과 저장, 제출/postMessage 흐름은 변경하지 않았다.
- OneDrive 원본 관리 폴더에도 동일 변경을 반영했다.
  - 모의고사 1~10회 `02. 개선판 (完)` 데스크톱/모바일 최종 HTML 20개를 동기화했다.
  - 1400題 1~4부 `02. 개선판` 데스크톱/모바일 최종 HTML 8개를 동기화했다.
  - 총 28개 OneDrive 대응 파일과 로컬 파일의 SHA-256 해시 일치를 확인했다.
- `tools/result-page-check.mjs`는 1400題 결과 페이지에서 하단 미응답 상세 요소가 렌더링되지 않아야 한다는 기준으로 갱신했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - 모의고사 1~10회와 1400題 1~4부 데스크톱/모바일 HTML 28개 인라인 스크립트 문법 파싱 통과.
  - 빠른 문항 이동 정적 검사 통과: 모의고사 20개, 1400題 8개, 1400題 모바일 40문항 단위 4개 파일 확인.
  - `node tools/result-page-check.mjs`로 전체 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합을 검사했고 통과했다.

## 18. 2026-06-11 빠른 문항 이동 답 배지 대비 및 오답 목록 7줄 기준 조정

- 사용자 피드백에 따라 모의고사 1~10회와 1400題 1~4부 데스크톱/모바일의 빠른 문항 이동 답 배지(`[n]`)를 흰 글자에서 벗어나 잘 보이도록 조정했다.
  - 답 배지는 흰 배경, 초록/주요색 글자, 얇은 테두리로 표시되도록 했다.
  - 선택 답이 있는 문항은 기존 의도대로 `28 [1]` 형식으로 보이도록 유지했다.
- 1400題 1~4부 데스크톱/모바일 결과 페이지의 틀린 문항 카드 문구를 `내 답`에서 `선택`으로 바꿨다.
- 1400題 결과 페이지 하단 `틀린 문항` 목록은 세로 7문항 기준까지 표시되도록 늘리고, 7줄을 넘으면 목록 내부에서 상하 스크롤되도록 조정했다.
  - 오답 카드 최소 높이를 54px로 고정하고, 목록 최대 높이를 446px로 맞췄다.
  - 결과 페이지의 상단 미응답 통계는 유지했고, 하단 미응답 상세 영역은 이전 결정대로 다시 추가하지 않았다.
- 문제 데이터, 정답, `videoId`, Firebase 저장/결과 저장, 제출/postMessage 흐름은 변경하지 않았다.
- OneDrive 원본 관리 폴더에도 동일 변경을 반영했다.
  - 모의고사 1~10회 `02. 개선판 (完)` 데스크톱/모바일 최종 HTML 20개를 동기화했다.
  - 1400題 1~4부 `02. 개선판` 데스크톱/모바일 최종 HTML 8개를 동기화했다.
  - 총 28개 OneDrive 대응 파일과 로컬 파일의 SHA-256 해시 일치를 확인했다.
- `tools/result-page-check.mjs`는 1400題 결과 페이지에서 `내 답` 문구가 남아 있지 않아야 하고, 7줄을 넘는 오답 목록이 내부 스크롤을 가져야 한다는 기준을 추가했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - 모의고사 1~10회와 1400題 1~4부 데스크톱/모바일 HTML 28개 인라인 스크립트 문법 파싱 통과.
  - 빠른 문항 이동 답 배지 정적 검사 통과: 모의고사 20개, 1400題 8개 확인.
  - Chrome DevTools Protocol로 `quiz-1400/desktop-01.html`을 열어 답 선택 후 빠른 문항 이동 첫 칸이 `[3]` 배지를 표시하고, 배지 글자색이 흰색이 아님을 확인했다.
  - `node tools/result-page-check.mjs`로 전체 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합을 검사했고 통과했다.

## 19. 2026-06-11 빠른 문항 이동 답 배지 박스 제거 및 정답 표기 초록색 적용

- 사용자 피드백에 따라 모의고사 1~10회와 1400題 1~4부 데스크톱/모바일의 빠른 문항 이동 `[n]` 표기에서 흰 배경, 테두리, 둥근 배지 효과를 제거했다.
  - `[n]`은 문항 번호 옆 텍스트로만 보이게 했다.
  - 문항 번호와 선택 답을 분리한 `nav-number`, `nav-answer` 구조는 100번대 잘림 방지를 위해 유지했다.
- 1400題 1~4부 데스크톱/모바일 결과 페이지의 `틀린 문항` 카드에서 `정답 n번` 부분만 초록색으로 표시되도록 했다.
  - `선택 n번`은 기존 색상 흐름을 유지했다.
  - 문제 데이터, 정답, `videoId`, Firebase 저장/결과 저장, 제출/postMessage 흐름은 변경하지 않았다.
- OneDrive 원본 관리 폴더에도 동일 변경을 반영했다.
  - 모의고사 1~10회 `02. 개선판 (完)` 데스크톱/모바일 최종 HTML 20개를 동기화했다.
  - 1400題 1~4부 `02. 개선판` 데스크톱/모바일 최종 HTML 8개를 동기화했다.
  - 총 28개 OneDrive 대응 파일과 로컬 파일의 SHA-256 해시 일치를 확인했다.
- `tools/result-page-check.mjs`는 1400題 결과 페이지에서 `정답 n번` 라벨이 별도 클래스로 표시되고 초록색을 사용하는지 검사하도록 갱신했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - 모의고사 1~10회와 1400題 1~4부 데스크톱/모바일 HTML 28개 인라인 스크립트 문법 파싱 통과.
  - 정적 검사 통과: 빠른 문항 이동 `[n]`에 배지형 배경/테두리/둥근 처리 없음, 1400題 8개 결과 카드에 `review-answer-correct` 적용 확인.
  - 대표 결과 스크린샷 확인: `quiz-1400/mobile-01.html` 390x844에서 `정답 n번`이 초록색으로 표시됨.
  - Chrome DevTools Protocol로 `quiz-1400/mobile-01.html`을 열어 답 선택 후 빠른 문항 이동 `[3]`에 별도 배경, 테두리, radius가 없음을 확인했다.
  - `node tools/result-page-check.mjs`로 전체 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합을 검사했고 통과했다.

## 20. 2026-06-11 빠른 문항 이동 선택 답 초록색 유지

- 사용자 피드백에 따라 빠른 문항 이동에서 현재 문항이면서 답을 선택한 칸이 흰색 글자로 바뀌지 않도록 조정했다.
  - 모의고사 1~10회와 1400題 1~4부 데스크톱/모바일 전체에 `active + answered` 상태의 초록색 우선 규칙을 추가했다.
  - `[n]` 답 표시는 흰 배경이나 테두리 없이 텍스트만 유지하되, 선택 후에도 초록색으로 보이게 했다.
  - 1400題 모바일의 `active` 흰색 강제 규칙을 `active.answered`일 때 초록색으로 다시 덮도록 보강했다.
- 문제 데이터, 정답, `videoId`, Firebase 저장/결과 저장, 제출/postMessage 흐름은 변경하지 않았다.
- OneDrive 원본 관리 폴더에도 동일 변경을 반영했다.
  - 모의고사 1~10회 `02. 개선판 (完)` 데스크톱/모바일 최종 HTML 20개를 동기화했다.
  - 1400題 1~4부 `02. 개선판` 데스크톱/모바일 최종 HTML 8개를 동기화했다.
  - 총 28개 OneDrive 대응 파일과 로컬 파일의 SHA-256 해시 일치를 확인했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - 모의고사 1~10회와 1400題 1~4부 데스크톱/모바일 HTML 28개 인라인 스크립트 문법 파싱 통과.
  - `node tools/result-page-check.mjs`로 전체 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합을 검사했고 통과했다.
  - Chrome DevTools Protocol로 `quiz-1400/mobile-01.html`을 열어 5번 답 선택 후 빠른 문항 이동 `1[5]`의 버튼 글자색과 `[5]` 글자색이 모두 `var(--success)` 초록색으로 계산됨을 확인했다.

## 21. 2026-06-11 빠른 문항 이동 선택 답 크기와 기준선 정렬

- 사용자 피드백에 따라 빠른 문항 이동의 `[n]` 답 표기가 문항 번호와 어긋나 보이지 않도록 조정했다.
  - 모의고사 1~10회와 1400題 1~4부 데스크톱/모바일 전체에서 `.nav-answer`의 글자 크기를 축소값 `0.78em`에서 부모 글자 크기 상속으로 변경했다.
  - `[n]`의 줄 높이와 기준선을 문항 번호와 같은 흐름으로 맞췄다.
  - 흰 배경, 테두리, 둥근 배지 효과는 다시 추가하지 않았다.
- 문제 데이터, 정답, `videoId`, Firebase 저장/결과 저장, 제출/postMessage 흐름은 변경하지 않았다.
- OneDrive 원본 관리 폴더에도 동일 변경을 반영했다.
  - 모의고사 1~10회 `02. 개선판 (完)` 데스크톱/모바일 최종 HTML 20개를 동기화했다.
  - 1400題 1~4부 `02. 개선판` 데스크톱/모바일 최종 HTML 8개를 동기화했다.
  - 총 28개 OneDrive 대응 파일과 로컬 파일의 SHA-256 해시 일치를 확인했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - 모의고사 1~10회와 1400題 1~4부 데스크톱/모바일 HTML 28개 인라인 스크립트 문법 파싱 통과.
  - `node tools/result-page-check.mjs`로 전체 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합을 검사했고 통과했다.
  - 정적 검사로 28개 HTML에서 `.nav-answer`가 `font-size: inherit`, `vertical-align: baseline`을 사용하고 배지형 배경/테두리 스타일이 없음을 확인했다.
  - Chrome DevTools Protocol로 `quiz-1400/mobile-01.html`을 열어 5번 답 선택 후 `1`과 `[5]`의 글자 크기, 줄 높이, 상단 위치, 높이가 동일함을 확인했다.

## 22. 2026-06-11 모의고사 모바일 빠른 문항 이동 40문항 페이지 적용

- 사용자 피드백에 따라 모의고사 1~10회 모바일 HTML의 빠른 문항 이동을 80문항 일괄 표시에서 40문항 단위 페이지 구조로 변경했다.
  - `NAV_PAGE_SIZE=40`을 추가하고, 1~40번 / 41~80번을 좌우 화살표로 이동하도록 했다.
  - 스마트폰 세로 화면에서는 5열 x 8줄로 표시되도록 자동 크기 계산을 조정했다.
  - 선택 답 표시는 기존 `1 [5]` 형식을 유지하고, 선택된 답 표시가 초록색으로 유지되도록 했다.
  - 데스크톱 모의고사 파일은 요청 범위 밖이므로 수정하지 않았다.
- 문제 데이터, 정답, `videoId`, Firebase 저장/제출 흐름은 변경하지 않았다.
- OneDrive 원본 관리 폴더의 모의고사 1~10회 `2-2. ★★★ n회 모의고사 코드 - 모바일용.html`에도 동일 변경을 반영했다.
  - `-DESKTOP-N9UCJ8E` 충돌 파일은 수정하지 않았다.
  - 로컬 `mock-exam/mobile-01.html`~`mobile-10.html`과 OneDrive 대응 파일 10개 SHA-256 해시 일치를 확인했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - 모의고사 모바일 10개 HTML의 inline script 20개 문법 파싱 통과.
  - 정적 검사로 모바일 10개 파일 모두 `NAV_PAGE_SIZE=40`, `nav-prev/nav-next`, `updateNavDisplay()`가 1회씩 존재하고, `FIXED_SIZE=80` 및 `Math.ceil(80/cols)`가 남아 있지 않음을 확인했다.
  - 로컬 Chrome + Playwright로 `mock-exam/mobile-01.html`을 390x844 모바일 viewport에서 열어 확인했다: 1페이지 1~40번, 2페이지 41~80번, 각 페이지 40개 버튼, 5열 배치, 좌우 화살표 활성/비활성 상태 정상, 선택 답 `[5]` 초록색 표시 정상.

## 23. 2026-06-12 모의고사 학습 결과 UI를 1400제 기준으로 통일

- 사용자 요청에 따라 모의고사 1~10회 데스크톱/모바일 학습 결과 화면을 1400題 1~4부와 같은 오답 중심 결과 UI로 맞췄다.
  - 결과 상단을 `학습 결과`, 회차 배지, 응시자명 구조로 통일했다.
  - 요약 카드에는 합격/불합격, 정답률, 정답, 오답, 미응답, 필기/실기 기준을 표시하도록 했다.
  - 하단 복습 영역은 `오답 중심 복습`으로 통일하고, 미응답 상세 목록은 중복 노출하지 않았다.
  - 틀린 문항 카드의 `정답 n번`은 초록색 강조를 사용하고, `선택 n번`은 일반 텍스트 흐름을 유지했다.
  - 태블릿 폭에서 결과 헤더와 요약 카드가 가로로 넘치지 않도록 결과 UI 전용 반응형 기준을 보강했다.
- 문제 데이터, 정답, `videoId`, Firebase 저장/제출 흐름, 빠른 문항 이동 문제풀이 UI는 변경하지 않았다.
- OneDrive 원본 관리 폴더의 모의고사 1~10회 `02. 개선판 (完)` 데스크톱/모바일 최종 HTML 20개에도 동일 변경을 반영했다.
  - `-DESKTOP-N9UCJ8E` 충돌 파일은 수정하지 않았다.
  - 로컬 `mock-exam/desktop-01.html`~`desktop-10.html`, `mock-exam/mobile-01.html`~`mobile-10.html`과 OneDrive 대응 파일 20개 SHA-256 해시 일치를 확인했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - `node tools/result-page-check.mjs`로 전체 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합을 검사했고 통과했다.
  - 모의고사 1~10회와 1400題 1~4부 데스크톱/모바일 HTML 28개 인라인 스크립트 문법 파싱 통과.
  - 모의고사 20개 파일의 결과 UI 한글 문구가 `?`로 깨지지 않고 `학습 결과`, `오답 중심 복습`, `틀린 문항`, `정답률`, `선택`, `응시자`로 들어간 것을 확인했다.
  - 앱 내 브라우저는 로컬 `file://` HTML 접근이 정책상 차단되어 우회하지 않았고, 결과 페이지 전용 자동 검증 스크립트와 정적 검증을 기준으로 확인했다.

## 24. 2026-06-12 모의고사 제출 후 상위 페이지 예전 결과 화면 노출 방지

- 사용자 확인 화면에서 여전히 `시험 결과`, `학습 요약`, `C4 / M3` 구조가 보이는 문제를 재확인했다.
  - GitHub Pages의 직접 `mock-exam/desktop-01.html`에는 새 `학습 결과`, `오답 중심 복습` UI가 배포되어 있었다.
  - 실제 사용 흐름에서는 모의고사 제출 후 `window.opener.postMessage(...)` 다음 `window.close()`가 실행되어, 상위 페이지의 예전 `examResult` 결과 렌더러가 보일 수 있음을 확인했다.
- 모의고사 1~10회 데스크톱/모바일 제출 흐름을 1400題처럼 현재 시험 창 안에서 결과를 계속 보여 주도록 조정했다.
  - `window.close()` 호출을 제거했다.
  - 새 `renderResultNav()`가 결과 요약을 그리기 전에 예전 `총점: ...` 텍스트를 먼저 넣던 코드를 제거했다.
  - 기존 `postMessage` 전송은 상위 페이지 연동 가능성을 위해 유지했다.
- 문제 데이터, 정답, `videoId`, Firebase 저장, Google Forms 제출, `postMessage` 데이터 전송 자체는 변경하지 않았다.
- OneDrive 원본 관리 폴더의 모의고사 1~10회 `02. 개선판 (完)` 데스크톱/모바일 최종 HTML 20개에도 동일 변경을 반영했다.
  - `-DESKTOP-N9UCJ8E` 충돌 파일은 수정하지 않았다.
  - 로컬 모의고사 HTML 20개와 OneDrive 대응 파일 20개 SHA-256 해시 일치를 확인했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - `node tools/result-page-check.mjs`로 전체 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합을 검사했고 통과했다.
  - 모의고사 1~10회와 1400題 1~4부 데스크톱/모바일 HTML 28개 인라인 스크립트 문법 파싱 통과.
  - 정적 검사로 모의고사 20개 파일에 `window.close()`와 예전 `scoreSpan.textContent` 총점 표시가 남아 있지 않음을 확인했다.
  - 로컬 Chrome + Playwright로 `mock-exam/desktop-01.html`을 제출 상태로 재현했고, 제출 후 페이지가 닫히지 않으며 `1회 모의고사 학습 결과`, `오답 중심 복습`, `틀린 문항`, `정답 n번 / 선택 n번`이 표시됨을 확인했다.

## 25. 2026-06-12 결과 복습 박스 폭 통일 및 단일 오답 카드 폭 제한

- 사용자 확인 화면에서 1400題 결과 화면은 요약 카드와 오답 복습 박스가 같은 폭으로 보이지만, 모의고사는 하단 오답 복습 박스만 좁게 표시되는 문제를 확인했다.
  - 모의고사 1~10회 데스크톱/모바일에 남아 있던 예전 결과 CSS의 `max-width: 760px` 제한이 새 결과 UI를 덮어쓰고 있었다.
  - 모의고사 결과 복습 박스를 1400題와 같은 `1120px` 기준 폭으로 맞췄다.
- 오답이 1문항뿐일 때 카드가 한 줄 전체로 과하게 늘어나는 문제를 줄였다.
  - 모의고사 1~10회와 1400題 1~4부 데스크톱/모바일 전체에 `result-review-single` 규칙을 추가했다.
  - 오답이 1개일 때는 카드 폭을 약 360px로 제한하고, 모바일 폭에서는 1열 전체 폭을 사용하도록 했다.
  - 오답이 여러 개일 때의 기존 그리드 배치는 유지했다.
- 1400題와 모의고사의 결과 복습 박스, 틀린 문항 카드, `정답 n번 / 선택 n번` 표기 규칙을 같은 기준으로 맞췄다.
- 문제 데이터, 정답, `videoId`, Firebase 저장, Google Forms 제출, `postMessage` 데이터 전송 흐름은 변경하지 않았다.
- OneDrive 원본 관리 폴더에도 동일 변경을 반영했다.
  - 모의고사 1~10회 `02. 개선판 (完)` 데스크톱/모바일 최종 HTML 20개를 동기화했다.
  - 1400題 1~4부 `02. 개선판` 데스크톱/모바일 최종 HTML 8개를 동기화했다.
  - 총 28개 OneDrive 대응 파일과 로컬 파일의 SHA-256 해시 일치를 확인했다.
- 검증:
  - `git diff --check` 통과.
  - `node tools/result-page-check.mjs`로 전체 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합을 검사했고 통과했다.
  - 모의고사 1~10회와 1400題 1~4부 데스크톱/모바일 HTML 28개 인라인 스크립트 문법 파싱 통과.
  - 로컬 Chrome + Playwright로 `mock-exam/desktop-01.html` 결과 화면을 2560x720에서 재현했고, 요약 카드와 오답 복습 박스가 모두 1120px, 단일 오답 카드가 360px로 계산됨을 확인했다.
  - 로컬 Chrome + Playwright로 `quiz-1400/desktop-01.html`의 결과 복습 DOM에 단일 오답 카드 규칙을 적용해 1120px 박스 안에서 카드가 360px로 계산됨을 확인했다.

## 26. 2026-06-12 모의고사 시작 시 새 창 실행 복구

- 사용자 확인에 따라 1~10회 모의고사에서 아이디/이름 입력 후 GitHub Pages 시험 창이 새로 뜨지 않고, 이후 `다시 풀기` 버튼을 눌러야 시험 화면이 보이는 흐름을 점검했다.
  - 모의고사 데스크톱/모바일 HTML 20개에는 `window.open()` 흐름이 없고, 숨겨진 `start-quiz-btn` 호출 시 현재 창에서 바로 `startQuiz(uid, name)`만 실행하고 있음을 확인했다.
  - `uid/name` 쿼리값이 이미 붙어 새 창으로 열린 경우에는 기존처럼 자동으로 `startQuiz()`를 실행하도록 유지했다.
  - `uid/name` 쿼리값이 없는 상태에서 시작 버튼이 호출되는 경우에는 현재 모의고사 URL에 `uid/name`을 붙여 `_blank` 새 창으로 열도록 `openQuizLaunchWindow()` 흐름을 추가했다.
  - 브라우저 팝업 차단으로 새 창이 열리지 않는 경우에는 팝업 차단 해제 안내를 표시하도록 했다.
- 문제 데이터, 정답, `videoId`, Firebase 저장/제출, 결과 UI, 빠른 문항 이동 UI는 변경하지 않았다.
- OneDrive 원본 관리 폴더의 모의고사 1~10회 `02. 개선판 (完)` 데스크톱/모바일 최종 HTML 20개에도 동일 변경을 반영했다.
  - `-DESKTOP-N9UCJ8E` 충돌 파일은 수정하지 않았다.
  - 로컬 모의고사 HTML 20개와 OneDrive 대응 파일 20개 SHA-256 해시 일치를 확인했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - `node tools/result-page-check.mjs`로 전체 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합을 검사했고 통과했다.
  - 모의고사 1~10회 데스크톱/모바일 HTML 20개 인라인 스크립트 문법 파싱 통과.
  - 정적 검사로 모의고사 20개 파일 모두 `openQuizLaunchWindow(uid,name)` 시작 버튼 연결, `window.open(buildQuizLaunchUrl(...), "_blank")`, 쿼리값 자동 시작 `startQuiz(uid,name)`이 각각 1회씩 존재함을 확인했다.
  - 앱 내 브라우저는 `file://` 직접 접근이 정책상 차단되어, 로컬 HTTP 서버(`127.0.0.1:8765`)로 `mock-exam/desktop-01.html?uid=...&name=...`를 열어 `quiz-page`가 표시되고 `uid/name`이 반영되는 것을 확인했다.

## 27. 2026-06-12 모의고사 본창 유지 + 새 창 결과 표시 보정

- 사용자 재설명에 따라 원하는 흐름을 다시 정리했다.
  - 올리사이트 본 창은 아이디/이름 입력 화면을 그대로 유지한다.
  - 문제풀이와 학습 결과는 새 GitHub Pages 창에서만 표시한다.
  - 기존 결과가 있어도 본 창에서 결과 화면으로 바꾸지 않고, 새 창을 열어 GitHub Pages 결과/풀이 화면을 보여 준다.
- OneDrive 원본 관리 폴더의 모의고사 1~10회 `1. ★★★ n회 모의고사 로그인 코드.html`을 보정했다.
  - `olGo()`에서 Firebase 기존 결과 조회 후 본 창 결과 화면을 표시하던 흐름을 제거했다.
  - 버튼 클릭 즉시 `olGetExamUrl(uid, uname)`을 `_blank` 새 창으로 열도록 변경해 팝업 차단 가능성을 줄였다.
  - 새 창에서 오는 `examResult` 메시지는 더 이상 `olShowResult(e.data)`로 본 창을 바꾸지 않도록 했다.
- GitHub Pages용 모의고사 1~10회 데스크톱/모바일 HTML 20개에서도 `window.opener.postMessage(resultData, "*")` 결과 전달을 제거했다.
  - 새 창 안에서 문제풀이와 학습 결과가 계속 표시되는 흐름은 유지했다.
  - Firebase 저장, Google Forms 제출, 문제 데이터, 정답, 빠른 문항 이동, 결과 UI는 변경하지 않았다.
- OneDrive 원본 관리 폴더의 모의고사 1~10회 `02. 개선판 (完)` 데스크톱/모바일 최종 HTML 20개에도 동일 변경을 반영했다.
  - 로컬 모의고사 HTML 20개와 OneDrive 대응 파일 20개 SHA-256 해시 일치를 확인했다.
  - `-DESKTOP-N9UCJ8E` 충돌 파일은 수정하지 않았다.
- 검증:
  - `git diff --check` 통과.
  - `node tools/result-page-check.mjs`로 전체 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합을 검사했고 통과했다.
  - 모의고사 1~10회 데스크톱/모바일 HTML 20개 인라인 스크립트 문법 파싱 통과.
  - OneDrive 모의고사 로그인 코드 10개 인라인 스크립트 문법 파싱 통과.
  - 정적 검사로 로그인 코드 10개에서 `olGo()`가 새 창을 직접 열고, `fetch(dbURL)` 기존 결과 조회와 `olShowResult(e.data)` 본창 결과 표시가 남아 있지 않음을 확인했다.
  - 정적 검사로 GitHub Pages용 모의고사 20개 파일에 `postMessage(resultData)` 결과 전달이 남아 있지 않음을 확인했다.

## 28. 2026-06-12 모의고사 모바일 결과 헤더와 기준 문구 보정

- 사용자 확인 화면 기준으로 모의고사 모바일 학습 결과 UI가 1400題 모바일 결과 UI와 다르게 보이는 부분을 보정했다.
  - 모의고사 모바일 1~10회 결과 헤더에서 `응시자` 문구를 강제로 다음 줄 전체 폭으로 보내던 모바일 CSS 규칙을 제거했다.
  - 1400題 모바일처럼 배지, `학습 결과`, 응시자명이 가능한 한 같은 헤더 흐름에서 자연스럽게 배치되도록 했다.
  - `필기 n / 35 · 실기 n / 45`와 `합격 기준 필기 21문항 이상 · 실기 27문항 이상`을 별도 줄로 나누어 표시하도록 결과 기준 문구를 구조화했다.
- 문제 데이터, 정답, Firebase 저장/제출, 빠른 문항 이동, 데스크톱 결과 UI는 변경하지 않았다.
- OneDrive 원본 관리 폴더의 모의고사 1~10회 `02. 개선판 (完)` 모바일 최종 HTML 10개에도 동일 변경을 반영했다.
  - 로컬 `mock-exam/mobile-01.html`~`mobile-10.html`과 OneDrive 대응 파일 10개 SHA-256 해시 일치를 확인했다.
  - `-DESKTOP-N9UCJ8E` 충돌 파일은 수정하지 않았다.
- 검증:
  - `git diff --check` 통과.
  - `node tools/result-page-check.mjs`로 전체 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합을 검사했고 통과했다.
  - 모의고사 모바일 10개 HTML 인라인 스크립트 문법 파싱 통과.
  - 정적 검사로 모의고사 모바일 10개 파일 모두 `result-standard-line` 2줄 기준 문구가 들어가고, `.result-user-name { width:100%; ... }` 강제 줄바꿈 규칙이 남아 있지 않음을 확인했다.
## 29. 2026-06-12 1회 모의고사 학습 완료 안내 샘플 적용

- 1회 모의고사만 대상으로, GitHub Pages 문제풀이 창과 올리사이트 로그인 코드 사이에 학습 완료 안내 흐름을 샘플로 추가했다.
  - `mock-exam/desktop-01.html`, `mock-exam/mobile-01.html`의 `submitQuiz()`에서 결과 화면을 표시한 뒤 `mockExamComplete` 완료 신호만 opener로 보내도록 했다.
  - 점수, 정답, 선택 답안 같은 결과 데이터는 opener로 보내지 않고, 완료 여부와 1회 모의고사 식별 정보만 전달한다.
  - OneDrive 1회 로그인 코드에는 `ol_done_notice` 안내 영역과 `mockExamComplete` 수신 처리를 추가했다.
  - 올리사이트 본 창은 아이디/이름 입력 화면을 유지하고, 완료 신호를 받으면 "문제풀이가 완료되었습니다. 올리사이트의 학습 완료하기 버튼을 눌러주세요." 안내만 표시한다.
  - 학습 완료하기 버튼 자동 클릭은 적용하지 않았다.
- OneDrive 원본 관리 폴더 동기화:
  - 1회 모의고사 데스크톱 최종 코드와 `mock-exam/desktop-01.html`의 SHA-256 일치를 확인했다.
  - 1회 모의고사 모바일 최종 코드와 `mock-exam/mobile-01.html`의 SHA-256 일치를 확인했다.
  - 1회 모의고사 로그인 코드는 OneDrive 최종 코드 파일에 직접 반영했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - `node tools/result-page-check.mjs` 통과: 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합.
  - 1회 데스크톱/모바일 HTML과 OneDrive 1회 로그인 코드의 인라인 스크립트 문법 검사 통과.
  - 로컬 HTTP 미리보기에서 `mockExamComplete` 메시지 수신 후 완료 안내가 `display: block`으로 표시됨을 확인했다.

## 30. 2026-06-18 모의고사 불합격 시 올리사이트 입장 차단 문구 노출 방지

- `WORKLOG.md` 기준 마지막 작업이 2026-06-12 `1회 모의고사 학습 완료 안내 샘플 적용`이었음을 확인했다.
- 사용자 확인에 따라 문제를 끝까지 풀고 불합격 또는 학습 완료 조건 미충족으로 판정된 경우, 올리사이트 본창에 `입장할 수 없습니다`를 표시하는 것은 문맥상 맞지 않다고 판단했다.
- OneDrive 원본 관리 폴더의 모의고사 1~10회 `1. ★★★ n회 모의고사 로그인 코드.html` 10개를 보정했다.
  - `olShowIncompleteNotice()`가 무조건 `olShowScopeDeniedNotice()`를 호출하던 흐름을 수정해, 실제 `scope-denied` 메시지가 있을 때만 입장 차단 화면을 사용하도록 했다.
  - 일반 미완료/불합격 상태는 `ol_done_notice` 안내 박스로 표시되도록 유지했다.
  - 안내 문구를 `학습 완료 조건을 충족하지 못했습니다. 모든 문항을 풀고 합격 기준을 충족해야 완료 안내가 표시됩니다. 불합격으로 표시된 경우 결과 화면에서 오답을 복습한 뒤 다시 풀어 주세요.`로 바꿨다.
  - 진행 중 안내도 `합격하면` 대신 `합격 기준을 충족하면`으로 정리했다.
- 1400題 로그인 코드는 확인 결과 미완료/불합격 흐름에서 별도 진행/조건 미충족 안내를 사용하고 있어, 이번 변경에서는 수정하지 않았다.
- 로컬 `mock-exam`/`quiz-1400` 문제풀이 HTML, 문제 데이터, 정답, Firebase 저장/제출, GitHub Pages 결과 UI는 변경하지 않았다.
- 검증:
  - OneDrive 모의고사 로그인 코드 10개에서 기존 `if (olShowScopeDeniedNotice()) return;` 호출이 0개가 되었음을 확인했다.
  - 새 `olScopeDeniedMessage` 조건부 호출과 새 미완료/불합격 안내 문구가 각각 10개 들어간 것을 확인했다.
  - OneDrive 모의고사 로그인 코드 10개 인라인 스크립트 문법 파싱 통과.

## 31. 2026-06-18 1회 모의고사 토론방 패널 compact 추가 축소 및 빠른 복구 보정

- 사용자 확인 화면 기준으로 1회 모의고사 로그인 코드의 토론방 패널 열림/닫힘 상태만 보정했다.
  - 기본 화면은 패널이 닫힌 상태에서 기존처럼 큰 안내 카드로 표시되도록 유지했다.
  - 토론방 패널이 열려 `ol-wrap` 폭이 좁아진 상태에서만 compact 안내 카드가 적용되도록 유지했다.
  - compact 카드 폭을 `min(100%,660px)`, 안내 박스 폭을 `min(100%,510px)`로 더 줄여 패널 열림 상태에서 하단 안내 박스가 잘리지 않도록 했다.
  - compact 상태의 제목, 배지, 안쪽 여백을 소폭 줄여 16:9 카드 안에서 안정적으로 들어가도록 했다.
  - 패널 닫힘 후 원래 큰 카드로 더 빨리 복구되도록 `ResizeObserver`, 100ms 안정화 감시, focus 시 `olResize()` 호출을 추가했다.
- 로컬 `mock-exam`/`quiz-1400` 문제풀이 HTML, 문제 데이터, 정답, Firebase 저장/제출, GitHub Pages 결과 UI는 변경하지 않았다.
- OneDrive 원본 관리 폴더의 1회 모의고사 로그인 최종 코드 파일에 직접 반영했다.
  - 파일: `01. 합격!! 모의고사/01회 모의고사/02. 개선판 (完)/02. (★삭제 주의) 최종 코드/1. ★★★ 1회 모의고사 로그인 코드.html`
  - SHA-256: `C15156AAD28F06B4DD420BE0A409052116959C3C6F2B7C95D7D7022BAC0C0BB5`
- 검증:
  - OneDrive 1회 모의고사 로그인 코드 인라인 스크립트 문법 파싱 통과.
  - Edge headless 검증에서 토론방 패널 열림 가정 폭 `780x450` 기준 compact 카드 `660x371`, 안내 박스 `510px`로 잘리지 않음을 확인했다.
  - 패널 닫힘 가정 폭 `1150x780` 변경 후 약 140ms 이내 기본 큰 카드 `1150x647`로 복구됨을 확인했다.

## 32. 2026-06-18 1회 모의고사 토론방 패널 compact 텍스트 크기 보정

- 사용자 확인 화면 기준으로 토론방 패널 열림 상태에서 카드 크기는 줄었지만 안내 문구 글자 크기가 그대로라 하단이 잘리는 문제를 보정했다.
  - compact 상태에서만 배지 글자 크기를 `20px`, 제목 글자 크기를 `38px`로 줄였다.
  - `ol_done_notice` 내부 안내 문구가 부모 박스 크기와 함께 줄어들도록 `--ol-notice-main-size`, `--ol-notice-detail-size`, `--ol-notice-line-height` 변수를 적용했다.
  - compact 상태의 안내 큰 문구는 `25px`, 보조 문구는 `19px`, 줄간격은 `1.34`로 낮췄다.
  - 패널 닫힘 상태에서는 기존 큰 화면용 제목, 안내 문구 크기로 복구되도록 유지했다.
- 로컬 `mock-exam`/`quiz-1400` 문제풀이 HTML, 문제 데이터, 정답, Firebase 저장/제출, GitHub Pages 결과 UI는 변경하지 않았다.
- OneDrive 원본 관리 폴더의 1회 모의고사 로그인 최종 코드 파일에 직접 반영했다.
  - 파일: `01. 합격!! 모의고사/01회 모의고사/02. 개선판 (完)/02. (★삭제 주의) 최종 코드/1. ★★★ 1회 모의고사 로그인 코드.html`
  - SHA-256: `6BC763AD31195E44B375330C5DE688F4CDCCD5672B60522552619171712D1000`
- 검증:
  - OneDrive 1회 모의고사 로그인 코드 인라인 스크립트 문법 파싱 통과.
  - Edge headless 검증에서 compact 폭 `780px` 기준 카드 `660x371`, 안내 박스 `510x95`, 제목 `38px`, 안내 큰 문구 `25px`, 보조 문구 `19px`로 카드 안에 들어감을 확인했다.
  - 패널 닫힘 가정 폭 `1150px` 변경 후 약 140ms 이내 기본 큰 카드 `1150x647`, 제목 `62px`, 안내 큰 문구 `34px`로 복구됨을 확인했다.

## 33. 2026-06-18 1회 모의고사 모바일 결과 화면 이전/다음 버튼 숨김

- 사용자 요청에 따라 1회 모의고사 모바일 학습결과 화면에서 하단 `이전`/`다음` 버튼이 남아 보이는 문제만 보정했다.
  - 저장된 결과를 다시 여는 경로와 제출 직후 결과 화면으로 전환하는 경로에서 `quizPage`를 `display:none!important`로 확실히 숨기도록 했다.
  - 데스크톱 파일, 결과 UI 구성, 점수 계산, Firebase 저장/제출, 문제 데이터, 정답, 다시 풀기/학습 완료 버튼은 변경하지 않았다.
- OneDrive 원본 관리 폴더의 1회 모의고사 모바일 최종 코드 파일에도 동일 변경을 반영했다.
  - 파일: `01. 합격!! 모의고사/01회 모의고사/02. 개선판 (完)/02. (★삭제 주의) 최종 코드/2-2. ★★★ 1회 모의고사 코드 - 모바일용.html`
  - 로컬 `mock-exam/mobile-01.html`과 OneDrive 대응 파일 SHA-256 일치: `0AE032C08E9C66296FEA56BEB70DDEA06C8DC4DE8769A268D0FF52F6FD094012`
- 검증:
  - Git diff 기준 `mock-exam/mobile-01.html`의 결과 화면 전환 2줄만 변경됨을 확인했다.
  - 로컬/OneDrive 1회 모바일 HTML 인라인 스크립트 문법 파싱 통과.
  - Edge headless 검증에서 결과 상태는 `quizDisplay:none`, `navRendered:false`이고, 퀴즈 상태로 되돌리면 `navRendered:true`로 복구됨을 확인했다.

## 34. 2026-06-18 모의고사 태블릿 문항판과 로그인 안내 표시 보정

- 사용자 요청 세 가지에 한정해 모의고사 모바일 코드와 OneDrive 로그인 최종 코드만 보정했다.
  - `mock-exam/mobile-01.html`~`mock-exam/mobile-10.html`의 태블릿 가로 문항판에서 모의고사는 80문항 전체가 한 화면에 나오도록 `getNavPageSize()`를 추가했다.
  - 1400제처럼 40문항씩 나뉘는 흐름은 모의고사 태블릿 가로 화면에는 적용하지 않고, 휴대폰/비태블릿 문항판은 기존 40문항 페이지 흐름을 유지했다.
  - OneDrive 모의고사 1~10회 로그인 최종 코드의 입장 차단 안내 문구를 `입력하신 정보는 해당 모의고사에 등록되어 있지 않습니다.` / `관리자에게 문의해 주세요.` 두 줄로 보이도록 `white-space:pre-line`과 줄바꿈 문자를 반영했다.
  - 1회 모의고사 로그인 코드의 기본 학습 완료 안내 카드만 `min-height`를 `400px`에서 `430px`로, 패딩을 `36px`에서 `38px`로 소폭 늘려 세로로 눌려 보이는 느낌을 줄였다.
- 1400제, 데스크톱 문제풀이, 문제 데이터, 정답, Firebase 저장/제출, 결과 화면 로직은 변경하지 않았다.
- OneDrive 원본 관리 폴더의 모의고사 1~10회 모바일 최종 코드 파일에 로컬 모바일 파일과 동일한 내용을 동기화했다.
- 검증:
  - `git diff --check` 통과.
  - 로컬 `mock-exam/mobile-01.html`~`mock-exam/mobile-10.html`과 OneDrive 대응 모바일 최종 코드 SHA-256 일치 확인.
  - Edge headless 태블릿 가로 검증에서 1회 모바일 문항판이 `pageSize:80`, 버튼 `1`~`80`, 좌우 화살표 `display:none`, 20열 렌더링으로 확인됐다.
  - 로컬 모바일 HTML 10개와 OneDrive 로그인 HTML 10개 인라인 스크립트 문법 파싱 통과.
  - OneDrive 로그인 HTML 10개에서 안내 문구 줄바꿈, `white-space:pre-line`, 1회 카드 `min-height:430px` 반영을 확인했다.

## 35. 2026-06-19 1회 모의고사 로그인 안내 패널 디자인 통일

- 사용자 확인 화면 기준으로 1회 모의고사 로그인 코드의 안내 UI만 3회 모의고사 `입장할 수 없습니다` 패널 디자인에 맞췄다.
  - `아직 학습 완료 전입니다` 안내가 기존 초록/주황 `ol_done_notice` 박스가 아니라 `ol_denied` 패널 구조를 사용하도록 바꿨다.
  - `입장할 수 없습니다` 안내는 기존 3회와 같은 제목, 안내문, `다시 입력하기` 버튼 구조를 유지하도록 공통 표시 함수를 사용하게 했다.
  - 1회 전용 `stable/compact` 레이아웃 분기는 우회해 토론방 패널 폭이 좁아질 때 3회처럼 일반 축소 흐름을 타도록 했다.
- OneDrive 원본 관리 폴더의 1회 모의고사 로그인 최종 코드 파일에만 반영했다.
  - 파일: `01. 합격!! 모의고사/01회 모의고사/02. 개선판 (完)/02. (★삭제 주의) 최종 코드/1. ★★★ 1회 모의고사 로그인 코드.html`
  - SHA-256: `E44AE8098F7596B7BFDB90398065206B1833A0B8EEE45419653C38F11E9A667A`
- 검증:
  - OneDrive 1회 모의고사 로그인 코드 인라인 스크립트 문법 파싱 통과.
  - Edge headless 태블릿 조건에서 1회 `입장할 수 없습니다` 패널이 3회 `입장할 수 없습니다` 패널과 동일한 렌더링 수치로 확인됐다.
  - 토론방 패널 폭 가정 `520px`에서도 1회 `입장할 수 없습니다`와 3회 `입장할 수 없습니다`가 카드, 패널, 패딩, 버튼 표시 기준으로 일치함을 확인했다.
  - 1회 `아직 학습 완료 전입니다` 안내도 `ol_denied` 패널 구조를 사용하고 기존 `ol_done_notice`는 숨김 상태임을 확인했다.

## 36. 2026-06-19 1회 모의고사 로그인 안내 패널 높이 보정

- 사용자 확인 화면 기준으로 1회 모의고사 로그인 코드의 `아직 학습 완료 전입니다` 안내 패널 높이가 3회 모의고사 `입장할 수 없습니다` 패널보다 낮게 보이는 문제만 보정했다.
  - `아직 학습 완료 전입니다` 안내에서도 `다시 입력하기` 버튼 영역을 유지하도록 했다.
  - `ol_denied_msg`에 두 줄 안내문 높이 기준을 예약해 한 줄 안내문이어도 3회 `입장할 수 없습니다` 패널과 같은 카드 높이 흐름을 타도록 했다.
- OneDrive 원본 관리 폴더의 1회 모의고사 로그인 최종 코드 파일에만 반영했다.
  - 파일: `01. 합격!! 모의고사/01회 모의고사/02. 개선판 (完)/02. (★삭제 주의) 최종 코드/1. ★★★ 1회 모의고사 로그인 코드.html`
  - SHA-256: `A51E3A4DD2A7C8DF72A4DD41C590288BEA2ECF6574E3E0901A1A56FCC10A8E70`
- 검증:
  - OneDrive 1회 모의고사 로그인 코드 인라인 스크립트 문법 파싱 통과.
  - Edge headless 태블릿 조건에서 1회 `아직 학습 완료 전입니다`, 1회 `입장할 수 없습니다`, 3회 `입장할 수 없습니다`가 `1125px` 기준 카드 `1125x427`, 안내 패널 `720x255`로 일치함을 확인했다.
  - 토론방 패널 폭 가정 `520px`에서도 세 안내 상태가 카드 `520x345`, 안내 패널 `478x221`로 일치함을 확인했다.

## 37. 2026-06-19 1회 모의고사 학습 완료 전 안내 버튼 제거와 강조 보정

- 사용자 확인 화면 기준으로 1회 모의고사 로그인 코드의 `아직 학습 완료 전입니다` 안내에서 `다시 입력하기` 버튼이 보이는 어색함만 보정했다.
  - 해당 상태에서는 `다시 입력하기` 버튼을 숨기고, 흰 안내 패널 자체에 3회 `입장할 수 없습니다` 기준 최소 높이를 유지하도록 했다.
  - 문구 묶음은 패널 중앙에 배치하고 글자 크기를 키웠으며, `학습 완료`만 굵은 주황색으로 강조했다.
  - 일반 `입장할 수 없습니다` 상태는 기존처럼 `다시 입력하기` 버튼이 보이도록 유지했다.
- OneDrive 원본 관리 폴더의 1회 모의고사 로그인 최종 코드 파일에만 반영했다.
  - 파일: `01. 합격!! 모의고사/01회 모의고사/02. 개선판 (完)/02. (★삭제 주의) 최종 코드/1. ★★★ 1회 모의고사 로그인 코드.html`
  - SHA-256: `E20E9C5E476EE4DD5D89793785F51ED1B6A20B17DA25CD3A1EC688E5E19D7965`
- 검증:
  - OneDrive 1회 모의고사 로그인 코드 인라인 스크립트 문법 파싱 통과.
  - Edge headless 태블릿 조건에서 `아직 학습 완료 전입니다` 상태의 버튼이 `display:none`이고 `학습 완료` 강조 색상이 `rgb(249, 115, 22)`임을 확인했다.
  - `1125px` 기준 1회 `아직 학습 완료 전입니다`, 1회 `입장할 수 없습니다`, 3회 `입장할 수 없습니다`가 카드 `1125x427`, 안내 패널 `720x255`로 일치함을 확인했다.
  - 토론방 패널 폭 가정 `520px`에서도 세 상태가 카드 `520x345`, 안내 패널 `478x221`로 일치함을 확인했다.

## 38. 2026-06-19 1회 모의고사 스마트폰 로그인 안내 줄바꿈 보정

- 사용자 확인 화면 기준으로 1회 모의고사 로그인 코드의 스마트폰 폭 표시만 보정했다.
  - 스마트폰 폭에서 제목이 `청각장애인 요양보호사` / `자격시험 대비` 두 줄로 보이도록 했다.
  - `아직 학습 완료 전입니다` 안내는 스마트폰 폭에서 `아직 학습 완료` / `전입니다.` 두 줄로 보이고, `학습 완료` 강조는 유지했다.
  - 보조 문구는 스마트폰 폭에서 `불합격이면 복습 후` / `다시 풀어주세요.` 두 줄로 보이도록 했다.
  - 태블릿/데스크톱 폭에서는 기존 한 줄 문구 흐름을 유지했다.
- OneDrive 원본 관리 폴더의 1회 모의고사 로그인 최종 코드 파일에만 반영했다.
  - 파일: `01. 합격!! 모의고사/01회 모의고사/02. 개선판 (完)/02. (★삭제 주의) 최종 코드/1. ★★★ 1회 모의고사 로그인 코드.html`
  - SHA-256: `93899F95643F2CA92B7C5FBA36B6E58D9B723416899AF87DC8BE6EF6353F7FB3`
- 검증:
  - OneDrive 1회 모의고사 로그인 코드 인라인 스크립트 문법 파싱 통과.
  - Edge headless 스마트폰 폭 `390px`에서 요청한 세 줄바꿈이 모두 적용되고, `다시 입력하기` 버튼은 `display:none` 상태임을 확인했다.
  - 스마트폰 폭에서 카드 `390x513`, 안내 패널 `342x263`으로 잘림 없이 렌더링됨을 확인했다.

## 39. 2026-06-19 2~10회 모의고사 로그인 UI 1회 기준 통일

- 사용자 요청에 따라 1회 모의고사 로그인 UI 디자인을 기준으로 2~10회 모의고사 로그인 최종 코드에 동일하게 적용했다.
  - 파란 로그인 카드 구조, `ol_denied` 안내 패널, `아직 학습 완료 전입니다` 중앙 정렬 안내, `학습 완료` 주황색 강조, 스마트폰 줄바꿈, 토론방 패널 폭 대응 흐름을 1회와 동일하게 맞췄다.
  - 회차별 연결값은 각 회차에 맞게 유지했다: `2~10회 모의고사`, `exam2`~`exam10`, `mobile-02.html`~`mobile-10.html`, `desktop-02.html`~`desktop-10.html`.
  - 2~10회 파일을 회차별 값만 1회 값으로 정규화했을 때 1회 파일과 완전히 동일함을 확인했다.
- OneDrive 원본 관리 폴더의 2~10회 모의고사 로그인 최종 코드 파일에 반영했다.
  - 파일: `01. 합격!! 모의고사/02회 모의고사`~`10회 모의고사/.../02. (★삭제 주의) 최종 코드/1. ★★★ N회 모의고사 로그인 코드.html`
- 검증:
  - OneDrive 1~10회 모의고사 로그인 코드 인라인 스크립트 문법 파싱 통과.
  - 1~10회 모두 배지, 결과 배지, `examId`, 모바일/데스크톱 파일 연결값이 각 회차와 일치하고 다른 회차 `examId`가 섞이지 않음을 확인했다.
  - Edge headless 스마트폰 폭 `390px`에서 1~10회 모두 제목 `청각장애인 요양보호사` / `자격시험 대비`, 안내 `아직 학습 완료` / `전입니다.`, 보조 문구 `불합격이면 복습 후` / `다시 풀어주세요.` 줄바꿈이 동일하게 적용됨을 확인했다.
  - 스마트폰 폭에서 1~10회 모두 카드 `390x513`, 안내 패널 `342x263`, `다시 입력하기` 버튼 `display:none` 상태로 동일하게 렌더링됨을 확인했다.
