# WORKLOG

## 35. 2026-06-14 1회 모의고사 결과 화면 학습 완료 버튼 및 올리사이트 완료 안내 보정

- 사용자 요청에 따라 1회 모의고사 결과 화면에서 `회 선택하기` 버튼을 제거하고, `다시 풀기` 아래에 `학습 완료` 버튼을 추가했다.
- `mock-exam/desktop-01.html`과 `mock-exam/mobile-01.html` 모두 `학습 완료` 버튼 클릭 시 올리사이트 본 창을 focus한 뒤 GitHub Pages 문제풀이/결과 창을 닫도록 했다.
- 창이 닫히지 않는 환경에서는 안내 문구를 표시하도록 했다.
  - 데스크톱: `↘`와 함께 오른쪽 아래의 `학습을 완료합니다` 버튼 안내
  - 모바일: `↓`와 함께 화면 아래의 `학습을 완료합니다` 버튼 안내
- OneDrive 1회 모의고사 로그인 코드 최종 파일의 완료 안내도 데스크톱/모바일에 맞춰 `↘`/`↓` 안내가 나오도록 수정했다.
- OneDrive 대응 파일 동기화:
  - `2-1. ★★★ 1회 모의고사 코드 - 데스크톱용.html`
  - `2-2. ★★★ 1회 모의고사 코드 - 모바일용.html`
  - 로컬 파일과 OneDrive 대응 파일의 SHA-256 해시 일치를 확인했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - 1회 데스크톱/모바일 코드와 1회 로그인 코드 인라인 스크립트 문법 검사 통과.
  - `node tools/result-page-check.mjs` 통과: 28개 파일, 196개 결과 화면 viewport 케이스 확인.
  - Chrome/Playwright에서 1회 데스크톱/모바일 결과 화면의 버튼 순서가 `다시 풀기`, `학습 완료`로 표시되고, `회 선택하기` 버튼이 사라졌음을 확인했다.
  - Chrome/Playwright에서 1회 로그인 코드 완료 안내가 데스크톱은 `↘`, 모바일은 `↓` 문구로 표시됨을 확인했다.
- 남은 주의사항:
  - 이번 작업은 사용자 요청대로 1회 모의고사 데스크톱/모바일 코드와 1회 로그인 코드만 수정했고, 2~10회는 수정하지 않았다.

## 34. 2026-06-14 1회 모의고사 로그인 코드 학습 결과 UI를 1400제 모바일 결과 UI 기준으로 보정

- 사용자 추가 설명에 따라 대상 범위를 1회 모의고사 `로그인 코드` 안의 `학습 결과 UI 디자인`으로 재확정했다.
- 기준 파일을 다시 확인해 `1400題 4부 로그인 코드 - 모바일용.html`의 `학습 결과`, `오답 중심 복습`, `틀린 문항` 스크롤 목록 구조를 기준으로 삼았다.
- OneDrive 1회 모의고사 로그인 코드 최종 파일의 결과 화면을 다음 구조로 교체했다.
  - 상단: `1회 모의고사 / 학습 결과 / 응시자`
  - 요약 카드: 합격·불합격 상태, 정답률, 정답, 오답, 미응답, 필기·실기 점수와 합격 기준
  - 복습 카드: `오답 중심 복습` 아래 `틀린 문항` 목록만 내부 세로 스크롤
  - 하단 버튼: `회 선택하기`, `다시 풀기`
- 로그인 화면, 새 창 열기, 완료 안내, Firebase 완료 확인 흐름은 건드리지 않았다.
- 검증:
  - 1회 로그인 코드 인라인 스크립트 문법 검사 통과.
  - Chrome/Playwright 모바일 390x844 렌더링에서 `학습 결과` UI, `오답 중심 복습`, `틀린 문항 2문항`, `회 선택하기`/`다시 풀기` 버튼 표시 확인.
  - 오답 80개 케이스에서 `.result-review-list`만 `overflow-y:auto`로 실제 내부 스크롤되고, 바깥 복습 카드는 스크롤 박스로 변하지 않음을 확인.
- 남은 주의사항:
  - 이번 작업은 사용자 요청대로 1회 모의고사 로그인 코드만 수정했고, 2~10회 모의고사 로그인 코드는 수정하지 않았다.

## 33. 2026-06-14 1회 모의고사 모바일 결과 복습 박스 스크롤 보정

- 사용자 요청에 따라 1회 모의고사 모바일 학습 결과 화면만 대상으로 확인했다.
- `mock-exam/mobile-01.html`의 결과 화면에서 `#quick-nav-container-result`에 남아 있던 `overflow-x:hidden` 때문에 바깥 복습 카드가 세로 스크롤 영역처럼 계산되는 문제를 보정했다.
- 바깥 복습 카드는 1400제 결과 UI처럼 `overflow:visible`로 두고, 실제 상하 스크롤은 내부 `.result-review-list`의 `overflow-y:auto`에만 남도록 한 줄만 수정했다.
- OneDrive 대응 파일 `01회 모의고사/02. 개선판 (完)/02. (★삭제 주의) 최종 코드/2-2. ★★★ 1회 모의고사 코드 - 모바일용.html`에도 동일 변경을 반영했다.
- 로컬 파일과 OneDrive 대응 파일의 SHA-256 해시가 일치함을 확인했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - `node tools/result-page-check.mjs` 통과: 28개 파일, 196개 결과 화면 viewport 케이스 확인.
  - Chrome/Playwright 모바일 390x844 렌더링에서 바깥 복습 카드 `overflow-y: visible`, 내부 틀린 문항 목록 `overflow-y: auto` 확인.
- 남은 주의사항:
  - 이번 작업은 사용자 요청대로 1회 모의고사 모바일 결과 UI만 수정했고, 2~10회 모의고사 및 1400제 파일은 수정하지 않았다.

## 32. 2026-06-14 OneDrive 우선 확인 흐름을 AGENTS 고정 규칙으로 승격

- 사용자가 매번 요청하지 않아도 같은 방식으로 처리되도록 `AGENTS.md`에 고정 작업 규칙을 추가했다.
- `mock-exam` 또는 `quiz-1400` 코드 파일 수정 시 기본 순서를 다음으로 명시했다.
  1. 로컬 파일 수정
  2. 대응 OneDrive 최종 코드 파일 동기화/저장
  3. OneDrive 또는 로컬 미리보기 기준 확인
  4. 검증 실행
  5. `WORKLOG.md` 갱신
  6. Git 커밋
  7. GitHub `origin/main` 푸시
- 이 OneDrive 우선 확인 흐름과 자동 커밋/푸시 규칙은 사용자가 명시적으로 규칙 변경을 요청하지 않는 한 임의로 빼거나, 약화하거나, 삭제하지 않는다고 기록했다.

## 31. 2026-06-14 OneDrive 우선 확인 후 GitHub 확정 흐름

- 앞으로 `mock-exam` 또는 `quiz-1400` 코드 수정 시 기본 확인 순서를 다음처럼 운영한다.
  1. 로컬 저장소 파일을 수정한다.
  2. 대응되는 OneDrive 최종 코드 파일에 먼저 동기화한다.
  3. OneDrive/로컬 미리보기 기준으로 화면과 동작을 먼저 확인한다.
  4. 이상이 없으면 로컬 Git 저장소에 커밋한다.
  5. 마지막에 GitHub `origin/main`으로 푸시한다.
- 이유:
  - 다른 컴퓨터에서 작업할 때는 OneDrive가 실시간 동기화 기준이 되므로, GitHub Pages를 매번 열어 확인하는 것보다 빠르고 캐시 문제도 적다.
  - GitHub Pages 확인은 배포 후 최종 확인용으로만 쓰는 편이 토큰과 시간을 줄일 수 있다.
  - OneDrive 폴더 자체는 현재 `.git` 저장소가 아니므로 "OneDrive 커밋"이 아니라 "OneDrive 최종 코드 파일 동기화/저장"으로 표현한다.
- 단, GitHub는 최종 이력 관리와 배포 기준이므로 생략하지 않는다.
  - 작업 완료 전에는 OneDrive 동기화 확인, 검증 실행, `WORKLOG.md` 갱신, Git 커밋, GitHub 푸시까지 마무리한다.

## 30. 2026-06-12 모의고사 모바일 결과 UI 재정렬 및 완료 안내 보완

- 사용자 확인 화면에서 1400제 모바일 결과 UI와 모의고사 모바일 결과 UI가 다르게 보이는 문제를 다시 확인했다.
  - 모의고사 모바일 1~10회 결과 화면의 이전 오버라이드 일부가 남아 있어 1400제와 간격, 버튼 구조, 결과 카드 배치가 완전히 같지 않았다.
  - `mock-exam/mobile-01.html`~`mock-exam/mobile-10.html` 결과 UI 보정 블록을 1400제 모바일 결과 UI 기준으로 다시 맞췄다.
  - 결과 화면 하단에 1400제의 `장 선택하기` 버튼과 같은 위치/구조로 `회 선택하기` 버튼을 추가했다.
  - `회 선택하기`는 문제풀이 창에서 자동으로 올리사이트 본 창을 focus/close 시도하고, 닫히지 않으면 본 창에서 회차를 선택하라는 안내를 표시한다.
- 1회 모의고사 완료 안내가 실제 환경에서 뜨지 않는 문제를 보완했다.
  - OneDrive 1회 로그인 코드의 새 창 열기 순서를 `window.open()` 우선, 실패 시 `window.top.open()` fallback으로 조정했다.
  - `postMessage`가 환경에 따라 opener로 돌아오지 않을 수 있어, 1회 로그인 코드에 Firebase 결과 존재 여부만 확인하는 보조 완료 확인을 추가했다.
  - 본 창은 결과 페이지로 전환하지 않고, 결과 저장이 확인되면 "문제풀이가 완료되었습니다. 올리사이트의 학습 완료하기 버튼을 눌러주세요." 안내만 표시한다.
  - 학습 완료하기 자동 클릭은 적용하지 않았다.
- OneDrive 원본 관리 폴더 동기화:
  - 모의고사 1~10회 모바일 최종 코드 10개를 로컬 `mock-exam/mobile-01.html`~`mobile-10.html`과 동기화했고 SHA-256 일치를 확인했다.
  - 1회 모의고사 로그인 코드는 OneDrive 최종 코드 파일에 직접 반영했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - `node tools/result-page-check.mjs` 통과: 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합.
  - 모의고사 모바일 1~10회 HTML 인라인 스크립트 문법 검사 통과.
  - 1회 로그인 코드 인라인 스크립트 문법 검사 통과.
  - 로컬 모바일 미리보기에서 결과 화면이 `회 선택하기`/`다시 풀기` 2개 버튼, `오답 중심 복습`, 1400제형 결과 카드 폭으로 표시됨을 확인했다.
  - 로컬 완료 안내 보조 확인 미리보기에서 Firebase 결과 존재 응답 시 완료 안내가 `display: block`으로 표시됨을 확인했다.

## 1. 현재 프로젝트 목표

- 청각장애인 요양보호사 자격시험 대비 모의고사 웹 파일을 관리한다.
- A컴퓨터와 B컴퓨터에서 같은 GitHub 저장소를 기준으로 이어 작업할 수 있게 한다.
- 핵심 작업 대상은 `mock-exam`, `quiz-1400`, `vocabulary` 폴더이며, 문제/정답 데이터는 원본 PDF, 정답지, 검수 엑셀 기준으로만 수정한다.

## 2. 지금까지 완료한 작업

- `mock-exam` 1~10회 데스크톱용/모바일용 파일을 Git 저장소에 정리했다.
- 1회 모의고사에서 만든 모바일/태블릿 UI 개선 사항을 2~10회 모의고사에도 동일하게 반영했다.
- 스마트폰에서 내용이 넘칠 때만 문항 영역이 세로 스크롤되도록 조정했다.
- 태블릿 세로 화면에서는 남는 공간이 과도하지 않도록 문제 영역 크기를 자동 조절하도록 수정했다.
- 결과 페이지의 틀린 문항, 미응답 문항, 정답 문항 칩 목록을 화면 크기에 맞게 반응형으로 정리했다.
- 통합 엑셀과 PDF/정답지 기준으로 1~10회 총 800문항의 정답번호를 재검토했다.
- 엑셀 기준으로 발견된 정답번호 불일치 43건을 데스크톱용과 모바일용 모두 수정했다.
- 명확한 문구 오타 일부를 원문 확인 후 수정했다.
  - 예: `허루` -> `하루`
  - 예: `뇌졸중으로 뇌가 손상` -> `뇌졸중으로 소뇌가 손상`
  - 예: `영양주머니를 영양액을` -> `영양주머니에 영양액을`
  - 예: 3회 23번 중복 보기 수정
- `quiz-1400/login.html`을 추가했고, `quiz-1400/mobile-01.html`의 모바일 스크롤 관련 수정도 반영했다.
- `AGENTS.md`에 사용자가 "작업 종료 정리해 주세요"라고 요청할 때 수행할 작업 종료 규칙을 추가했다.
- 최상위 폴더를 번호 형식으로 정리했다.
  - `01. 청각장애인 요양보호사`
  - `02. 작업 목록`
- 실제 작업 대상 폴더 `mock-exam`, `quiz-1400`, `vocabulary`는 삭제하지 않고 `02. 작업 목록` 아래로 이동했다.
- 사용자가 `03. 기존 프로젝트 파일`은 불필요하다고 판단해 삭제했다.
- 앞으로 `02. 작업 목록/mock-exam` 또는 `02. 작업 목록/quiz-1400` 코드 파일을 수정할 때는 OneDrive의 원본 관리 폴더에도 동일 변경을 반영하기로 했다.
  - 기준 경로: `C:\Users\COM-JHYUTG\OneDrive\바탕 화면\01. 개인용\01. 자기계발\01. 코딩\00. 업무 관련 코딩\1. 청각장애인 요양보호사 양성`
  - 모의고사: `01. 합격!! 모의고사`
  - 1400제: `02. 합격!! 1400題`
- OneDrive의 `01회 모의고사/02. 개선판 (完)/02. (★삭제 주의) 최종 코드` 폴더를 검토했다. 파일은 삭제하지 않고, 유지/삭제 후보만 분류했다.
- OneDrive의 `01회 모의고사/02. 개선판 (完)/02. (★삭제 주의) 최종 코드` 폴더를 정리했다. 로컬 최신 데스크톱/모바일 파일을 OneDrive 주력 파일에 반영한 뒤, 삭제 후보 파일을 제거해 3개 파일만 남겼다.
- 사용자의 요청으로 대비용 파일 3개를 복구했다. 삭제 당시 원본은 휴지통/주변 폴더에서 찾지 못해, 로컬 최신 `desktop-01.html` 기준으로 `3. ★★★ 1회 모의고사 퀴즈 문제 데이터 코드.html`, `4. ★★ 1회 모의고사 임시용 코드.html`, `5. ★ 1회 모의고사 검토용 코드.html`을 다시 생성했다.
- `02. 작업 목록/(관련 자료)/모의고사_1-10회_문항_정답_정리_PDF대조완료.xlsx` 기준으로 `02. 작업 목록/mock-exam` 1~10회 데스크톱/모바일 문제 데이터를 재검증했다.
- 검증 결과 20개 HTML 파일 모두 각 80문항이며, 데스크톱/모바일 문제 데이터는 서로 일치했고, 엑셀 기준 정답번호 불일치는 0건이었다.
- 엑셀의 문제/보기 텍스트와 HTML 텍스트는 문항 설명 포함 여부, 마침표/띄어쓰기, 이미지 보기 표기 등에서 차이가 있었다. 다만 엑셀 원본에도 문맥상 명백히 의심되는 보기 텍스트가 있어 HTML 문제 데이터를 엑셀 텍스트로 일괄 덮어쓰지는 않았다.

## 3. 수정한 파일과 수정 이유

| 파일/폴더 | 수정 이유 |
| --- | --- |
| `mock-exam/desktop-01.html` ~ `mock-exam/desktop-10.html` | 1~10회 데스크톱용 모의고사 UI, 결과 페이지, 문제/정답 데이터 보정 |
| `mock-exam/mobile-01.html` ~ `mock-exam/mobile-10.html` | 1~10회 모바일용 모의고사 UI, 스마트폰/태블릿 반응형 처리, 문제/정답 데이터 보정 |
| `quiz-1400/login.html` | 올리사이트/퀴즈용 로그인 페이지 추가 |
| `quiz-1400/mobile-01.html` | 모바일 화면에서 불필요한 세로 스크롤이 남지 않도록 조정 |
| `AGENTS.md` | 작업 종료 정리 요청 시 Git 상태 확인, 변경 요약, `WORKLOG.md` 갱신, 검증 기록, 남은 작업 정리, 명시적 허용 시 커밋/푸시 규칙 추가 |
| `WORKLOG.md` | A/B 컴퓨터에서 이어 작업하기 위한 핵심 작업 요약 기록 |
| `01. 청각장애인 요양보호사/` | 청각장애인 요양보호사 관련 자료를 별도로 관리하기 위한 빈 분류 폴더 |
| `02. 작업 목록/` | 실제 작업 대상인 `mock-exam`, `quiz-1400`, `vocabulary`를 관리하기 위한 분류 폴더 |
| `AGENTS.md` | 앞으로 `mock-exam`, `quiz-1400` 코드 수정 시 OneDrive 대응 파일에도 동일 변경을 반영하도록 작업 규칙 추가 |

## 4. 실행한 테스트 또는 확인한 내용

- Git 상태 확인:
  - 브랜치: `main`
  - 원격 저장소: `origin` -> `https://github.com/jlysm91/deaf-care-worker-exam.git`
  - `HEAD`와 `origin/main` 동기화 여부 확인
- `mock-exam` 전체 데이터 검증:
  - 데스크톱 10개 파일, 모바일 10개 파일 모두 각 80문항인지 확인
  - 데스크톱/모바일의 문항, 보기, 정답, 영상 ID 데이터가 서로 일치하는지 확인
  - 통합 엑셀 기준 800문항 정답번호 불일치가 0건인지 확인
- `git diff --check`로 공백 오류 및 기본 diff 오류 여부를 확인했다.
- 사용자가 모바일/태블릿 UI 적용 상태를 실제 화면에서 확인했다.
- 마지막으로 반영된 주요 커밋:
  - `4e82806 Add quiz login page and mobile scroll fix`
  - `aa038d1 Correct integrated mock exam answers`
  - `5291a36 Correct mock exam PDF content`
  - `c169bc5 Apply first mock exam UI to rounds 2-10`
- 폴더 구조 정리 확인:
  - `01. 청각장애인 요양보호사`, `02. 작업 목록` 확인
  - `02. 작업 목록` 아래에 `mock-exam`, `quiz-1400`, `vocabulary` 보관 확인
  - OneDrive 기준 경로와 `01. 합격!! 모의고사`, `02. 합격!! 1400題` 폴더 존재 확인
  - `git status -sb`로 폴더 이동으로 인한 경로 변경 상태 확인
- OneDrive 1회 모의고사 최종 코드 폴더 검토:
  - 총 29개 파일 확인
  - `.bak-*` 백업 파일 16개, `-DESKTOP-N9UCJ8E` 충돌 파일 5개 확인
  - `2-1. ★★★ 1회 모의고사 코드 - 데스크톱용(2차).html`, `2-2. ★★★ 1회 모의고사 코드 - 모바일용.html`은 80문항이나 로컬 기준 파일보다 1회 문구 보정이 덜 반영됨
  - `3. ★★★ 1회 모의고사 퀴즈 문제 데이터 코드.html`, `4. ★★ 1회 모의고사 임시용 코드.html`, `5. ★ 1회 모의고사 검토용 코드.html`은 `모0120` 문항 ID가 빠지고 과거 오타가 남아 있어 실사용 파일로 부적절함
- OneDrive 1회 모의고사 최종 코드 폴더 정리 확인:
  - 정리 실행 시점에 표시된 삭제 후보 10개 파일 삭제
  - 남은 파일은 로그인 코드, 데스크톱용 최종 코드, 모바일용 최종 코드 3개
  - OneDrive 데스크톱/모바일 최종 코드 파일이 로컬 `02. 작업 목록/mock-exam/desktop-01.html`, `mobile-01.html`과 SHA-256 해시 기준으로 동일함을 확인
  - 데스크톱/모바일 최종 코드 모두 `모0101`~`모0180` 80개 문항 ID 확인
  - 과거 오타 패턴 `요양보서비스`, `허루`, `심한 화상이었면`, `30분 정도로`, `다리를 통로`, `완전히 관심`, `굽은 자세` 등이 남아 있지 않음을 확인
- OneDrive 1회 모의고사 대비용 파일 복구 확인:
  - `3. ★★★ 1회 모의고사 퀴즈 문제 데이터 코드.html`은 로컬 최신 `desktop-01.html`의 `quizRawData` 블록 기준으로 재생성
  - `4. ★★ 1회 모의고사 임시용 코드.html`, `5. ★ 1회 모의고사 검토용 코드.html`은 로컬 최신 `desktop-01.html`과 SHA-256 해시 기준 동일
  - 복구한 3개 파일 모두 `모0101`~`모0180` 80개 문항 ID 확인
  - 복구한 3개 파일 모두 과거 오타 패턴이 남아 있지 않음을 확인
- `mock-exam` 1~10회 엑셀 대조 검증:
  - 기준 엑셀 `전체문항` 시트에서 800문항을 읽어 대조
  - `02. 작업 목록/mock-exam/desktop-01.html` ~ `desktop-10.html`, `mobile-01.html` ~ `mobile-10.html` 모두 `quizRawData` 80문항 확인
  - 데스크톱/모바일 같은 회차의 `number`, `question`, `options`, `correctAnswer`, `videoId` 일치 확인
  - 엑셀 기준 `correctAnswer` 불일치 0건 확인
  - 엑셀 텍스트와 HTML 텍스트의 차이는 발견됐지만, 엑셀 자체에 의심 행이 있어 코드에 일괄 반영하지 않음
- 사용자가 추가 제공한 PDF 10개를 이미지 렌더링 후 Windows OCR과 페이지 이미지 육안 확인으로 재검수했다.
  - PDF는 텍스트 추출이 되지 않는 스캔형 파일이라 `pypdfium2` 렌더링 이미지와 Windows OCR 결과를 함께 사용했다.
  - 엑셀 기준 정답번호는 맞았지만, PDF 원문 기준 문제/보기 문구가 실제로 다른 항목이 있어 `mock-exam` 1, 2, 3, 4, 5, 8회 데스크톱/모바일 파일을 수정했다.
  - 주요 수정: 2회 5번 보기 ④/⑤, 2회 50번 문항, 2회 57번 보기 배열, 2회 75번 보기 배열, 3회 18번 정답 보기, 4회 41번 정답 보기, 8회 54번 보기 배열 등.
  - `02. 작업 목록/mock-exam`의 수정 회차 12개 HTML은 모두 80문항이며, 데스크톱/모바일 같은 회차 데이터가 일치함을 확인했다.
  - OneDrive `01. 합격!! 모의고사`의 대응 주력 최종 코드 파일(`2-1` 데스크톱용, `2-2` 모바일용) 12개를 로컬 최신 파일과 동일하게 맞췄고 SHA-256 해시 일치를 확인했다.
  - OneDrive 대응 데이터 전용 최종 파일(`3. ★★★ ... 퀴즈 문제 데이터 코드.html`) 6개는 로컬 최신 `desktop-XX.html`의 `quizRawData` 블록 기준으로 재생성했고, 80문항 데이터가 로컬과 일치함을 확인했다.
  - OCR 임시 산출물 `.tmp_pdf_ocr`, `.tmp_pdf_pages`는 검증 후 삭제했다.
  - `git diff --check`는 통과했으며, Git의 CRLF 변환 경고만 표시됐다.

## 5. 아직 남은 작업

- 새 오타 검수표나 원본 PDF 기준 추가 수정 요청이 있으면 문제/보기/정답만 좁게 수정한다.
- 실제 GitHub Pages 또는 올리사이트 반영 화면에서 캐시가 남아 있을 수 있으므로 필요 시 새로고침 후 확인한다.
- `quiz-1400`의 나머지 회차나 로그인 흐름을 추가로 손볼 경우, `mock-exam`과 섞지 말고 별도 범위로 작업한다.
- 새 컴퓨터에서 이어 작업할 때는 먼저 원격 최신 상태를 가져와 로컬 상태를 맞춘다.
- 현재 `01. 청각장애인 요양보호사`는 빈 폴더라 Git에는 표시되지 않을 수 있다. 필요하면 실제 목록 파일을 추가한다.
- `mock-exam`, `quiz-1400` 수정 시 OneDrive 쪽 대응 파일 구조가 로컬과 다르므로 최종 코드 파일을 먼저 확인한다. 중복 백업 파일이나 `-DESKTOP-N9UCJ8E` 같은 충돌 파일은 사용자가 요청하지 않는 한 수정하지 않는다.
- OneDrive 1회 모의고사 최종 코드 폴더는 주력 3개 파일과 대비용 3개 파일, 총 6개 파일을 유지한다. 이후 1회 모의고사 수정 시 주력 데스크톱/모바일 파일을 우선 반영하고, 필요 시 대비용 파일도 최신 기준으로 맞춘다.
- 엑셀 텍스트와 HTML 텍스트를 완전히 맞추려면 먼저 엑셀 원본의 의심 행을 원본 PDF/docx로 재확인해야 한다. 예: 3회 10번 보기처럼 문항 주제와 맞지 않는 보기 텍스트가 `정상` 상태로 남아 있는 행이 있다.

## 6. 다음 컴퓨터에서 이어서 작업할 때 주의할 점

- 작업 시작 전 반드시 실행한다.
  - `git status -sb`
  - `git fetch origin main`
  - `git rev-list --left-right --count HEAD...origin/main`
- 로컬이 뒤처져 있으면 먼저 `git pull --ff-only origin main`으로 최신화한다.
- 작업 중 로컬 변경이 있으면 덮어쓰지 말고 먼저 변경 파일을 확인한다.
- 문제/정답 데이터는 임의로 바꾸지 말고 원본 PDF, 정답지, 검수 엑셀 중 확인 가능한 기준이 있을 때만 수정한다.
- `videoId`는 문제 데이터와 연결되어 있으므로 문제/보기/정답 수정 중에도 함부로 삭제하거나 바꾸지 않는다.
- 데스크톱용과 모바일용은 같은 회차의 문제 데이터가 일치해야 한다.
- `02. 작업 목록/mock-exam` 또는 `02. 작업 목록/quiz-1400` 코드 파일을 수정하면 OneDrive 기준 경로의 대응 파일에도 동일 변경을 반영하고, 작업 종료 전 반영 여부를 확인한다.
- GitHub Pages나 외부 사이트는 반영 지연 또는 브라우저 캐시가 있을 수 있다.
- 민감 정보, 로그인 정보, API 키는 이 파일이나 코드에 기록하지 않는다.

## 7. 2026-06-04 작업 종료 요약

- 1~10회 모의고사 코드 작업은 종료 상태다.
- 마지막 반영 커밋은 `4151e87 Correct mock exams from PDF review`이며, `main` 브랜치는 `origin/main`과 동기화되어 있었다.
- PDF 10개를 스캔 이미지 기준으로 재검수했고, 확인된 문제/보기 문구 오류는 `02. 작업 목록/mock-exam`의 데스크톱용/모바일용 파일에 반영했다.
- 수정 대상은 1, 2, 3, 4, 5, 8회 모의고사 코드 파일이며, 같은 회차의 데스크톱용/모바일용 문제 데이터 일치를 확인했다.
- OneDrive `01. 합격!! 모의고사`의 대응 주력 최종 코드 파일과 데이터 전용 최종 파일에도 동일 변경을 반영했다.
- 검증 결과 `mock-exam` 데스크톱용 10개, 모바일용 10개 파일은 모두 각 80문항이고, 데스크톱/모바일 같은 회차 데이터가 일치했다.
- `git diff --check`는 통과했고, OCR 임시 산출물 `.tmp_pdf_ocr`, `.tmp_pdf_pages`는 삭제했다.
- 이번 종료 정리 요청 전 기준 Git 작업 트리는 깨끗했다.
- 이번 종료 정리에서는 코드 파일을 추가 수정하지 않았고, `WORKLOG.md`에 종료 요약만 추가했다.

## 8. 2026-06-04 1400題 1부 다듬기 진행 요약

- GitHub 저장소 구조는 루트의 `mock-exam`, `quiz-1400`, `vocabulary`, `AGENTS.md`, `WORKLOG.md` 형식으로 유지하기로 했다. 로컬 정리용 `01/02` 폴더 구조를 GitHub에 반영하지 않는다.
- 이번 작업 범위는 `quiz-1400/desktop-01.html`, `quiz-1400/mobile-01.html` 2개 파일로 제한했다.
- `quiz-1400/login.html`과 OneDrive의 `1. ★★★ 1부 로그인 코드.html`은 사용자 지시에 따라 수정하지 않았다.
- 1400題 1부 결과 페이지를 모의고사 코드의 결과 UX처럼 `학습 요약`, `틀린 문항`, `미응답 문항`, `정답 문항` 칩 목록 방식으로 바꿨다.
- 모바일 1부는 결과 페이지 전환 후 하단 문제 풀이 버튼이 남지 않도록 `quiz-active` 상태일 때만 퀴즈 화면이 강제 표시되게 보정했다.
- 모바일 1부에 모의고사 모바일 기준의 태블릿 세로 화면 보기 카드 자동 높이/글자 크기 보정 로직을 추가했다.
- 문제 데이터, 정답, `videoId`, Firebase `examId`, `examResult1400` postMessage 제출 흐름은 바꾸지 않았다.
- OneDrive `02. 합격!! 1400題/1부/2. (★삭제 주의) 최종 코드/02. 개선판`의 데스크톱용/모바일용 최종 코드 파일에도 동일 변경을 반영했고 SHA-256 해시 일치를 확인했다.
- 검증:
  - `git diff --check` 통과. Git의 CRLF 변환 경고만 표시됨.
  - `quiz-1400/desktop-01.html`, `quiz-1400/mobile-01.html` 인라인 스크립트 문법 파싱 통과.
  - 1부 장별 데이터 개수 확인: 1장 33문항, 2장 103문항, 3장 29문항, 4장 64문항.
  - 데스크톱/모바일 1부의 장별 문제 데이터 일치 확인.
  - 로컬 브라우저에서 데스크톱 결과 화면, 스마트폰 결과 화면, 스마트폰 문제 화면, 태블릿 세로 문제 화면을 확인했다.
- 초기 1부 다듬기 변경은 사용자 요청에 따라 커밋/푸시까지 완료했다. 이후 사용자가 학습 결과 UI만 두 번째 사진 방식으로 되돌리도록 추가 요청했다.

## 9. 2026-06-04 1400題 1부 결과 UI 추가 복구 요약

- 사용자 요청에 따라 `quiz-1400/desktop-01.html`, `quiz-1400/mobile-01.html`의 학습 결과 UI만 두 번째 사진 기준의 `문항별 결과 (C: 정답 / M: 내 답)` 그리드/페이저 방식으로 되돌렸다.
- `학습 요약`, `틀린 문항`, `미응답 문항`, `정답 문항` 칩 목록 CSS와 렌더링 함수만 제거하고, 80문항 단위 결과 그리드와 이전/다음 페이지 이동을 복구했다.
- 로그인 코드, 제출/장 선택 흐름, 문제 데이터, 정답, `videoId`, Firebase `examId`, `examResult1400` postMessage 흐름, 모바일/태블릿 문제 화면 보정은 바꾸지 않았다.
- OneDrive `02. 합격!! 1400題/1부/2. (★삭제 주의) 최종 코드/02. 개선판`의 데스크톱용/모바일용 최종 코드 파일에도 동일 변경을 반영했고 SHA-256 해시 일치를 확인했다.
- 검증:
  - `git diff --check` 통과.
  - 데스크톱/모바일 인라인 스크립트 문법 파싱 통과.
  - 1부 장별 데이터 개수 확인: 1장 33문항, 2장 103문항, 3장 29문항, 4장 64문항.
  - 데스크톱/모바일 1부의 장별 문제 데이터 개수 일치 확인.
  - 임시 검증용 복사본을 headless Chrome으로 렌더링해 데스크톱 결과 화면은 10열, 모바일 결과 화면은 5열 문항별 그리드로 표시되고 칩 UI가 남아 있지 않음을 확인했다.

## 10. 2026-06-04 1400題 1부 모바일 세로 화면 추가 조정 요약

- 사용자 요청에 따라 `quiz-1400/mobile-01.html`만 수정했다. 로그인 코드, 데스크톱용 코드, 문제 데이터, 정답, `videoId`, Firebase `examId`, 제출/장 선택 흐름은 바꾸지 않았다.
- 스마트폰/태블릿 세로 화면에서 빠른 문항 이동 패널이 과도하게 크게 뜨지 않도록 하단 팝업 폭, 화살표, 버튼 높이, 마지막 페이지 빈 칸 표시를 조정했다.
- 스마트폰 세로 화면의 스크롤 판정을 1회 모의고사 모바일 방식과 맞춰 `#quiz-content.scroll-needed/no-scroll-needed` 기준으로 다시 조정했다.
  - 문제 내용과 5지선다가 한 화면에 맞으면 `#quiz-content.no-scroll-needed`가 붙고 상하 스크롤이 작동하지 않는다.
  - 내용이 넘치면 `#quiz-content.scroll-needed`가 붙어 문제 내용과 5지선다가 함께 상하 스크롤된다.
  - `#options`는 별도 스크롤 컨테이너로 만들지 않고 모의고사처럼 `#quiz-content` 안에서 함께 움직인다.
- 닫힌 상태의 빠른 문항 이동 그리드가 보기 카드 위에 투명하게 남아 터치를 가로막지 않도록 `visibility`와 `pointer-events`를 차단했다.
- OneDrive `02. 합격!! 1400題/1부/2. (★삭제 주의) 최종 코드/02. 개선판/3. ★★★ 1부 코드 - 모바일용.html`에도 동일 변경을 반영했고 SHA-256 해시 일치를 확인했다.
- 검증:
  - `git diff --check` 통과.
  - `quiz-1400/mobile-01.html` 인라인 스크립트 문법 파싱 통과.
  - headless Chrome 390x844 스마트폰 세로 화면에서 1장 1번 기준 `#quiz-content.no-scroll-needed`, `overflowY: hidden`, 문서/보기/문제 영역 스크롤값 모두 0임을 확인했다.
  - headless Chrome 390x640 스마트폰 세로 화면에서 오버플로 발생 시 `#quiz-content.scroll-needed`가 적용되고, 보기 카드 위 터치 드래그로 `#quiz-content.scrollTop`이 증가하며 문서 스크롤은 0임을 확인했다.
  - 닫힌 빠른 문항 이동 그리드는 `visibility: hidden`, `pointer-events: none`이고, 열린 상태에서는 `visible/auto`로 돌아오는 것을 확인했다.
  - 1부 전체 229문항을 390x844와 390x640 스마트폰 세로 화면으로 스캔해 오버플로 여부와 `scroll-needed`, `no-scroll-needed` 적용 여부가 어긋나는 문항이 없음을 확인했다.
  - headless Chrome 390x844 및 768x1024 세로 화면에서 빠른 문항 이동 패널의 2장 1번/103번 표시 높이를 확인했다.

## 11. 2026-06-04 1400題 1부 모바일 빠른 문항 이동 시범 조정 요약

- 사용자 요청에 따라 `quiz-1400/mobile-01.html`만 수정했다. 로그인 코드, 데스크톱용 코드, 문제 데이터, 정답, `videoId`, Firebase `examId`, 제출/장 선택 흐름은 바꾸지 않았다.
- 스마트폰 세로 화면에서 빠른 문항 이동 버튼이 아래쪽에서 잘려 보이던 문제를 보정했다.
  - 버튼 실제 높이가 이전 CSS의 `.nav-btn` 변수에 덮여 계산 높이보다 커지는 부분을 막고, 빠른 문항 이동 전용 `--quick-nav-btn-height` 기준으로 버튼/화살표/그리드 높이를 맞췄다.
  - 패널 내부 그리드는 세로 중앙 정렬되도록 해서 마지막 페이지처럼 문항 수가 적을 때도 아래로 붙지 않게 했다.
- 80번 다음 페이지처럼 마지막 줄 번호가 적은 경우, 남은 번호가 왼쪽부터 시작하지 않고 10열 그리드의 가운데에 오도록 첫 번호의 `grid-column-start`를 계산해 적용했다.
- OneDrive `02. 합격!! 1400題/1부/2. (★삭제 주의) 최종 코드/02. 개선판/3. ★★★ 1부 코드 - 모바일용.html`에도 동일 변경을 반영했고 SHA-256 해시 일치를 확인했다.
- 검증:
  - `git diff --check` 통과.
  - `quiz-1400/mobile-01.html` 인라인 스크립트 문법 파싱 통과.
  - headless Chrome 390x844 스마트폰 세로 화면에서 빠른 문항 이동 1~80번 페이지가 잘리지 않고, 81~103번 페이지는 같은 패널 높이 안에서 세로 중앙에 배치됨을 확인했다.
  - headless Chrome 390x640 스마트폰 세로 화면에서도 1~80번 페이지와 81~103번 페이지가 잘리지 않고, 101~103번이 10열 그리드 가운데에 배치됨을 확인했다.

## 2026-06-05 1400題 1~4부 문제/정답 데이터 대조 및 반영 요약

- 기준 자료: OneDrive `00. 문제 및 정답 내용 정리/1400題_1_2_3_4부_문항정답_PDF원문대조_최종본.xlsx`
- 대상 코드: `quiz-1400/desktop-01.html` ~ `desktop-04.html`, `quiz-1400/mobile-01.html` ~ `mobile-04.html`
- 1~4부 전체 1190문항을 `부-장-절-문항번호` 키 기준으로 코드 데이터와 대조했다.
- 코드의 기존 `correctAnswer`를 정답 기준으로 유지하고, Excel/PDF 정리본에서 확인 가능한 문제/보기 OCR성 오탈자를 수정했다.
- 수정 범위는 문제/보기 텍스트 중심이며 `videoId`, 제출/결과 흐름, Firebase/postMessage 흐름은 변경하지 않았다.
- OneDrive 원본 관리 폴더의 1400題 1~4부 최종 코드 HTML 12개 파일에도 동일한 문제/보기 데이터 변경을 반영했다.
- Excel 정리본의 `문항정리_1_2_3_4부`, `3부_PDF원문보완완료`, `3부_수정내역` 시트도 코드 기준과 맞도록 갱신했다.
- 검증 결과:
  - 원격 GitHub Pages 코드 기준 데스크톱/모바일 데이터 수: 1부 229문항, 2부 211문항, 3부 509문항, 4부 241문항
  - 데스크톱/모바일 동일성: 1~4부 모두 일치
  - Excel 정리본과 GitHub 원격 코드 최종 대조: 1190문항 기준 누락 0건, 추가 0건, 불일치 0건
- GitHub 반영:
  - 최종 원격 커밋: `0e3b5e20764d917a4e78e4743ef9145dbec120b8`
  - 커밋 메시지: `Correct 1400 quiz data text`
  - 작성자/커미터: `Codex <267193182+codex@users.noreply.github.com>`
- 작업 중 임시 GitHub Actions workflow를 사용했으나 최종 원격 트리에는 임시 workflow 파일이 남지 않도록 정리했다.
- 이번 작업 범위 밖의 UI/CSS 변경은 최종 GitHub 반영분에서 제외했다.

## 2026-06-05 1회 모의고사/1400題 1부 모바일 빠른 문항 이동 시범 조정

- 사용자 승인 전 전체 반영을 피하기 위해 `mock-exam/mobile-01.html`, `quiz-1400/mobile-01.html`만 수정했다.
- 1400題 1부 모바일 빠른 문항 이동에서 선택한 답을 문항 번호 옆에 작은 `data-answer` 표시로 붙였다. 3자리 문항에서도 숫자와 선택 답이 겹치지 않도록 번호와 답 표시를 분리했다.
- 1회 모의고사 모바일 빠른 문항 이동은 기존 80문항 전체 표시 구조를 유지하고, 패널 폭·내부 여백·간격·버튼 높이만 소폭 조정했다.
- OneDrive 대응 최종 모바일 파일 2개에도 동일본을 반영하고 SHA-256 해시 일치를 확인했다.
- 검증:
  - `mock-exam/mobile-01.html`, `quiz-1400/mobile-01.html` 인라인 스크립트 문법 파싱 통과.
  - headless Chrome 390x844 모바일 세로 화면에서 1400題 1부 2장 103번 선택 답 `(2)` 표시, 빠른 문항 이동 패널 overflow 없음 확인.
  - headless Chrome 390x844 모바일 세로 화면에서 1회 모의고사 빠른 문항 이동 패널 overflow 없음 확인.
- GitHub 커밋/푸시는 사용자 승인 전 시범 단계라 진행하지 않았다.

## 12. 2026-06-11 결과 페이지 공통 검증 시작 요약

- 모의고사와 1400題의 문제 풀이/점수 계산/저장 흐름은 서로 다르므로, 실제 HTML 코드 공통화 전에 결과 페이지 레이아웃 검증 기반을 먼저 추가했다.
- 새 파일 `tools/result-page-check.mjs`를 추가했다.
  - 별도 npm 패키지 없이 로컬 Chrome/Edge headless 모드를 CDP로 제어한다.
  - `mock-exam` 20개 파일과 `quiz-1400` 8개 파일의 결과 페이지 CSS를 대상으로 검증용 결과 화면을 구성한다.
  - 스마트폰, 짧은 스마트폰, 태블릿 세로, 태블릿 가로, 노트북, 데스크톱 화면 크기에서 가로 넘침, 주요 결과 요소 겹침, 결과 개수 일치를 검사한다.
  - `--target`, `--viewports`, `--screenshots` 옵션을 지원한다.
- 이번 단계에서는 `mock-exam` 또는 `quiz-1400` 실제 HTML 코드 파일을 수정하지 않았다. 따라서 OneDrive 대응 코드 파일 동기화는 필요하지 않았다.
- 검증:
  - 대표 파일 4개(`mock-exam/desktop-01.html`, `mock-exam/mobile-01.html`, `quiz-1400/desktop-01.html`, `quiz-1400/mobile-01.html`)를 2개 화면 크기로 먼저 검사했고 통과했다.
  - `node tools/result-page-check.mjs`로 전체 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합을 검사했고 통과했다.
- 다음 단계에서 실제 공통화 작업을 진행할 경우, 먼저 결과 렌더링 함수의 공통 데이터 형태를 정하고 `mock-exam`과 `quiz-1400`에는 각각 어댑터만 두는 방식으로 좁게 적용한다.

## 13. 2026-06-11 결과 페이지 검증 도구 자동 커밋/푸시 규칙 반영

- 사용자가 이번 결과 페이지 공통 검증 도구 작업은 별도 재요청 없이 커밋/푸시까지 진행하라고 명시했다.
- `AGENTS.md`에 기본 원칙은 명시 요청 후 커밋/푸시하되, 사용자가 특정 작업군에 대해 자동 커밋/푸시를 허용한 경우에는 검증 완료 후 별도 재확인 없이 커밋/푸시한다는 예외 규칙을 추가했다.
- 이번 자동 커밋/푸시 대상은 `tools/result-page-check.mjs`, 관련 `AGENTS.md`/`WORKLOG.md` 갱신으로 제한한다.
- 원격 `origin/main`이 로컬보다 16커밋 앞서 있어 먼저 fast-forward로 최신화했고, `WORKLOG.md` 충돌은 원격 기록과 결과 페이지 검증 기록을 모두 보존하는 방식으로 정리했다.

## 14. 2026-06-11 1400題 1~4부 결과 페이지 한글/오답 중심 구조 반영

- 사용자 요청에 따라 `quiz-1400/desktop-01.html` ~ `desktop-04.html`, `quiz-1400/mobile-01.html` ~ `mobile-04.html`의 학습 결과 페이지를 `C:/M:` 표기 중심의 전체 문항 칸 구조에서 한글 표기와 오답 중심 복습 구조로 바꿨다.
  - 결과 요약은 `정답`, `오답`, `미응답`으로 표시한다.
  - 결과 상세는 `복습이 필요한 문항` 아래 `틀린 문항`, `미응답 문항` 목록으로 분리한다.
  - 각 항목은 `정답 n번`, `내 답 n번` 또는 `내 답 없음`으로 보여 주며, 기존 `C: 정답 / M: 내 답` 표기는 제거했다.
- 모바일용 1400題 1~4부 빠른 문항 이동은 문항 번호만 보이도록 통일했다.
  - 선택 답 `(2)` 같은 별도 텍스트 표시와 `data-answer` CSS를 제거했다.
  - 답변 완료 상태는 기존처럼 버튼 색상으로만 구분한다.
- 문제 데이터, 정답, `videoId`, Firebase 저장/결과 저장, 제출/postMessage 흐름은 변경하지 않았다.
- OneDrive 원본 관리 폴더의 1400題 1~4부 `02. 개선판` 데스크톱/모바일 최종 HTML 8개에도 동일 변경을 반영했고, 로컬 파일과 SHA-256 해시 일치를 확인했다.
- `tools/result-page-check.mjs`는 새 1400題 결과 구조를 검증하도록 갱신했다.
  - 모바일 파일 앞쪽 `document.write('<style>...')` 문자열을 실제 CSS로 잘못 잡지 않도록 가장 큰 실제 `<style>` 블록을 사용하게 고쳤다.
  - 검증 harness의 `body`에 `desktop-page`/`mobile-page` 클래스를 붙여 실제 CSS 적용 상태와 더 가깝게 검사하도록 했다.
  - `position: fixed/sticky` 요소는 의도적 오버레이로 보고 일반 형제 겹침 오류에서 제외했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - 1400題 1~4부 데스크톱/모바일 HTML 8개 인라인 스크립트 문법 파싱 통과.
  - `node tools/result-page-check.mjs`로 전체 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합을 검사했고 통과했다.
- 후속 사용자 요청으로 이 저장소의 기본 종료 규칙이 자동 커밋/푸시로 바뀌었으므로, 이 변경분도 함께 커밋/푸시 대상에 포함한다.

## 15. 2026-06-11 전체 작업 자동 커밋/푸시 기본 규칙 반영

- 사용자가 앞으로는 별도 지시 없이도 작업 완료 후 Codex가 알아서 커밋/푸시해야 한다고 명시했다.
- `AGENTS.md`의 기존 규칙을 특정 작업군 예외 방식에서 저장소 전체 기본 자동 커밋/푸시 방식으로 바꿨다.
  - Codex가 코드, 문서, 도구 파일을 수정한 경우 작업 완료 후 관련 검증, OneDrive 대응 파일 확인, `WORKLOG.md` 갱신, `git status` 확인, 스테이징, 커밋, `origin/main` 푸시를 기본 종료 흐름으로 삼는다.
  - 사용자가 해당 작업에서 `커밋하지 마`, `푸시하지 마`, `검토만 해`처럼 명시적으로 금지한 경우에만 자동 커밋/푸시를 하지 않는다.
  - 검증 실패, 원격 선행 커밋, 병합 충돌, 인증 문제처럼 바로 푸시할 수 없는 상황은 가능한 범위에서 해결을 시도하고, 해결 불가능한 차단 사유만 보고한다.
- 이 규칙 변경에 따라 직전 1400題 1~4부 결과 페이지/모바일 빠른 문항 이동 개선 작업도 이번 커밋/푸시에 포함한다.

## 16. 2026-06-11 1400題 결과 페이지 오답 중심/미응답 접기 재조정

- 사용자 피드백에 따라 직전 결과 페이지의 하단 중복 요약 카드 구조를 줄이고, 1400題 1~4부 데스크톱/모바일 결과 화면을 오답 중심 구조로 다시 조정했다.
  - 하단 `정답/오답/미응답` 요약 카드 3개를 제거했다.
  - `틀린 문항`을 첫 영역으로 크게 보여 주고, `미응답 문항`은 기본 접힘 상태의 `details` 영역으로 숨겼다.
  - 미응답을 펼쳤을 때는 반복적인 `정답/내 답 없음` 카드 대신 문항 번호 칩만 보이게 줄였다.
- 상단 결과 요약과 하단 복습 목록이 서로 다른 숫자를 보이지 않도록 `analyzeResultAnswers()` 분석 함수를 추가하고, `showResult()`와 `renderResultGrid()`가 같은 계산 결과를 쓰도록 바꿨다.
- 모바일 빠른 문항 이동의 번호-only 구조와 선택 답 텍스트 제거는 이전 불편을 해결한 부분이므로 유지했다.
- 문제 데이터, 정답, `videoId`, Firebase 저장/결과 저장, 제출/postMessage 흐름은 변경하지 않았다.
- OneDrive 원본 관리 폴더의 1400題 1~4부 `02. 개선판` 데스크톱/모바일 최종 HTML 8개에도 동일 변경을 반영했고, 로컬 파일과 SHA-256 해시 일치를 확인했다.
- `tools/result-page-check.mjs`도 새 구조에 맞춰 1400題 결과 페이지에서 접힌 미응답 영역과 미응답 번호 칩을 검사하도록 갱신했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - 1400題 1~4부 데스크톱/모바일 HTML 8개 인라인 스크립트 문법 파싱 통과.
  - 대표 스크린샷 확인: `quiz-1400/desktop-01.html` 1366x768, `quiz-1400/mobile-01.html` 390x844에서 오답 중심/미응답 접기 구조가 정상 표시됨.
  - `node tools/result-page-check.mjs`로 전체 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합을 검사했고 통과했다.

## 17. 2026-06-11 결과 페이지 미응답 상세 제거 및 빠른 문항 이동 답 배지 통일

- 사용자 피드백에 따라 1400題 1~4부 데스크톱/모바일 결과 페이지의 하단 `미응답 문항` 상세 영역을 완전히 제거했다.
  - 상단 결과 요약의 `미응답` 통계는 유지했다.
  - 하단 복습 영역은 `틀린 문항`만 보여 주도록 줄였다.
  - 미응답 상세 영역을 제거한 만큼 `틀린 문항` 목록의 세로 표시 높이를 조금 늘렸다.
- 빠른 문항 이동 표기를 모의고사 1~10회와 1400題 1~4부에 다시 통일했다.
  - 선택 답이 있는 문항은 `28 [1]`처럼 보이도록 했다.
  - 실제 구현은 문항 번호와 답을 `nav-number`, `nav-answer` span으로 분리해 100번대 문항에서 텍스트가 잘리지 않도록 했다.
  - 모의고사 모바일은 기존처럼 1~80번이 한 창에 보이는 구조를 유지했다.
  - 1400題 모바일은 100문항 이상에서 폭이 좁아지는 문제를 줄이기 위해 빠른 문항 이동을 40문항 단위로 나누도록 조정했다.
- 문제 데이터, 정답, `videoId`, Firebase 저장/결과 저장, 제출/postMessage 흐름은 변경하지 않았다.
- OneDrive 원본 관리 폴더에도 동일 변경을 반영했다.
  - 모의고사 1~10회 `02. 개선판 (完)` 데스크톱/모바일 최종 HTML 20개를 동기화했다.
  - 1400題 1~4부 `02. 개선판` 데스크톱/모바일 최종 HTML 8개를 동기화했다.
  - 총 28개 OneDrive 대응 파일과 로컬 파일의 SHA-256 해시 일치를 확인했다.
- `tools/result-page-check.mjs`는 1400題 결과 페이지에서 하단 미응답 상세 요소가 렌더링되지 않아야 한다는 기준으로 갱신했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - 모의고사 1~10회와 1400題 1~4부 데스크톱/모바일 HTML 28개 인라인 스크립트 문법 파싱 통과.
  - 빠른 문항 이동 정적 검사 통과: 모의고사 20개, 1400題 8개, 1400題 모바일 40문항 단위 4개 파일 확인.
  - `node tools/result-page-check.mjs`로 전체 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합을 검사했고 통과했다.

## 18. 2026-06-11 빠른 문항 이동 답 배지 대비 및 오답 목록 7줄 기준 조정

- 사용자 피드백에 따라 모의고사 1~10회와 1400題 1~4부 데스크톱/모바일의 빠른 문항 이동 답 배지(`[n]`)를 흰 글자에서 벗어나 잘 보이도록 조정했다.
  - 답 배지는 흰 배경, 초록/주요색 글자, 얇은 테두리로 표시되도록 했다.
  - 선택 답이 있는 문항은 기존 의도대로 `28 [1]` 형식으로 보이도록 유지했다.
- 1400題 1~4부 데스크톱/모바일 결과 페이지의 틀린 문항 카드 문구를 `내 답`에서 `선택`으로 바꿨다.
- 1400題 결과 페이지 하단 `틀린 문항` 목록은 세로 7문항 기준까지 표시되도록 늘리고, 7줄을 넘으면 목록 내부에서 상하 스크롤되도록 조정했다.
  - 오답 카드 최소 높이를 54px로 고정하고, 목록 최대 높이를 446px로 맞췄다.
  - 결과 페이지의 상단 미응답 통계는 유지했고, 하단 미응답 상세 영역은 이전 결정대로 다시 추가하지 않았다.
- 문제 데이터, 정답, `videoId`, Firebase 저장/결과 저장, 제출/postMessage 흐름은 변경하지 않았다.
- OneDrive 원본 관리 폴더에도 동일 변경을 반영했다.
  - 모의고사 1~10회 `02. 개선판 (完)` 데스크톱/모바일 최종 HTML 20개를 동기화했다.
  - 1400題 1~4부 `02. 개선판` 데스크톱/모바일 최종 HTML 8개를 동기화했다.
  - 총 28개 OneDrive 대응 파일과 로컬 파일의 SHA-256 해시 일치를 확인했다.
- `tools/result-page-check.mjs`는 1400題 결과 페이지에서 `내 답` 문구가 남아 있지 않아야 하고, 7줄을 넘는 오답 목록이 내부 스크롤을 가져야 한다는 기준을 추가했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - 모의고사 1~10회와 1400題 1~4부 데스크톱/모바일 HTML 28개 인라인 스크립트 문법 파싱 통과.
  - 빠른 문항 이동 답 배지 정적 검사 통과: 모의고사 20개, 1400題 8개 확인.
  - Chrome DevTools Protocol로 `quiz-1400/desktop-01.html`을 열어 답 선택 후 빠른 문항 이동 첫 칸이 `[3]` 배지를 표시하고, 배지 글자색이 흰색이 아님을 확인했다.
  - `node tools/result-page-check.mjs`로 전체 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합을 검사했고 통과했다.

## 19. 2026-06-11 빠른 문항 이동 답 배지 박스 제거 및 정답 표기 초록색 적용

- 사용자 피드백에 따라 모의고사 1~10회와 1400題 1~4부 데스크톱/모바일의 빠른 문항 이동 `[n]` 표기에서 흰 배경, 테두리, 둥근 배지 효과를 제거했다.
  - `[n]`은 문항 번호 옆 텍스트로만 보이게 했다.
  - 문항 번호와 선택 답을 분리한 `nav-number`, `nav-answer` 구조는 100번대 잘림 방지를 위해 유지했다.
- 1400題 1~4부 데스크톱/모바일 결과 페이지의 `틀린 문항` 카드에서 `정답 n번` 부분만 초록색으로 표시되도록 했다.
  - `선택 n번`은 기존 색상 흐름을 유지했다.
  - 문제 데이터, 정답, `videoId`, Firebase 저장/결과 저장, 제출/postMessage 흐름은 변경하지 않았다.
- OneDrive 원본 관리 폴더에도 동일 변경을 반영했다.
  - 모의고사 1~10회 `02. 개선판 (完)` 데스크톱/모바일 최종 HTML 20개를 동기화했다.
  - 1400題 1~4부 `02. 개선판` 데스크톱/모바일 최종 HTML 8개를 동기화했다.
  - 총 28개 OneDrive 대응 파일과 로컬 파일의 SHA-256 해시 일치를 확인했다.
- `tools/result-page-check.mjs`는 1400題 결과 페이지에서 `정답 n번` 라벨이 별도 클래스로 표시되고 초록색을 사용하는지 검사하도록 갱신했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - 모의고사 1~10회와 1400題 1~4부 데스크톱/모바일 HTML 28개 인라인 스크립트 문법 파싱 통과.
  - 정적 검사 통과: 빠른 문항 이동 `[n]`에 배지형 배경/테두리/둥근 처리 없음, 1400題 8개 결과 카드에 `review-answer-correct` 적용 확인.
  - 대표 결과 스크린샷 확인: `quiz-1400/mobile-01.html` 390x844에서 `정답 n번`이 초록색으로 표시됨.
  - Chrome DevTools Protocol로 `quiz-1400/mobile-01.html`을 열어 답 선택 후 빠른 문항 이동 `[3]`에 별도 배경, 테두리, radius가 없음을 확인했다.
  - `node tools/result-page-check.mjs`로 전체 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합을 검사했고 통과했다.

## 20. 2026-06-11 빠른 문항 이동 선택 답 초록색 유지

- 사용자 피드백에 따라 빠른 문항 이동에서 현재 문항이면서 답을 선택한 칸이 흰색 글자로 바뀌지 않도록 조정했다.
  - 모의고사 1~10회와 1400題 1~4부 데스크톱/모바일 전체에 `active + answered` 상태의 초록색 우선 규칙을 추가했다.
  - `[n]` 답 표시는 흰 배경이나 테두리 없이 텍스트만 유지하되, 선택 후에도 초록색으로 보이게 했다.
  - 1400題 모바일의 `active` 흰색 강제 규칙을 `active.answered`일 때 초록색으로 다시 덮도록 보강했다.
- 문제 데이터, 정답, `videoId`, Firebase 저장/결과 저장, 제출/postMessage 흐름은 변경하지 않았다.
- OneDrive 원본 관리 폴더에도 동일 변경을 반영했다.
  - 모의고사 1~10회 `02. 개선판 (完)` 데스크톱/모바일 최종 HTML 20개를 동기화했다.
  - 1400題 1~4부 `02. 개선판` 데스크톱/모바일 최종 HTML 8개를 동기화했다.
  - 총 28개 OneDrive 대응 파일과 로컬 파일의 SHA-256 해시 일치를 확인했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - 모의고사 1~10회와 1400題 1~4부 데스크톱/모바일 HTML 28개 인라인 스크립트 문법 파싱 통과.
  - `node tools/result-page-check.mjs`로 전체 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합을 검사했고 통과했다.
  - Chrome DevTools Protocol로 `quiz-1400/mobile-01.html`을 열어 5번 답 선택 후 빠른 문항 이동 `1[5]`의 버튼 글자색과 `[5]` 글자색이 모두 `var(--success)` 초록색으로 계산됨을 확인했다.

## 21. 2026-06-11 빠른 문항 이동 선택 답 크기와 기준선 정렬

- 사용자 피드백에 따라 빠른 문항 이동의 `[n]` 답 표기가 문항 번호와 어긋나 보이지 않도록 조정했다.
  - 모의고사 1~10회와 1400題 1~4부 데스크톱/모바일 전체에서 `.nav-answer`의 글자 크기를 축소값 `0.78em`에서 부모 글자 크기 상속으로 변경했다.
  - `[n]`의 줄 높이와 기준선을 문항 번호와 같은 흐름으로 맞췄다.
  - 흰 배경, 테두리, 둥근 배지 효과는 다시 추가하지 않았다.
- 문제 데이터, 정답, `videoId`, Firebase 저장/결과 저장, 제출/postMessage 흐름은 변경하지 않았다.
- OneDrive 원본 관리 폴더에도 동일 변경을 반영했다.
  - 모의고사 1~10회 `02. 개선판 (完)` 데스크톱/모바일 최종 HTML 20개를 동기화했다.
  - 1400題 1~4부 `02. 개선판` 데스크톱/모바일 최종 HTML 8개를 동기화했다.
  - 총 28개 OneDrive 대응 파일과 로컬 파일의 SHA-256 해시 일치를 확인했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - 모의고사 1~10회와 1400題 1~4부 데스크톱/모바일 HTML 28개 인라인 스크립트 문법 파싱 통과.
  - `node tools/result-page-check.mjs`로 전체 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합을 검사했고 통과했다.
  - 정적 검사로 28개 HTML에서 `.nav-answer`가 `font-size: inherit`, `vertical-align: baseline`을 사용하고 배지형 배경/테두리 스타일이 없음을 확인했다.
  - Chrome DevTools Protocol로 `quiz-1400/mobile-01.html`을 열어 5번 답 선택 후 `1`과 `[5]`의 글자 크기, 줄 높이, 상단 위치, 높이가 동일함을 확인했다.

## 22. 2026-06-11 모의고사 모바일 빠른 문항 이동 40문항 페이지 적용

- 사용자 피드백에 따라 모의고사 1~10회 모바일 HTML의 빠른 문항 이동을 80문항 일괄 표시에서 40문항 단위 페이지 구조로 변경했다.
  - `NAV_PAGE_SIZE=40`을 추가하고, 1~40번 / 41~80번을 좌우 화살표로 이동하도록 했다.
  - 스마트폰 세로 화면에서는 5열 x 8줄로 표시되도록 자동 크기 계산을 조정했다.
  - 선택 답 표시는 기존 `1 [5]` 형식을 유지하고, 선택된 답 표시가 초록색으로 유지되도록 했다.
  - 데스크톱 모의고사 파일은 요청 범위 밖이므로 수정하지 않았다.
- 문제 데이터, 정답, `videoId`, Firebase 저장/제출 흐름은 변경하지 않았다.
- OneDrive 원본 관리 폴더의 모의고사 1~10회 `2-2. ★★★ n회 모의고사 코드 - 모바일용.html`에도 동일 변경을 반영했다.
  - `-DESKTOP-N9UCJ8E` 충돌 파일은 수정하지 않았다.
  - 로컬 `mock-exam/mobile-01.html`~`mobile-10.html`과 OneDrive 대응 파일 10개 SHA-256 해시 일치를 확인했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - 모의고사 모바일 10개 HTML의 inline script 20개 문법 파싱 통과.
  - 정적 검사로 모바일 10개 파일 모두 `NAV_PAGE_SIZE=40`, `nav-prev/nav-next`, `updateNavDisplay()`가 1회씩 존재하고, `FIXED_SIZE=80` 및 `Math.ceil(80/cols)`가 남아 있지 않음을 확인했다.
  - 로컬 Chrome + Playwright로 `mock-exam/mobile-01.html`을 390x844 모바일 viewport에서 열어 확인했다: 1페이지 1~40번, 2페이지 41~80번, 각 페이지 40개 버튼, 5열 배치, 좌우 화살표 활성/비활성 상태 정상, 선택 답 `[5]` 초록색 표시 정상.

## 23. 2026-06-12 모의고사 학습 결과 UI를 1400제 기준으로 통일

- 사용자 요청에 따라 모의고사 1~10회 데스크톱/모바일 학습 결과 화면을 1400題 1~4부와 같은 오답 중심 결과 UI로 맞췄다.
  - 결과 상단을 `학습 결과`, 회차 배지, 응시자명 구조로 통일했다.
  - 요약 카드에는 합격/불합격, 정답률, 정답, 오답, 미응답, 필기/실기 기준을 표시하도록 했다.
  - 하단 복습 영역은 `오답 중심 복습`으로 통일하고, 미응답 상세 목록은 중복 노출하지 않았다.
  - 틀린 문항 카드의 `정답 n번`은 초록색 강조를 사용하고, `선택 n번`은 일반 텍스트 흐름을 유지했다.
  - 태블릿 폭에서 결과 헤더와 요약 카드가 가로로 넘치지 않도록 결과 UI 전용 반응형 기준을 보강했다.
- 문제 데이터, 정답, `videoId`, Firebase 저장/제출 흐름, 빠른 문항 이동 문제풀이 UI는 변경하지 않았다.
- OneDrive 원본 관리 폴더의 모의고사 1~10회 `02. 개선판 (完)` 데스크톱/모바일 최종 HTML 20개에도 동일 변경을 반영했다.
  - `-DESKTOP-N9UCJ8E` 충돌 파일은 수정하지 않았다.
  - 로컬 `mock-exam/desktop-01.html`~`desktop-10.html`, `mock-exam/mobile-01.html`~`mobile-10.html`과 OneDrive 대응 파일 20개 SHA-256 해시 일치를 확인했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - `node tools/result-page-check.mjs`로 전체 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합을 검사했고 통과했다.
  - 모의고사 1~10회와 1400題 1~4부 데스크톱/모바일 HTML 28개 인라인 스크립트 문법 파싱 통과.
  - 모의고사 20개 파일의 결과 UI 한글 문구가 `?`로 깨지지 않고 `학습 결과`, `오답 중심 복습`, `틀린 문항`, `정답률`, `선택`, `응시자`로 들어간 것을 확인했다.
  - 앱 내 브라우저는 로컬 `file://` HTML 접근이 정책상 차단되어 우회하지 않았고, 결과 페이지 전용 자동 검증 스크립트와 정적 검증을 기준으로 확인했다.

## 24. 2026-06-12 모의고사 제출 후 상위 페이지 예전 결과 화면 노출 방지

- 사용자 확인 화면에서 여전히 `시험 결과`, `학습 요약`, `C4 / M3` 구조가 보이는 문제를 재확인했다.
  - GitHub Pages의 직접 `mock-exam/desktop-01.html`에는 새 `학습 결과`, `오답 중심 복습` UI가 배포되어 있었다.
  - 실제 사용 흐름에서는 모의고사 제출 후 `window.opener.postMessage(...)` 다음 `window.close()`가 실행되어, 상위 페이지의 예전 `examResult` 결과 렌더러가 보일 수 있음을 확인했다.
- 모의고사 1~10회 데스크톱/모바일 제출 흐름을 1400題처럼 현재 시험 창 안에서 결과를 계속 보여 주도록 조정했다.
  - `window.close()` 호출을 제거했다.
  - 새 `renderResultNav()`가 결과 요약을 그리기 전에 예전 `총점: ...` 텍스트를 먼저 넣던 코드를 제거했다.
  - 기존 `postMessage` 전송은 상위 페이지 연동 가능성을 위해 유지했다.
- 문제 데이터, 정답, `videoId`, Firebase 저장, Google Forms 제출, `postMessage` 데이터 전송 자체는 변경하지 않았다.
- OneDrive 원본 관리 폴더의 모의고사 1~10회 `02. 개선판 (完)` 데스크톱/모바일 최종 HTML 20개에도 동일 변경을 반영했다.
  - `-DESKTOP-N9UCJ8E` 충돌 파일은 수정하지 않았다.
  - 로컬 모의고사 HTML 20개와 OneDrive 대응 파일 20개 SHA-256 해시 일치를 확인했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - `node tools/result-page-check.mjs`로 전체 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합을 검사했고 통과했다.
  - 모의고사 1~10회와 1400題 1~4부 데스크톱/모바일 HTML 28개 인라인 스크립트 문법 파싱 통과.
  - 정적 검사로 모의고사 20개 파일에 `window.close()`와 예전 `scoreSpan.textContent` 총점 표시가 남아 있지 않음을 확인했다.
  - 로컬 Chrome + Playwright로 `mock-exam/desktop-01.html`을 제출 상태로 재현했고, 제출 후 페이지가 닫히지 않으며 `1회 모의고사 학습 결과`, `오답 중심 복습`, `틀린 문항`, `정답 n번 / 선택 n번`이 표시됨을 확인했다.

## 25. 2026-06-12 결과 복습 박스 폭 통일 및 단일 오답 카드 폭 제한

- 사용자 확인 화면에서 1400題 결과 화면은 요약 카드와 오답 복습 박스가 같은 폭으로 보이지만, 모의고사는 하단 오답 복습 박스만 좁게 표시되는 문제를 확인했다.
  - 모의고사 1~10회 데스크톱/모바일에 남아 있던 예전 결과 CSS의 `max-width: 760px` 제한이 새 결과 UI를 덮어쓰고 있었다.
  - 모의고사 결과 복습 박스를 1400題와 같은 `1120px` 기준 폭으로 맞췄다.
- 오답이 1문항뿐일 때 카드가 한 줄 전체로 과하게 늘어나는 문제를 줄였다.
  - 모의고사 1~10회와 1400題 1~4부 데스크톱/모바일 전체에 `result-review-single` 규칙을 추가했다.
  - 오답이 1개일 때는 카드 폭을 약 360px로 제한하고, 모바일 폭에서는 1열 전체 폭을 사용하도록 했다.
  - 오답이 여러 개일 때의 기존 그리드 배치는 유지했다.
- 1400題와 모의고사의 결과 복습 박스, 틀린 문항 카드, `정답 n번 / 선택 n번` 표기 규칙을 같은 기준으로 맞췄다.
- 문제 데이터, 정답, `videoId`, Firebase 저장, Google Forms 제출, `postMessage` 데이터 전송 흐름은 변경하지 않았다.
- OneDrive 원본 관리 폴더에도 동일 변경을 반영했다.
  - 모의고사 1~10회 `02. 개선판 (完)` 데스크톱/모바일 최종 HTML 20개를 동기화했다.
  - 1400題 1~4부 `02. 개선판` 데스크톱/모바일 최종 HTML 8개를 동기화했다.
  - 총 28개 OneDrive 대응 파일과 로컬 파일의 SHA-256 해시 일치를 확인했다.
- 검증:
  - `git diff --check` 통과.
  - `node tools/result-page-check.mjs`로 전체 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합을 검사했고 통과했다.
  - 모의고사 1~10회와 1400題 1~4부 데스크톱/모바일 HTML 28개 인라인 스크립트 문법 파싱 통과.
  - 로컬 Chrome + Playwright로 `mock-exam/desktop-01.html` 결과 화면을 2560x720에서 재현했고, 요약 카드와 오답 복습 박스가 모두 1120px, 단일 오답 카드가 360px로 계산됨을 확인했다.
  - 로컬 Chrome + Playwright로 `quiz-1400/desktop-01.html`의 결과 복습 DOM에 단일 오답 카드 규칙을 적용해 1120px 박스 안에서 카드가 360px로 계산됨을 확인했다.

## 26. 2026-06-12 모의고사 시작 시 새 창 실행 복구

- 사용자 확인에 따라 1~10회 모의고사에서 아이디/이름 입력 후 GitHub Pages 시험 창이 새로 뜨지 않고, 이후 `다시 풀기` 버튼을 눌러야 시험 화면이 보이는 흐름을 점검했다.
  - 모의고사 데스크톱/모바일 HTML 20개에는 `window.open()` 흐름이 없고, 숨겨진 `start-quiz-btn` 호출 시 현재 창에서 바로 `startQuiz(uid, name)`만 실행하고 있음을 확인했다.
  - `uid/name` 쿼리값이 이미 붙어 새 창으로 열린 경우에는 기존처럼 자동으로 `startQuiz()`를 실행하도록 유지했다.
  - `uid/name` 쿼리값이 없는 상태에서 시작 버튼이 호출되는 경우에는 현재 모의고사 URL에 `uid/name`을 붙여 `_blank` 새 창으로 열도록 `openQuizLaunchWindow()` 흐름을 추가했다.
  - 브라우저 팝업 차단으로 새 창이 열리지 않는 경우에는 팝업 차단 해제 안내를 표시하도록 했다.
- 문제 데이터, 정답, `videoId`, Firebase 저장/제출, 결과 UI, 빠른 문항 이동 UI는 변경하지 않았다.
- OneDrive 원본 관리 폴더의 모의고사 1~10회 `02. 개선판 (完)` 데스크톱/모바일 최종 HTML 20개에도 동일 변경을 반영했다.
  - `-DESKTOP-N9UCJ8E` 충돌 파일은 수정하지 않았다.
  - 로컬 모의고사 HTML 20개와 OneDrive 대응 파일 20개 SHA-256 해시 일치를 확인했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - `node tools/result-page-check.mjs`로 전체 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합을 검사했고 통과했다.
  - 모의고사 1~10회 데스크톱/모바일 HTML 20개 인라인 스크립트 문법 파싱 통과.
  - 정적 검사로 모의고사 20개 파일 모두 `openQuizLaunchWindow(uid,name)` 시작 버튼 연결, `window.open(buildQuizLaunchUrl(...), "_blank")`, 쿼리값 자동 시작 `startQuiz(uid,name)`이 각각 1회씩 존재함을 확인했다.
  - 앱 내 브라우저는 `file://` 직접 접근이 정책상 차단되어, 로컬 HTTP 서버(`127.0.0.1:8765`)로 `mock-exam/desktop-01.html?uid=...&name=...`를 열어 `quiz-page`가 표시되고 `uid/name`이 반영되는 것을 확인했다.

## 27. 2026-06-12 모의고사 본창 유지 + 새 창 결과 표시 보정

- 사용자 재설명에 따라 원하는 흐름을 다시 정리했다.
  - 올리사이트 본 창은 아이디/이름 입력 화면을 그대로 유지한다.
  - 문제풀이와 학습 결과는 새 GitHub Pages 창에서만 표시한다.
  - 기존 결과가 있어도 본 창에서 결과 화면으로 바꾸지 않고, 새 창을 열어 GitHub Pages 결과/풀이 화면을 보여 준다.
- OneDrive 원본 관리 폴더의 모의고사 1~10회 `1. ★★★ n회 모의고사 로그인 코드.html`을 보정했다.
  - `olGo()`에서 Firebase 기존 결과 조회 후 본 창 결과 화면을 표시하던 흐름을 제거했다.
  - 버튼 클릭 즉시 `olGetExamUrl(uid, uname)`을 `_blank` 새 창으로 열도록 변경해 팝업 차단 가능성을 줄였다.
  - 새 창에서 오는 `examResult` 메시지는 더 이상 `olShowResult(e.data)`로 본 창을 바꾸지 않도록 했다.
- GitHub Pages용 모의고사 1~10회 데스크톱/모바일 HTML 20개에서도 `window.opener.postMessage(resultData, "*")` 결과 전달을 제거했다.
  - 새 창 안에서 문제풀이와 학습 결과가 계속 표시되는 흐름은 유지했다.
  - Firebase 저장, Google Forms 제출, 문제 데이터, 정답, 빠른 문항 이동, 결과 UI는 변경하지 않았다.
- OneDrive 원본 관리 폴더의 모의고사 1~10회 `02. 개선판 (完)` 데스크톱/모바일 최종 HTML 20개에도 동일 변경을 반영했다.
  - 로컬 모의고사 HTML 20개와 OneDrive 대응 파일 20개 SHA-256 해시 일치를 확인했다.
  - `-DESKTOP-N9UCJ8E` 충돌 파일은 수정하지 않았다.
- 검증:
  - `git diff --check` 통과.
  - `node tools/result-page-check.mjs`로 전체 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합을 검사했고 통과했다.
  - 모의고사 1~10회 데스크톱/모바일 HTML 20개 인라인 스크립트 문법 파싱 통과.
  - OneDrive 모의고사 로그인 코드 10개 인라인 스크립트 문법 파싱 통과.
  - 정적 검사로 로그인 코드 10개에서 `olGo()`가 새 창을 직접 열고, `fetch(dbURL)` 기존 결과 조회와 `olShowResult(e.data)` 본창 결과 표시가 남아 있지 않음을 확인했다.
  - 정적 검사로 GitHub Pages용 모의고사 20개 파일에 `postMessage(resultData)` 결과 전달이 남아 있지 않음을 확인했다.

## 28. 2026-06-12 모의고사 모바일 결과 헤더와 기준 문구 보정

- 사용자 확인 화면 기준으로 모의고사 모바일 학습 결과 UI가 1400題 모바일 결과 UI와 다르게 보이는 부분을 보정했다.
  - 모의고사 모바일 1~10회 결과 헤더에서 `응시자` 문구를 강제로 다음 줄 전체 폭으로 보내던 모바일 CSS 규칙을 제거했다.
  - 1400題 모바일처럼 배지, `학습 결과`, 응시자명이 가능한 한 같은 헤더 흐름에서 자연스럽게 배치되도록 했다.
  - `필기 n / 35 · 실기 n / 45`와 `합격 기준 필기 21문항 이상 · 실기 27문항 이상`을 별도 줄로 나누어 표시하도록 결과 기준 문구를 구조화했다.
- 문제 데이터, 정답, Firebase 저장/제출, 빠른 문항 이동, 데스크톱 결과 UI는 변경하지 않았다.
- OneDrive 원본 관리 폴더의 모의고사 1~10회 `02. 개선판 (完)` 모바일 최종 HTML 10개에도 동일 변경을 반영했다.
  - 로컬 `mock-exam/mobile-01.html`~`mobile-10.html`과 OneDrive 대응 파일 10개 SHA-256 해시 일치를 확인했다.
  - `-DESKTOP-N9UCJ8E` 충돌 파일은 수정하지 않았다.
- 검증:
  - `git diff --check` 통과.
  - `node tools/result-page-check.mjs`로 전체 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합을 검사했고 통과했다.
  - 모의고사 모바일 10개 HTML 인라인 스크립트 문법 파싱 통과.
  - 정적 검사로 모의고사 모바일 10개 파일 모두 `result-standard-line` 2줄 기준 문구가 들어가고, `.result-user-name { width:100%; ... }` 강제 줄바꿈 규칙이 남아 있지 않음을 확인했다.
## 29. 2026-06-12 1회 모의고사 학습 완료 안내 샘플 적용

- 1회 모의고사만 대상으로, GitHub Pages 문제풀이 창과 올리사이트 로그인 코드 사이에 학습 완료 안내 흐름을 샘플로 추가했다.
  - `mock-exam/desktop-01.html`, `mock-exam/mobile-01.html`의 `submitQuiz()`에서 결과 화면을 표시한 뒤 `mockExamComplete` 완료 신호만 opener로 보내도록 했다.
  - 점수, 정답, 선택 답안 같은 결과 데이터는 opener로 보내지 않고, 완료 여부와 1회 모의고사 식별 정보만 전달한다.
  - OneDrive 1회 로그인 코드에는 `ol_done_notice` 안내 영역과 `mockExamComplete` 수신 처리를 추가했다.
  - 올리사이트 본 창은 아이디/이름 입력 화면을 유지하고, 완료 신호를 받으면 "문제풀이가 완료되었습니다. 올리사이트의 학습 완료하기 버튼을 눌러주세요." 안내만 표시한다.
  - 학습 완료하기 버튼 자동 클릭은 적용하지 않았다.
- OneDrive 원본 관리 폴더 동기화:
  - 1회 모의고사 데스크톱 최종 코드와 `mock-exam/desktop-01.html`의 SHA-256 일치를 확인했다.
  - 1회 모의고사 모바일 최종 코드와 `mock-exam/mobile-01.html`의 SHA-256 일치를 확인했다.
  - 1회 모의고사 로그인 코드는 OneDrive 최종 코드 파일에 직접 반영했다.
- 검증:
  - `git diff --check` 통과.
  - `node --check tools/result-page-check.mjs` 통과.
  - `node tools/result-page-check.mjs` 통과: 28개 파일 x 7개 화면 크기, 총 196개 결과 페이지 조합.
  - 1회 데스크톱/모바일 HTML과 OneDrive 1회 로그인 코드의 인라인 스크립트 문법 검사 통과.
  - 로컬 HTTP 미리보기에서 `mockExamComplete` 메시지 수신 후 완료 안내가 `display: block`으로 표시됨을 확인했다.
