# WORKLOG

## 1. 현재 프로젝트 목표

- 청각장애인 요양보호사 자격시험 대비 모의고사 웹 파일을 관리한다.
- A컴퓨터와 B컴퓨터에서 같은 GitHub 저장소를 기준으로 이어 작업할 수 있게 한다.
- 핵심 작업 대상은 `mock-exam`, `quiz-1400`, `vocabulary` 폴더이며, 문제/정답 데이터는 원본 PDF, 정답지, 검수 엑셀 기준으로만 수정한다.

## 2. 지금까지 완료한 작업

- `mock-exam` 1~10회 데스크톱용/모바일용 파일을 Git 저장소에 정리했다.
- 1회 모의고사에서 만든 모바일/태블릿 UI 개선 사항을 2~10회 모의고사에도 동일하게 반영했다.
- 스마트폰에서 내용이 넘칠 때만 문항 영역이 세로 스크롤되도록 조정했다.
- 태블릿 세로 화면에서는 남는 공간이 과도하지 않도록 문제 영역 크기를 자동 조절하도록 수정했다.
- 결과 페이지의 틀린 문항, 미응답 문항, 정답 문항 칩 목록을 화면 크기에 맞게 반응형으로 정리했다.
- 통합 엑셀과 PDF/정답지 기준으로 1~10회 총 800문항의 정답번호를 재검토했다.
- 엑셀 기준으로 발견된 정답번호 불일치 43건을 데스크톱용과 모바일용 모두 수정했다.
- 명확한 문구 오타 일부를 원문 확인 후 수정했다.
  - 예: `허루` -> `하루`
  - 예: `뇌졸중으로 뇌가 손상` -> `뇌졸중으로 소뇌가 손상`
  - 예: `영양주머니를 영양액을` -> `영양주머니에 영양액을`
  - 예: 3회 23번 중복 보기 수정
- `quiz-1400/login.html`을 추가했고, `quiz-1400/mobile-01.html`의 모바일 스크롤 관련 수정도 반영했다.
- `AGENTS.md`에 사용자가 "작업 종료 정리해 주세요"라고 요청할 때 수행할 작업 종료 규칙을 추가했다.
- 최상위 폴더를 번호 형식으로 정리했다.
  - `01. 청각장애인 요양보호사`
  - `02. 작업 목록`
- 실제 작업 대상 폴더 `mock-exam`, `quiz-1400`, `vocabulary`는 삭제하지 않고 `02. 작업 목록` 아래로 이동했다.
- 사용자가 `03. 기존 프로젝트 파일`은 불필요하다고 판단해 삭제했다.
- 앞으로 `02. 작업 목록/mock-exam` 또는 `02. 작업 목록/quiz-1400` 코드 파일을 수정할 때는 OneDrive의 원본 관리 폴더에도 동일 변경을 반영하기로 했다.
  - 기준 경로: `C:\Users\COM-JHYUTG\OneDrive\바탕 화면\01. 개인용\01. 자기계발\01. 코딩\00. 업무 관련 코딩\1. 청각장애인 요양보호사 양성`
  - 모의고사: `01. 합격!! 모의고사`
  - 1400제: `02. 합격!! 1400題`
- OneDrive의 `01회 모의고사/02. 개선판 (完)/02. (★삭제 주의) 최종 코드` 폴더를 검토했다. 파일은 삭제하지 않고, 유지/삭제 후보만 분류했다.
- OneDrive의 `01회 모의고사/02. 개선판 (完)/02. (★삭제 주의) 최종 코드` 폴더를 정리했다. 로컬 최신 데스크톱/모바일 파일을 OneDrive 주력 파일에 반영한 뒤, 삭제 후보 파일을 제거해 3개 파일만 남겼다.
- 사용자의 요청으로 대비용 파일 3개를 복구했다. 삭제 당시 원본은 휴지통/주변 폴더에서 찾지 못해, 로컬 최신 `desktop-01.html` 기준으로 `3. ★★★ 1회 모의고사 퀴즈 문제 데이터 코드.html`, `4. ★★ 1회 모의고사 임시용 코드.html`, `5. ★ 1회 모의고사 검토용 코드.html`을 다시 생성했다.
- `02. 작업 목록/(관련 자료)/모의고사_1-10회_문항_정답_정리_PDF대조완료.xlsx` 기준으로 `02. 작업 목록/mock-exam` 1~10회 데스크톱/모바일 문제 데이터를 재검증했다.
- 검증 결과 20개 HTML 파일 모두 각 80문항이며, 데스크톱/모바일 문제 데이터는 서로 일치했고, 엑셀 기준 정답번호 불일치는 0건이었다.
- 엑셀의 문제/보기 텍스트와 HTML 텍스트는 문항 설명 포함 여부, 마침표/띄어쓰기, 이미지 보기 표기 등에서 차이가 있었다. 다만 엑셀 원본에도 문맥상 명백히 의심되는 보기 텍스트가 있어 HTML 문제 데이터를 엑셀 텍스트로 일괄 덮어쓰지는 않았다.

## 3. 수정한 파일과 수정 이유

| 파일/폴더 | 수정 이유 |
| --- | --- |
| `mock-exam/desktop-01.html` ~ `mock-exam/desktop-10.html` | 1~10회 데스크톱용 모의고사 UI, 결과 페이지, 문제/정답 데이터 보정 |
| `mock-exam/mobile-01.html` ~ `mock-exam/mobile-10.html` | 1~10회 모바일용 모의고사 UI, 스마트폰/태블릿 반응형 처리, 문제/정답 데이터 보정 |
| `quiz-1400/login.html` | 올리사이트/퀴즈용 로그인 페이지 추가 |
| `quiz-1400/mobile-01.html` | 모바일 화면에서 불필요한 세로 스크롤이 남지 않도록 조정 |
| `AGENTS.md` | 작업 종료 정리 요청 시 Git 상태 확인, 변경 요약, `WORKLOG.md` 갱신, 검증 기록, 남은 작업 정리, 명시적 허용 시 커밋/푸시 규칙 추가 |
| `WORKLOG.md` | A/B 컴퓨터에서 이어 작업하기 위한 핵심 작업 요약 기록 |
| `01. 청각장애인 요양보호사/` | 청각장애인 요양보호사 관련 자료를 별도로 관리하기 위한 빈 분류 폴더 |
| `02. 작업 목록/` | 실제 작업 대상인 `mock-exam`, `quiz-1400`, `vocabulary`를 관리하기 위한 분류 폴더 |
| `AGENTS.md` | 앞으로 `mock-exam`, `quiz-1400` 코드 수정 시 OneDrive 대응 파일에도 동일 변경을 반영하도록 작업 규칙 추가 |

## 4. 실행한 테스트 또는 확인한 내용

- Git 상태 확인:
  - 브랜치: `main`
  - 원격 저장소: `origin` -> `https://github.com/jlysm91/deaf-care-worker-exam.git`
  - `HEAD`와 `origin/main` 동기화 여부 확인
- `mock-exam` 전체 데이터 검증:
  - 데스크톱 10개 파일, 모바일 10개 파일 모두 각 80문항인지 확인
  - 데스크톱/모바일의 문항, 보기, 정답, 영상 ID 데이터가 서로 일치하는지 확인
  - 통합 엑셀 기준 800문항 정답번호 불일치가 0건인지 확인
- `git diff --check`로 공백 오류 및 기본 diff 오류 여부를 확인했다.
- 사용자가 모바일/태블릿 UI 적용 상태를 실제 화면에서 확인했다.
- 마지막으로 반영된 주요 커밋:
  - `4e82806 Add quiz login page and mobile scroll fix`
  - `aa038d1 Correct integrated mock exam answers`
  - `5291a36 Correct mock exam PDF content`
  - `c169bc5 Apply first mock exam UI to rounds 2-10`
- 폴더 구조 정리 확인:
  - `01. 청각장애인 요양보호사`, `02. 작업 목록` 확인
  - `02. 작업 목록` 아래에 `mock-exam`, `quiz-1400`, `vocabulary` 보관 확인
  - OneDrive 기준 경로와 `01. 합격!! 모의고사`, `02. 합격!! 1400題` 폴더 존재 확인
  - `git status -sb`로 폴더 이동으로 인한 경로 변경 상태 확인
- OneDrive 1회 모의고사 최종 코드 폴더 검토:
  - 총 29개 파일 확인
  - `.bak-*` 백업 파일 16개, `-DESKTOP-N9UCJ8E` 충돌 파일 5개 확인
  - `2-1. ★★★ 1회 모의고사 코드 - 데스크톱용(2차).html`, `2-2. ★★★ 1회 모의고사 코드 - 모바일용.html`은 80문항이나 로컬 기준 파일보다 1회 문구 보정이 덜 반영됨
  - `3. ★★★ 1회 모의고사 퀴즈 문제 데이터 코드.html`, `4. ★★ 1회 모의고사 임시용 코드.html`, `5. ★ 1회 모의고사 검토용 코드.html`은 `모0120` 문항 ID가 빠지고 과거 오타가 남아 있어 실사용 파일로 부적절함
- OneDrive 1회 모의고사 최종 코드 폴더 정리 확인:
  - 정리 실행 시점에 표시된 삭제 후보 10개 파일 삭제
  - 남은 파일은 로그인 코드, 데스크톱용 최종 코드, 모바일용 최종 코드 3개
  - OneDrive 데스크톱/모바일 최종 코드 파일이 로컬 `02. 작업 목록/mock-exam/desktop-01.html`, `mobile-01.html`과 SHA-256 해시 기준으로 동일함을 확인
  - 데스크톱/모바일 최종 코드 모두 `모0101`~`모0180` 80개 문항 ID 확인
  - 과거 오타 패턴 `요양보서비스`, `허루`, `심한 화상이었면`, `30분 정도로`, `다리를 통로`, `완전히 관심`, `굽은 자세` 등이 남아 있지 않음을 확인
- OneDrive 1회 모의고사 대비용 파일 복구 확인:
  - `3. ★★★ 1회 모의고사 퀴즈 문제 데이터 코드.html`은 로컬 최신 `desktop-01.html`의 `quizRawData` 블록 기준으로 재생성
  - `4. ★★ 1회 모의고사 임시용 코드.html`, `5. ★ 1회 모의고사 검토용 코드.html`은 로컬 최신 `desktop-01.html`과 SHA-256 해시 기준 동일
  - 복구한 3개 파일 모두 `모0101`~`모0180` 80개 문항 ID 확인
  - 복구한 3개 파일 모두 과거 오타 패턴이 남아 있지 않음을 확인
- `mock-exam` 1~10회 엑셀 대조 검증:
  - 기준 엑셀 `전체문항` 시트에서 800문항을 읽어 대조
  - `02. 작업 목록/mock-exam/desktop-01.html` ~ `desktop-10.html`, `mobile-01.html` ~ `mobile-10.html` 모두 `quizRawData` 80문항 확인
  - 데스크톱/모바일 같은 회차의 `number`, `question`, `options`, `correctAnswer`, `videoId` 일치 확인
  - 엑셀 기준 `correctAnswer` 불일치 0건 확인
  - 엑셀 텍스트와 HTML 텍스트의 차이는 발견됐지만, 엑셀 자체에 의심 행이 있어 코드에 일괄 반영하지 않음

## 5. 아직 남은 작업

- 새 오타 검수표나 원본 PDF 기준 추가 수정 요청이 있으면 문제/보기/정답만 좁게 수정한다.
- 실제 GitHub Pages 또는 올리사이트 반영 화면에서 캐시가 남아 있을 수 있으므로 필요 시 새로고침 후 확인한다.
- `quiz-1400`의 나머지 회차나 로그인 흐름을 추가로 손볼 경우, `mock-exam`과 섞지 말고 별도 범위로 작업한다.
- 새 컴퓨터에서 이어 작업할 때는 먼저 원격 최신 상태를 가져와 로컬 상태를 맞춘다.
- 현재 `01. 청각장애인 요양보호사`는 빈 폴더라 Git에는 표시되지 않을 수 있다. 필요하면 실제 목록 파일을 추가한다.
- `mock-exam`, `quiz-1400` 수정 시 OneDrive 쪽 대응 파일 구조가 로컬과 다르므로 최종 코드 파일을 먼저 확인한다. 중복 백업 파일이나 `-DESKTOP-N9UCJ8E` 같은 충돌 파일은 사용자가 요청하지 않는 한 수정하지 않는다.
- OneDrive 1회 모의고사 최종 코드 폴더는 주력 3개 파일과 대비용 3개 파일, 총 6개 파일을 유지한다. 이후 1회 모의고사 수정 시 주력 데스크톱/모바일 파일을 우선 반영하고, 필요 시 대비용 파일도 최신 기준으로 맞춘다.
- 엑셀 텍스트와 HTML 텍스트를 완전히 맞추려면 먼저 엑셀 원본의 의심 행을 원본 PDF/docx로 재확인해야 한다. 예: 3회 10번 보기처럼 문항 주제와 맞지 않는 보기 텍스트가 `정상` 상태로 남아 있는 행이 있다.

## 6. 다음 컴퓨터에서 이어서 작업할 때 주의할 점

- 작업 시작 전 반드시 실행한다.
  - `git status -sb`
  - `git fetch origin main`
  - `git rev-list --left-right --count HEAD...origin/main`
- 로컬이 뒤처져 있으면 먼저 `git pull --ff-only origin main`으로 최신화한다.
- 작업 중 로컬 변경이 있으면 덮어쓰지 말고 먼저 변경 파일을 확인한다.
- 문제/정답 데이터는 임의로 바꾸지 말고 원본 PDF, 정답지, 검수 엑셀 중 확인 가능한 기준이 있을 때만 수정한다.
- `videoId`는 문제 데이터와 연결되어 있으므로 문제/보기/정답 수정 중에도 함부로 삭제하거나 바꾸지 않는다.
- 데스크톱용과 모바일용은 같은 회차의 문제 데이터가 일치해야 한다.
- `02. 작업 목록/mock-exam` 또는 `02. 작업 목록/quiz-1400` 코드 파일을 수정하면 OneDrive 기준 경로의 대응 파일에도 동일 변경을 반영하고, 작업 종료 전 반영 여부를 확인한다.
- GitHub Pages나 외부 사이트는 반영 지연 또는 브라우저 캐시가 있을 수 있다.
- 민감 정보, 로그인 정보, API 키는 이 파일이나 코드에 기록하지 않는다.
